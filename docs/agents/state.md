# Состояние работы — читается ПЕРВЫМ

> Этот файл — текущее положение дел: что сдано, что применено у владельца,
> какой состав приложения, что ждёт решения. Новая сессия отвечает на
> вопрос «что происходит?» отсюда, а не обходом дерева.
> Правило: обновляется В ТОМ ЖЕ ПАТЧЕ, что и изменение (2–3 строки правки).
> Полная история — `docs/changelog.ru.md` (сверху новые); как устроено —
> `docs/agents/README.md` и `docs/agents/data-flows.md`.

Снимок: **2026-09-23** · ветка агента: `arena/01a0c8e2-fallout-2d20-companion`
· последний сданный патч: **314** · у владельца на main применено до **314**
(слово владельца: «всё шикарно»).

## Состав приложения (игровыми терминами)

- **Герои.** Менеджер: создание/загрузка, папки, дубликаты, аватары, перенос
  файлом, облачная синхронизация (Google Drive). Автосохранение меняется
  через полсекунды после последнего действия.
- **Экраны.** Персонаж (SPECIAL в каноне Сила-Восприятие-Выносливость-
  Харизма-Интеллект-Ловкость-Удача, навыки, ОЗ, удача-очки), Снаряжение
  (оружие/броня, окна улучшения, силовая броня), Перки и черты, Инвентарь
  (сумка, вес, расходники), «Игровой сеттинг» (пока заглушка).
- **Роботы.** Анатомия — из данных: конечность-оружие / конечность со
  встроенным оружием / рука для ношения (может или нет); планы тела
  (не только гуманоиды); защита слоями (обшивка/броня/рама). Оружие живёт
  в ладони, вмонтированное в конечность и собственная атака конечности —
  на всех трёх местах моды ставятся и переживают сохранение.
- **Оружие.** Каталог — JSON сеттинга. Вариант (`trueItemId`) = «это то же
  оружие, только с другим именем» («Опасная бритва»; робо-версии 314:
  Огнемёт Мистера Помощника = Огнемёт и т.д.). Моды: единая правда — список
  id на экземпляре (311); заводские моды записи (`modIds`, 312 — Авто-10мм
  с авто-ресивером); списание зарядов за выстрел (296/298).
- **Прочее.** Крафт и разбор хлама, выживание (голод/жажда/сон через
  расширение состояния), локали ru/en, PWA (обновление по кнопке),
  премиум-статус приложения.

## Последняя серия (311–314, унификация модов оружия)

- 311 — одна правда модов + один путь записи (контракт движка,
  `src/engine/items/weaponMods.ts`); моды собственной атаки конечности
  переживают сохранение.
- 312 — связь робо-оружия с людской базой — в данных.
- 313 — стартовая карта агента (`docs/agents/`).
- 314 — робо-версии = полные варианты людского (`trueItemId`), боевых
  полей в робо-записях нет.
- Установщик патчей — «истина в содержимом файлов» (309), после установки
  печатает готовую команду закрепления коммитом.

## Открытые вопросы (ждут слова владельца)

1. **13 спорных модов оружия** — показывать в окнах улучшения или нет:
   `docs/reference-data/weapon-mods-open-questions.md` (патч 307).
2. **Загрузчик `.trpg`** — дверь сеттинга есть (292), установки пакета из
   окна игры ещё нет (`setting-contract.md`, дорожная карта п. 5–7).
3. **МК-3: каскад в хранилище** — следующий крупный шаг TS-серии
   (`docs/architecture/typescript-migration-plan.md`).

## Уроки (не повторять)

- Песочница агента периодически теряет историю коммитов (`.git` откатывается,
  дерево живёт) — коммит и пуш после КАЖДОГО шага.
- Повреждение байта в файлах патчей после сбоя песочницы — сверять с
  коммитной версией, не править на глаз.
- Установщик файлы не теряет (починен возврат «остатков отката»), но
  крепить результат установки коммитом — обязанность владельца; установщик
  теперь сам печатает команду.
- Связи «робо → людское» держать только в данных: зашитая карта жила в коде
  и указывала на несуществующие id (мёртвый груз, 311–312).
