# Состояние работы — читается ПЕРВЫМ

> Этот файл — текущее положение дел: что сдано, что применено у владельца,
> какой состав приложения, что ждёт решения. Новая сессия отвечает на
> вопрос «что происходит?» отсюда, а не обходом дерева.
> Правило: обновляется В ТОМ ЖЕ ПАТЧЕ, что и изменение (2–3 строки правки).
> Полная история — `docs/changelog.ru.md` (сверху новые); как устроено —
> `docs/agents/README.md` и `docs/agents/data-flows.md`.

Снимок: **2026-09-25** · ветка агента: `arena/01a0c8e2-fallout-2d20-companion`
· последний сданный патч: **374** · у владельца закоммичено до **365**
(экспорты файлов патчей на main: 364+365); ревизия модов: материал
собран — 369 (имена оружия), 370–371 (141 пара дальнего боя, 13 новых
модов), 372 (мили: 53 пары 1:1, 9 новых строк, 14 переименований),
373 (Лазер института: референс владельца совпал 17/17; Электросварка =
arc welder без модов — снять 24 лишние привязки), 374 (вес Динамо = 6 —
МАТЕРИАЛ РЕВИЗИИ ЗАВЕРШЁН). Счёт: совпали 539, добавить 580, снять 147,
новых строк 22; без списков только 10-мм ПП и Плазменная мина (не
трогаем). Робо-моды не трогаем (отдельный контур, работает). Данные не
тронуты. Дальше: серия патчей ревизии (канон-id, слияние фантомов,
названия по владельцу, новые 22 строки, перенос сохранений).

## Состав приложения (игровыми терминами)

- **Герои.** Менеджер: создание/загрузка, папки, дубликаты, аватары, перенос
  файлом, облачная синхронизация (Google Drive). Автосохранение меняется
  через полсекунды после последнего действия.
- **Экраны.** Персонаж (SPECIAL в каноне Сила-Восприятие-Выносливость-
  Харизма-Интеллект-Ловкость-Удача, навыки, ОЗ, удача-очки), Снаряжение
  (оружие/броня, окна улучшения, силовая броня), Перки и черты, Инвентарь
  (сумка, вес, расходники), «Игровой сеттинг» (пока заглушка).
- **Роботы.** Анатомия — из данных: конечность-оружие / конечность со
  встроенным оружием / рука для ношения (может или нет); планы тела
  (не только гуманоиды); защита слоями (обшивка/броня/рама). Оружие живёт
  в ладони, вмонтированное в конечность и собственная атака конечности —
  на всех трёх местах моды ставятся и переживают сохранение.
- **Оружие.** Каталог — JSON сеттинга. Вариант (`trueItemId`) = «это то же
  оружие, только с другим именем» («Опасная бритва»; робо-версии 314:
  Огнемёт Мистера Помощника = Огнемёт и т.д.). Моды: единая правда — список
  id на экземпляре (311); заводские моды записи (`modIds`, 312 — Авто-10мм
  с авто-ресивером); списание зарядов за выстрел (296/298).
- **Прочее.** Крафт: из инвентаря — окно с квадратами категорий (еда,
  напитки, препараты, взрывчатка, оружие, броня, силовая броня, патроны;
  пустые данными видны), рецепты-спойлеры (доступные белые, недоступные
  серые; внутри — материалы по редкости обычные/необычные/редкие и кнопка
  «Создать»; если можно больше одной — окно «сколько штук», по умолчанию 1),
  крафт с проверкой навыка и отчётом; «Назад» — к квадратам, «Закрыть» —
  в инвентарь. Разбор хлама, выживание (голод/жажда/сон через расширение
  состояния), локали ru/en, PWA (обновление по кнопке), премиум-статус.

## Последняя серия (311–317, моды оружия → каскад МК-3)

- 311 — одна правда модов + один путь записи (контракт движка,
  `src/engine/items/weaponMods.ts`); моды собственной атаки конечности
  переживают сохранение.
- 312 — связь робо-оружия с людской базой — в данных.
- 313 — стартовая карта агента (`docs/agents/`).
- 314 — робо-версии = полные варианты людского (`trueItemId`), боевых
  полей в робо-записях нет.
- 315 — карта памяти агента (`state.md`, этот файл).
- 316 — МК-3 шаг 1: формулы производных (ОЗ/инициатива/защита/ближний
  бой/грузоподъёмность) переехали в модуль сеттинга
  (`modules/fallout/logic/derivedStats.js`); движок — через дверь реестра;
  поведение без изменений.
- 317 — МК-3 шаг 2: хранилище пересчитывает производные само после любого
  изменения входов (каскад); 26 ручных вызовов удалены; поведение без
  изменений.
- 318 — новое окно крафта: квадраты категорий (8, пустые видны), рецепты-
  спойлеры с материалами по редкости, окно «сколько штук», «Назад»/«Закрыть»;
  механика (гейты, проверка навыка, отчёт) не менялась.
- 319 — фикс: кнопка «Крафт» открывает окно крафта (раньше — заглушка-алерт
  с первого коммита, окно было недостижимо с кнопки).
- 320 — PWA: PNG-иконка 192 (установка вернулась в Яндекс/Mi) + будильник
  застрявшего воркера /sw.js (давние клиенты снова получают обновления).
- 321 — система обновлений: version.json (версия = номер последнего патча,
  чейнджлог ru/en; обновляется В ТОМ ЖЕ ПАТЧЕ) + окно «Что нового» с
  галочкой «больше не показывать».
- 322 — правки окна крафта по тестированию владельца: прокрутка, квадраты
  по 3 в ряд (остаток: 1 — центр, 2 — слева), «есть · нужно» рядом с
  названием материала, явная подпись времени («время: 1 сут»).
- 323 — крафт по книге: час всем (еда/напитки 20 мин), осложнение +30/+10
  аддитивно (×2 отменено), вопрос «2 ОД → время вдвое?» после успеха;
  настройки раздела «Крафт» — потеря материалов при провале двумя группами
  (consumables/gear). Ступени 259 и множитель отменены словом владельца.
- 324 — общий пул ОД (domain/actionPoints.js, кап 6, в движке без UI):
  успешные проверки дают +1 ОД за успех сверх сложности (криты = 2 успеха);
  «2 ОД → время вдвое» тратит из пула. Групповой — мастер-экран подключится
  как есть.
- 325 — четыре правки окна крафта: светлая гамма как у модалок перков
  (CraftingModal.styles.js перекрашен, inline-цветов в модалке нет);
  взрывчатка (все 9 рецептов) — своя категория explosives, рецепты в
  recipes/explosives.json, id сохранены (weapon_*), генератор знает корзину
  (EXPLOSIVE_OUTPUT_IDS), квадрат «Оружие» пуст; материалы: заголовок
  редкости — «{label}: X из Y видов», строка — «есть N шт. · нужно M шт.»
  (виды ≠ штуки — вопрос владельца закрыт); подпись «время приготовления:
  …» (крафт-время и отчёт).
- 326 — ревизия материалов по слову владельца («не раздуваем»): счётчиков
  ВИДОВ больше нет ни в строке рецепта (готовое = «Можно крафтить»), ни в
  спойлере (заголовки редкости без цифр); у материала — «есть N шт. ·
  нужно M шт.»; materialGroups — только группировка. Квадраты
  Броня/Оружие/Силовая броня = будущий крафт МОДОВ этих категорий
  (материалоёмкость) — НЕ убирать. Печатная кривая «материалоёмкость →
  материалы» (MATERIALS_BY_COMPLEXITY в генераторе, кап 7+) сверена с
  таблицей владельца — совпадает; выписана в CRAFTING-MAPPING.md (раздел
  порождается генератором, руками не править).
- 329 — дизайн окна крафта (слово владельца): фон окна категорий —
  assets/bg.png (opacity 0.3, как экраны сеттинга); квадраты — как области
  home screen (#111827 + рамка #d4af37, подпись #f0e68c); спойлеры рецептов —
  карточки как в модалке перков (рамка #ddd, радиус 6, отступы);
  доступные — светлые, недоступные по перку/рангу — серые (#f1f5f9, имя
  #9aa1a9); доступность материалов — в скобках у названия («можно создать» /
  «не хватает материалов» / «нужен перк»); сложность/навык/время — внутри
  спойлера первой строкой.
- 330 — поправка к 329: квадраты категорий — по образцу КАРТОЧЕК
  ПЕРСОНАЖЕЙ home screen (characterCell: светлая панель, рамка #5a5a5a,
  радиус 8, имя #222, счётчик #555), а НЕ папок; урок — «как у home screen»
  = карточки персонажей, папки не ориентир.
- 333 — слово владельца про синтов: «Ламинированная, Прорезиненная,
  Микроуглеродистая, Нановолоконная — уникальные моды брони синтов» →
  из общих модов убраны mod_std_laminate/rubberized/microcarbon (15→12);
  миграция сейвов v25→v26 снимает appliedArmorModId/appliedClothingModId/
  appliedArmorMod с этими id (инвентарь, modifiedItems, equippedArmor).
  Остальное по модам брони: эффекты костюмных модов пришлёт владелец;
  предмет-рецепт расходуется при применении; система узнавания редких
  рецептов — сразу для всего крафта (справочник §3.1).
- 334 — материалы в спойлере плоским списком (слово владельца: заголовки
  «Материалы»/«Обычные материалы»/… излишни): строка «тип — есть N шт. ·
  нужно M шт.»; materialGroups из модели убраны, ключи словаря
  rarity*/materialsTitle остались (пригодятся редкости-подписи в крафте
  модов).
- 335 — окно обновлений «Что нового»: ОДИН РАЗ НА РЕЛИЗ, без галочки
  (закрытие окна само запоминает релиз). version.json: version = патч
  (правило 321), release = номер релиза (поднимает ВЛАДЕЛЕЦ словом
  «делай релиз»), notes = описание релиза (не патча). Релиз №1 описывает
  пачку 315+. Патчи между релизами окно не показывают.
- 336 — эффекты плетения и подкладок (таблица владельца) записаны в
  справочник §2.6; 10 модов добавлены в данные (uniq_weave_*,
  uniq_vaultsuit_*; категории costumeUniqueMods/vaultSuitUniqueMods).
  Релизы объявляет ВЛАДЕЛЕЦ (слово 336; патчи релиз не поднимают).
  Ждём: эффекты 5 общих модов (Облегчённая/карманы/свинец/ультралёгкая).
- 337 — таблица УСТАНОВКИ модов (диктовка владельца) записана в
  справочник §2.7; сверка: все 12 стандартных модов уже книжные, исправлены
  2 двойных учёта (Парирующая −2 Энерго СУ, Мягкая подкладка ног −2 Физ СУ —
  эффекты уже в спецэффектах). ПРОТИВОРЕЧИЕ к 333: Ламинированная/
  Прорезиненная/Микроуглеродистая в таблице установки — «всё тело» (в 333
  убраны как «синтовские»); вопрос владельцу — вернуть ли; в данных их
  пока нет. Вопрос №2: у 5 крафт-модов (карманы/свинец/ультралёгкая/
  облегчённая/глубокие карманы) в таблице установки нет — эффектов нет
  вообще или другая страница?
- 338 — разъяснения владельца: «ВСЁ ТЕЛО» в таблице установки = любая
  часть тела, НО только на броню синтов → удаление трёх модов в 333
  ПРАВИЛЬНОЕ (не восстанавливаем; на синтах — uniq_synth_*). ДОЛГ: 5 общих
  крафт-модов (Облегчённая/карманы/глубокие карманы/свинец/ультралёгкая) —
  владелец проверит категории и эффекты, пришлёт; до ответа в окна их не
  ставим. Все прочие данные по модам брони полны (таблица крафта 327 +
  таблица установки 337 + эффекты плетения/подкладок 336).
- 339 — слова владельца по трём модам записаны в описания эффектов
  (Парирующая: +2 Физ только против «Ближнего боя»; Мягкая подкладка ног:
  +2 Физ только при падении; Облегчённая рук: держимое оружие
  Ближний бой/Рукопашная получает «Проникающий» 1, складывается).
  «Откуда взято» (4 мода): из диктовки 327 (цитата в справочнике);
  «Облегчённая» resolved = мод рук; остальные 4 ждут слова — настоящие
  или ошибка диктовки.
- 340 — ДОЛГ ЗАКРЫТ (сверился с книгой): С карманами/С глубокими
  карманами/Свинцовая подкладка/Ультралёгкая — моды-РЕЦЕПТЫ, не
  моды-предметы («как байт и килобайт»): скрафтить можно, применить
  нельзя (в книге их среди модов-предметов нет; эррата). Данные модов
  брони ПОЛНЫ. Разграничение понятий записано в справочнике; судьба
  четырёх рецептов (не выпускать / выпускать с пометкой) — при старте
  серии крафта модов.
- 341 — КРАФТ МОДОВ БРОНИ готов к проверке (слово владельца: «жду
  тестирования»): квадрат «Броня» = 44 рецепта (BOOK_ARMOR_MOD_RECIPES в
  генераторе, recipes/armor.json, корзина armor, категория armor);
  материалы по ёмкости, ИНТ+Ремонт, перки-гейт, час, провал жжёт gear.
  Мод-предмет падает в сумку с каталожным именем (findCatalogEntry
  'armorMod' + префиксы uniq_/mod_std_ в hintByPrefix и addNewItem).
  ВОПРОС владельцу: гейтить ли установку мода наличием в сумке (сейчас
  установка свободна из каталога). Не начато: силовая броня, оружие,
  редкие рецепты, 4 мода-рецепта (340).
- 342 — слова владельца о модах: мод УНИВЕРСАЛЕН (если не указано иное) —
  материал семейства встаёт на любую часть предмета брони семейства;
  МАТЕРИАЛ ≠ МОДИФИКАЦИЯ (1+1, слоты appliedUniqueArmorModId /
  appliedArmorModId); разделы окна установки — «Материал брони» /
  «Модификации». Настройка «Требовать мод в сумке для установки»
  (modsRequireInventoryItem, раздел «Крафт», дефолт ВЫКЛ = свободная
  установка; ВКЛ — только моды из сумки, чистый фильтр
  filterModsByInventory в modsEquip).
- 343 — слово владельца: установленный мод-предмет — «экипирован» и
  привязан к носителю (installedOn), невидим в сумке; снятие/замена
  возвращает видимость; предмет-носитель удалён (расход до нуля) —
  моды уходят с ним (releaseModsBoundTo; свободная установка без
  экземпляра в сумке — просто запись на броне, возвращать нечего).
  Расходование мода при установке решено: НЕ расходуется.
- 344 — уточнение владельца: продажа/трата — КНОПКИ на предметах в
  инвентаре. Правило «предмет удалён — моды уходят» перенесено в стор
  (adjustItemQuantity, одна копия; UI-хелпер инвентаря делегирует).
  Привязка — к ПРЕДМЕТУ (экземпляр в сумке): id модов синхронно пишутся
  на инстанс, снятие/надевание брони моды не теряет; запись мода
  возможна и на слот БЕЗ ключа экземпляра (старые сохранения/перенос
  файлом) — тогда мод не флагается и остаётся видимым; предметы
  комплектов создания, как выяснил владелец (345), ВСЕГДА имеют
  экземпляры в сумке — «костюм кита» был ошибкой агента.
  ЗАКОН (будущее): моды оружия — то же поведение (см. §2.9 п.5
  справочника), реализация с квадратом «Оружие».
- 345 — вопрос владельца «что за косметика создания?» вскрыл ошибку
  отчёта 344: предметы комплекта создания (включая одежду) попадают в
  инвентарь КАК ПРЕДМЕТЫ с id (CharacterScreen → addNewItem, человек
  надевает сам) — «виртуального костюма кита» НЕТ. Реальный случай
  слота без ключа экземпляра — только старые сохранения/перенос файлом.
  Правки ТОЛЬКО в документах, код не менялся.
- 346 — НАЧАТО КРАФТ-ВЕТКА МОДОВ ОРУЖИЯ (решения владельца: выпускать 152
  мода с готовыми колонками, включая 48 уникальных; 53 без колонок — в
  обменник; «?»-строки — на ревизию). Владелец прислал таблицы книги
  (с. 222–223) — ревизия проведена: Большой магазин (сложность 4, Фанатик
  оружия 1), Быстросъёмный (сложность 5, кривая 5), конденсаторы (пер-
  ки Gun Nut + Science! парой). «?» снят с перков всех 205 и материалов
  кроме конденсаторных (mod_031/032 — ждут слова о сложности: в книге
  для конденсаторов колонки сложности НЕТ). Редкость «?»×9 — вне реви-
  зии, зачистить при ветке. Далее: генератор рецептов → квадрат
  «Оружие» → установка предмета с законом 343/344.
- 347 — слово владельца: конденсаторы ОСТАВИТЬ КАК ЕСТЬ (сложности 5 и 2,
  материалы подтверждены — «?» снят). Ревизия редкости по тем же табли-
  цам (с. 222): Большой и Быстросъёмный магазины — Uncommon (было
  «?Common»); «?» аудита снят со ВСЕХ полей weapon_mods.json — данных
  со знаком сомнения больше нет. 9 «?»-редкостей были: Скорострельный,
  Бронебойный, Настроенный, Экранированный, Большой, Быстросъёмный,
  Большой быстросъёмный, Полные конденсаторы, Катушка усиления
  (знаки были служебными фразами парсинга референс-приложения).
- 348 — КРАФТ МОДОВ ОРУЖИЯ ВКЛЮЧЁН (решения 346/347): генератор строит
  152 рецепта из колонок weapon_mods.json → recipes/weapons.json,
  квадрат «Оружие» жив (289 рецептов всего). Проводка: index.js
  (sections.weapons), registry CRAFTING_FILES, resolveItem case
  'weaponMod' (catalog.weaponMods), hintByPrefix/idPrefixType 'mod_' →
  weaponMod, categoryRules weapons. Провал жжёт gear (как брони).
  mod_113 «Serrated Blade»: опечатка аудита Blacksmtih→Blacksmith.
  mod_112 Extra Flame Jets: пустой навык → Ремонт (верстак оружия).
  Скрафченный мод — ПРЕДМЕТ в сумке (тип weaponMod). СЛЕДУЮЩИЙ ШАГ:
  установка мода оружия из сумки с флагом «экипирован» и привязкой
  (закон владельца 343/344) + гейт «требовать мод в сумке».
- 349 — ПЛАН ВЛАДЕЛЬЦА «под конец крафта» (дословно в справочнике
  §2.10): в модалках установки модов — кнопка «Создать» и требования
  на позиции (режим «моды через крафт»; скрыты, если мод в инвентаре);
  в категориях крафта — разделы по категории брони / слоту оружия
  («Кожаная броня», «Ресивер»).
- 350 — ОТКРЫТАЯ ТОЧКА 349 ЗАКРЫТА: режим «моды через крафт» = настройка
  «Установка модификаций» (быв. «Требовать мод в сумке», i18n ru/en
  переименованы словами владельца). Порядок подтверждён: следующий шаг —
  установка мода оружия из сумки, редизайн §2.10 — после него.
- Установщик патчей — «истина в содержимом файлов» (309), после установки
  печатает готовую команду закрепления коммитом.
- 351 — УСТАНОВКА МОДА ОРУЖИЯ ИЗ СУМКИ (закон 343/344 на оружии):
  проводка handleApplyModification (ветки storeItem/equippedWeapon —
  дифф diffModInstallPlan в modsEquip, флаг/привязка через store
  actions, кулаки — виртуальный носитель). Робо-ветки НЕ тронуты.
- 352 — КНОПКА «СОЗДАТЬ» В МОДАЛКЕ ОРУЖИЯ (первая очередь §2.10, для
  тестирования владельцем): список НЕ фильтруется (гейт 351 в модалке
  снят); у не установленного мода без предмета в сумке — кнопка справа
  (зелёная, если материалов хватает; dimmed opacity, если нет) и
  требования одной строкой «Перк N · материалы × · Сложность M»
  (buildModCraftHint в windowModel, чистая). Нажатие: craftRecipe +
  settleCraftTime (учит обе формы прогона: attempts[] и одиночный
  time.pending), отказы — Alert по кодам движка. Успех → мод в сумке,
  кнопка скрыта. Настройка «Установка модификаций» включает кнопки.
  Далее по §2.10: то же для модалок брони + разделы категорий.
- 353/354 — ПРАВКА МАКЕТА (замечания владельца): кнопка «Создать» —
  тёмный аналог applyButton модалки (#007AFF, borderRadius 5),
  компактная справа в строке материалов; требования блоком —
  «Требования: Перк N · Сложность M» и строка «есть/нужно» мелким
  слева (matCommon/matUncommon/matRare в ui крафта; helper отдаёт
  requirements + materialsLine).
- 355 — ЦВЕТ КНОПКИ: зелёная rgb(34,197,94) = #22c55e из стилей
  (PerkSelectModal.styles); синяя #007AFF в 353 была ошибкой агента.
- 356 — МЕХАНИЗМ ПРОВЕРОК: сложность 0 (снята навыком) спрашивает
  «Бросить кубики?» — «да» = бросок resolveD20Check с правилами
  Успехов/Провалов (1 = крит, 20 = осложнение; автопровал — осложнение
  без успехов, точность правила — 358), «нет» = автоуспех без броска
  (прежнее поведение).
  Движок: runCraft-опция zeroDifficulty ('auto'|'roll'), result.auto =
  «броска не было»; craftRecipe проксирует опцию. UI: вопрос в окне
  крафта (CraftingModal, один на пачку) и у кнопки «Создать» модалки
  оружия (craftingPreview → evaluation.auto). Тест zero-difficulty-ask-356.
- 357 — ОТЧЁТ О КРАФТЕ В МОДАЛКЕ УСТАНОВКИ МОДОВ (жалоба владельца:
  «материалы списались, кнопка потускнела и всё… не понятно, что
  произошло»). Общий компонент CraftReportView
  (modules/fallout/crafting/, стили styles/CraftReportView.styles — копия
  отчёта окна крафта 265/318/323); окно крафта рисует отчёт им же
  (разметка 1:1). Модалка оружия: отказ ДО проверки (перк/материалы) —
  прежние Alert 352; провал проверки и успех — отчёт buildCraftReport
  (одиночный прогон оборачивается {attempts:[run], stoppedEarly:0}):
  арифметика, кубики, сгоревшие материалы, время + вопрос про 2 ОД
  (324: спрашивается только при успехе и пуле ≥ 2, иначе полное время
  сразу). Заголовок отчёта — имя мода. Попутно исправлен скрытый краш
  buildCraftReport: «ОД: +N» читал d.ui.apEarnedLine вместо
  d.apEarnedLine (любой успех с лишними успехами ронял отчёт — и в окне
  крафта тоже). Тест weapon-mod-craft-report-357 (4).
- 358 — ЗАКОНЫ МЕХАНИЗМА ПРОВЕРОК (пояснения владельца при тесте 357):
  (а) автопровал = «нет успеха + осложнение», а НЕ «две двадцатки» —
  из d20Checks убрана отдельная оговорка про число двадцаток
  (AUTOMATIC_FAILURE_COMPLICATIONS); на стандартных 2d20 исходы не
  изменились (кубик-осложнение успехов не даёт), на 3+ кубиках правило
  владельца теперь точное. (б) Успехи сверх сложности — ВСЕГДА ОД
  (в крафте живёт с 324; закон для всех будущих проверок). (в)
  Последствие ПРОВАЛА определяет категория/ситуация: химия →
  зависимость, алкоголь → зависимость, крафт → потеря материалов или
  время (реализовано), проверка на болезнь → продление действия —
  применять при появлении соответствующих проверок. Тест
  check-auto-fail-358 (5, __tests__/domain/).
- 359 — МОД НА РОБО-ОРУЖИИ ПОДЧИНЯЕТСЯ ЗАКОНУ 343/344 (репорт владельца:
  скрафченный «Тюнер бета-волн» для лазер-гана штурмотрона «не ставится,
  хотя в инвентаре есть… не экипируется»). Диагноз (лабораторно): запись
  мода на оружие в слоте робота работает (plan robotSlot/installed,
  setInstalledWeaponMods, переживает сейв), но закон 343/344 на робо-ветках
  НЕ исполнялся — мод-предмет оставался в сумке («не экипируется»).
  Исправление: экран в ветке robotSlot исполняет дифф 343/344 с
  синтетическим носителем robotWeaponHostKey(слот, id оружия)
  (src/engine/items/weaponMods.ts); новое действие стора
  installRobotWeaponMod (паспорт обновлён); replaceLimb/unequipHeldWeapon
  слайса возвращают моды уходящего оружия в сумку. Карта потоков —
  data-flows.md. Тест robot-weapon-mod-flag-359 (5).
- 360 — РЕПЛЕЙ ЭКСПОРТА ВЛАДЕЛЬЦА (штурмомрон, «Смешанный Снайперский
  ствол Лазерный пистолет», жалоба: «Смешанный не снимается, замена на
  тюнер — изменений не происходит»; экспорт приложен к репорту и
  сохранён в __tests__/fixtures/assaultron-ghost-export.json). Два
  дефекта: (1) робо-оружие оседало копией-ПРИЗРАКОМ в equippedWeapons с
  устаревшими appliedMods — при загрузке вычищается (правило «встроенное
  оружие конечностей НЕ хранится в equippedWeapons» теперь исполняется);
  (2) loadRobotState держал худые слоты из сейва как есть — ветки записи
  модов (limb.builtinWeapons) на них молча не срабатывали; слоты теперь
  разворачиваются в полную форму прямо в loadRobotState (в памяти —
  всегда полная, как после комплекта). Запись модов робо-оружия после
  ЛЮБОЙ загрузки работает. Тест robot-mod-load-replay-360 (4) — прогон
  реального экспорта через настоящий loadCharacter.
- 361 — ЭКСПОРТ = СЕЙВ (слово владельца: «хорошо бы, чтобы экспорт
  всегда = сейву… когда-то были расхождения»). Причина расхождений
  найдена: экспорт ПОСТОЯННО ре-слаймил тело сейва каталогом экрана
  (createCharacterExportPayload, getEntry по moduleLocale) — другая
  локаль, обновившиеся правила слайма или двойной прогон давали тело,
  отличное от тела сейва. Теперь тело файла = тело записи БД БЕЗ
  повторной обработки; единственный владелец сжатия — saveCharacter
  (slimSaveData по каталогу активной локали до записи в БД).
  Вызывающие очищены от getEntry (HomeScreen, googleDriveSync ×2).
  Тест export-equals-save-361 (3): тело файла байт в байт = телу сейва;
  круг экспорт→импорт→экспорт не меняет тело; жирный сейв v18-
  экспортируется как есть.
- 362 — ХОТФИКС КРАША ЭКРАНА (репорт владельца, Web: «ReferenceError:
  Cannot access 'installRobotWeaponMod' before initialization» в
  WeaponsAndArmorScreen.js:1078). Причина — моя, из 359: селекторы
  действий стора стояли НИЖЕ handleApplyModification, а массив
  зависимостей useCallback читает их сразу при рендере → TDZ.
  Объявления перенесены выше колбэка (порядок: селекторы → колбэки);
  проводка — статический тест screen-selector-order-362 (2): селектор
  обязан быть объявлен выше колбэка с ним в deps. node_modules в
  песочнице снова исчезал — восстановлен npm ci.
- 363 — «МЁРТВЫЕ КНОПКИ» НА WEB (пачка репортов владельца: ресивер —
  «нет отклика у кнопки», тюнер — «прикрепляю — ui не меняется»,
  «Создать» горит у уже установленного). КОРЕНЬ ДОКАЗАН: Alert.alert в
  react-native-web — пустая функция (`alert() {}`); 352/356 повесили на
  него вопрос «бросить кубики?» (снятая сложность — владелец попал
  именно в него с ресивером) и сообщения об отказе гейта. Прикрепление
  мода при этом РАБОТАЛО (курсив = флаг 359). Исправление: вопрос про
  бросок — inline-диалог в ОБОИХ окнах (CraftingModal askZeroTarget,
  модалка craftNotice); отказ гейта — inline-диалог модалки; применение
  модов больше не закрывает окно молча — экран обновляет карточку
  (setSelectedWeaponForModification(modifiedWeapon) вместо
  handleCloseModificationModal), модалка показывает зелёную отметку
  «Применено: …». i18n: modals.installApplied, modals.createNoticeDone.
  Тест inline-feedback-363 (4): Alert-вызовов в двух окнах нет;
  inline-диалоги на месте; отметка применения на месте.
- 364 — ПЕРЕВОД 363 НА ЕДИНУЮ ТОЧКУ ДИАЛОГОВ + ШПАРГАЛКА МЕХАНИЗМОВ
  (слово владельца: «в программе единый механизм всплывающих окон,
  гарантирующий работу везде»; «давно не писал в документ-память, как
  работает приложение»). alertService/AlertHost
  (components/alerts/) — единственный способ показа диалогов (Promise,
  любые кнопки, веб=натив); showRawAlert с кнопками в формате
  системного Alert, промис резолвится индексом, закрытие без выбора =
  null. Вопрос про бросок (356) в обоих окнах и отказ гейта (352) —
  showRawAlert; своё inline-окно 363 снесено (askZeroTarget/craftNotice),
  ключ createNoticeDone удалён (у info «Ок» из общего словаря).
  Тест inline-feedback-363 переписан (5): Alert-вызовов нет, оба окна
  зовут alertService, своих диалоговых окон нет, отметка на месте.
- 365 — ПРАВКА ПО СЛОВУ ВЛАДЕЛЬЦА: для «документа-памяти, как работает
  приложение» НОВЫЙ файл не заводить — он есть с патча 313:
  `docs/agents/README.md` (стартовая карта ИИ-агента, дополняется при
  каждом переезде). mechanisms.md из 364 удалён, содержимое влито в
  README разделом «Как работают ключевые механизмы». Урок в «Уроках».
- 366 — ПОЧИНКА УСТАНОВКИ: файл патча 364 был собран от базы 12ad784
  (до 363) вместо e912926 (после 363) — содержал правки 363 внутри себя
  и не вставал после применённого 363; установщик останавливался на цели
  365, а «первый не вставший 275» в отчёте — шум (275 давно перекрыт
  поздними патчами). Файл 364 пересобран; 364 и 365 у владельца встали.
  Первая сборка самого файла 366 была испорчена (параллельные правки
  state.md: утеряна эта запись, добавлен дубль хвоста) — файл пересобран
  заново; испорченная версия ни у кого не успела примениться.
- 367 — ПОЧИНКА ЗАПИСИ МОДОВ НА НАДЕТОМ ОРУЖИИ ЧЕЛОВЕКА (репорт владельца:
  «применил к 10мм ресивер для автоогня — на карточках изменений нет», в
  его экспорте appliedMods пуст). КОРЕНЬ (патч 311): ветка storeItem
  классификатора писала мод только в копию инвентаря (updateItem), а
  карточки и сейв читают список надетого — мод оседал невидимо. Починка:
  сквозная запись setEquippedWeapons по ключу предмета после updateItem.
  По слову владельца «Применить» снова ЗАКРЫВАЕТ окно модов как раньше
  (зелёная отметка 364 снесена: installNote/installApplied удалены) —
  молчания больше нет, карточка обновляется сквозной записью. Приёмочный
  реплей __tests__/equipment/human-weapon-mod-write-367.test.js (3) на
  фикстуре экспорта владельца krosh-human-export.json: диагноз, потеря,
  починка + выживание сейв→загрузка. Путь ЗАГРУЗКИ потерь не имеет
  (проверено; подозрение «загрузка стирает моды» — артефакт пробника,
  мокшего исходный экспорт вместо сохранённой строки).
- 369 — СОПОСТАВЛЕНИЕ РУССКИХ НАЗВАНИЙ ОРУЖИЯ С ID (начало ревизии,
  слово владельца: «начни с сопоставления русских названий оружия с id,
  после — поддерживаемые моды»; имена владельца имеют ПРИОРИТЕТ).
  Файл владельца gemini-code-…json (на main; 77 стволов + 6 ближнего
  боя, частично) привязан к каталогу: 83/83 однозначно — 54 по имени,
  25 по статам из того же файла, 4 робо-двойника (взяты людские id).
  Коллизия каталога «Боевая винтовка» (combat_rifle/battle_rifle) снята
  именами владельца: «Боевой карабин»/«Самозарядная винтовка».
  Расхождения к ревизии: 27 имён (в т.ч. 9 правок «ё»), 6 позиций
  с расходящимися статами (цифры брать из файла владельца). Материал:
  docs/reference-data/weapon-names-mapping-369.md. Данные приложения
  не тронуты.
- 368 — ПЛАН РЕВИЗИИ МОДОВ (слово владельца: вопросы рецептов дублей
  решаются рефакторингом — «налицо разная трактовка переводов, которая
  привела к созданию фантомных модов»; порядок: сначала этап 1 — моды
  встают и работают, потом ревизия). Аудит: 31 улучшение размножено в
  95 строк (лишних 64) — 205 строк всего, рецептов нет у 53; механика на
  примере «Удобная» (mod_026 с рецептом / mod_194 без). Материал ревизии:
  docs/reference-data/weapon-mods-refactor-plan.md (форма: каноничные
  слаги вместо mod_NNN, одна строка на улучшение, колонки крафта — в
  рецептах по семействам, миграция сейвов без подъёма схемы). Данные не
  тронуты.

## Открытые вопросы (ждут слова владельца)

0. **Крафт модов брони** — таблица книги записана и сверена
   (`docs/reference-data/armor-mods-crafting.md`, 327–332). Ответы
   владельца (332): ёмкость крафта ≠ complexity предмета (то поле нигде
   не используется, данные не трогаем); 15 недостающих модов — моды
   ОДЕЖДЫ (плетение — большинству костюмов, комбинезон Убежища — плетение
   + подкладки); ламинарные дули-дубли синтов в общих модах лишние;
   редкость — система узнавания (обычные всем, необычные через перки,
   редкие через предметы-рецепты — новая категория инвентаря, принцип
   драйвера МК II, id предмета = id рецепта). Остались узкие вопросы
   (файл §3.1): эффекты костюмных модов, судьба трёх дублей, расход
   предмета-рецепта, охват системы узнавания. Серия патчей крафта модов
   НЕ начата до ответов.
1. **13 спорных модов оружия** — показывать в окнах улучшения или нет:
   `docs/reference-data/weapon-mods-open-questions.md` (патч 307).
2. **Загрузчик `.trpg`** — дверь сеттинга есть (292), установки пакета из
   окна игры ещё нет (`setting-contract.md`, дорожная карта п. 5–7).
3. **МК-3: каскад в хранилище** — идёт; шаги 1–2 сданы (316 — формулы в
   модуле, 317 — автопересчёт вместо 26 ручных вызовов); дальше —
   декларативный переезд формул в реестр выводимости
   (`src/engine/derivations/registry.ts`) с паритет-тестом и смерть
   подписки-зеркала 243 (`docs/architecture/typescript-migration-plan.md`).
4. **Перки: работают 22 из 177** — взять перк можно, но у большинства нет
   механики. План владельца: после МК-3 — перепись перков (таблица: кто
   работает/чего не хватает), затем серия по семействам (боевые/социальные/
   крафтовые; боевая треть нужна будущему экрану сражений).

## Уроки (не повторять)

- (366) Файл патча N генерировать СТРОГО от предыдущего «файл патча»-
  коммита: `git diff <prev-файл-патча> <контент-N>`; 364 собрал от базы
  до-363 — патч не вставал ровно после 363 (двойные правки), а e2e
  «поверх 12ad784» это маскировал. Перед сдачей — прогон цепочки
  `git apply --check` подряд (N−1→N→N+1) на копии предыдущего состояния.

- (366) Несколько правок ОДНОГО файла — строго ПО ОДНОЙ, с просмотром
  результата до коммита: параллельные правки state.md затёрли друг
  друга, задвоили хвост и испортили файл патча (владельцу он не достался —
  установщик отложил его, дерево не тронув).

- (367) Пробник загрузки обязан грузить РОВНО то, что проверяет: мок,
  указывающий на исходный экспорт вместо сохранённой строки, дал ложный
  диагноз «загрузка стирает моды» — потеря была только в записи (311
  storeItem-ветка). Перед выводом — проверять, ЧТО реально читает мок.

- (365) Новые агентские документы НЕ заводить без слова владельца:
  документ-память «как работает приложение» уже есть —
  `docs/agents/README.md` (патч 313); дополнения — разделами внутрь неё.

- ПРОТОКОЛ ВОПРОСОВ (331, слово владельца после 329→330): любое
  недопонимание или разночтение — СПРАШИВАТЬ владельца и «спихивать» на
  него описание деталей, даже если ответ кажется очевидным. Два патча на
  правку недопонимания хуже одного вопроса до работы. Вопросы в отчёте
  выделять ЯВНО: заголовок «ВОПРОСЫ — жду ответа по номерам», не абзацем.

- Песочница агента периодически теряет историю коммитов (`.git` откатывается,
  дерево живёт) — коммит и пуш после КАЖДОГО шага.
- Повреждение байта в файлах патчей после сбоя песочницы — сверять с
  коммитной версией, не править на глаз.
- Установщик файлы не теряет (починен возврат «остатков отката»), но
  крепить результат установки коммитом — обязанность владельца; установщик
  теперь сам печатает команду.
- Связи «робо → людское» держать только в данных: зашитая карта жила в коде
  и указывала на несуществующие id (мёртвый груз, 311–312).
- Патч, трогающий бинарные файлы (иконки и пр.), собирать только
  `git diff --binary` — обычный diff молча выбрасывает бинарь, установщик
  падает с «cannot apply binary patch» (320).
- Проводка «кнопка → окно» проверяется приёмочным тестом (урок 319: модалка
  жила в коде, но кнопка годами звала заглушку).
