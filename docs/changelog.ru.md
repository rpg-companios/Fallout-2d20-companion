# История изменений

---

## Микропатч — Нехватка ранга «Мусорщика» называется прямо (патч 275)

> Слово владельца (2026-09-17): на меле с «Мусорщиком» 1 ранга подстрока врала — «недоступно без «Мусорщика», +1 недоступно», хотя перк был и нужен был ранг 2. Уговор 264 («не видно то, что нельзя получить») остаётся в силе: имена срезанных материалов по-прежнему не показываются.

- Модуль различает причины: `filterCompositionByCeiling` возвращает минимальную отрезанную редкость, подстрока — `gate: 'rank' | 'no-perk'` и `requiredRank`. Экран при `'rank'` печатает «Разбор: требуется перк «Мусорщик» ранг N» и не тащит хвост «+N недоступно» (при полностью срезанном составе счётчик — шум). Без перка — прежняя формулировка уговора.
- Числа ранга — из правил модуля (`scrapperUncommonRank`/`scrapperRareRank`), не из текста: новая шкала редкости потребует правки правил, а не экранов. 5 регресс-тестов (в т.ч. строка экрана с `uniqueId`), имена локалей в проверке не фигурируют — словарь покрыт тестом обеих локалей. 906 зелёные, tsc чисто.
- Реестр патчей: 273/274 влиты вами отдельно (PR #20), их файлы из учёта сняты; в реестре — `patchs/275-salvage-rank-gate-message.patch`.

---

## Обновление — Реформа данных: разбор в карточках, рецепты без верстаков, единый junk-каталог (патч 269)

> Слово владельца (2026-09-17): хлам и рецепты разбора — один файл; material.json и junk.json живут в `data/junk/`; запись материала — `{id, materialType, weight, cost, rarity}`, ни itemType, ни «именованности» не нужно; движок не знает ни рецептов, ни материалов — только «рецепты в реестре, реестр указывает файлы»; рецепт — `{id, requires, materials}`, и id есть предмет на выходе; количество — одно поле `outputQuantity` (целое или кубики), «лишь бы одинаково во всей программе»; пачки и прочие материалы без различий — общий пул; `bench` и `sourcePage` из рецептов вон — провал сжигает материалы там, где это записано в самой строке; подписи d20-таблиц в данных — ошибка, подписи живут в i18n.

- `data/junk/`: junk.json (130 карточек; печатный состав разбора живёт прямо в строке — состав лупы видно из junk.json без второго файла; 129 владельцев состава, два легаси-предмета — без него), material.json (36 записей; materialType common/uncommon/rare сверяется с rarity 0/1/2 гардом), tables.json (только грани и ссылки; подписи категорий, таблиц и добычи переехали в `i18n/{loc}/data/junk/tables.json`). Отдельного salvage.json больше нет; радио остаётся линком и носит состав в собственной карточке general_goods.json — генератор пишет и сверяет именно туда.
- Рецепты — `data/recipes/` (боеприпасы 28, оружие 9, препараты 21, еда 27, напитки 8); манифест знает лишь «файл → категория → число». В записях нет ни верстаков, ни output, ни страниц, ни служебной пометки «материалы по сложности» — её смысл проверяется напрямую: набор только из сырья обязан равняться печатной кривой. Количество — `outputQuantity: 1 | {base, cd}`; «сгорание при провале» — флаг `failBurnsMaterials` в записи, генератор выставил его по печатному правилу (кухня и химия жгут): 74 флага, распределение по файлам слово в слово повторяет прежний верстачный разрез (19+74 разделения сохранены байт-в-байт по составу флагового разреза).
- Движок: сверка формы количества отделена от броска (данные проверяются без кубиков), выдача — `{itemId: recipe.id, quantity}`; «пачки» — три id в `CRAFT_RULES.packMaterialIds`, в данных и справочнике этого различия нет; разбор хлама без состава тянет общий пул до потолка редкости включительно с пачками («все материалы равны»), пачка не закрывает сама себя.
- Окно: вкладки — категории манифеста (Боеприпасы/Оружие/Препараты/Еда/Напитки, подписи `categoryNames` в обоих локалях), `benchView` и карты «верстак → навыки/перки/сгорание» из правил удалены; метка активности — `craft:<категория>`.
- Итоги: оба генератора чисты `--check`; тесты переведены на новую выкладку (новые гарды: подписей в данных таблиц нет, служебных флагов нет, манифест против файлов, форма флага честная, зеркала i18n по новым путям); 893 зелёные (85 файлов), tsc --noEmit чистый.

## Микропатч — Сгорание сворачивается в правило; пути групп живут в листах (патч 270)

> Замечания владельца (2026-09-17): «зачем 74 записи с флагом, если можно две строки в реестре»; «реестр затем и создавался, что группировка „что где живёт“ записана в нём — при росте категорий импорт обязан быть один: реестр, а не импорт-импорт-импорт по каждому документу».

- `failBurnsMaterials` удалён из 74 записей рецептов: в печатном наборе сгорание при провале совпадает с проверочным навыком (кухня — Выживание, химия — Наука, взрывчатка — Подрыв), и правило теперь живёт одной строкой `CRAFT_RULES.failBurnsMaterialsSkills`. Движок считает поле в данных сторонним и отказывает; генератор при выкладке сверяет навык с верстаком источника и падает при расхождении — разрез 21+27+8+9+9 сохранён гард-тестом байт-в-байт по семантике.
- Листы групп данных: `modules/fallout/data/junk/index.js` (карточки хлама, справочник материалов, таблицы, зеркала имён и подписи таблиц по локалям) и `modules/fallout/data/recipes/index.js` (манифест + файлы). domain/registry.js и i18n/equipmentCatalog.js импортируют листы, а не JSON: переезд или рост категории = одна правка в листе. Гард-тест запрещает реестру и каталогу таскать хлам-пути напрямую; подписи таблиц отдаёт `getScrapTableLabels(locale)`.
- По вопросу о единообразии составов: проверка всех 250 строк junk.json/радио — форма одна (`{material, count}` или `{material, base, dc[, effect}]`), расхождений ноль; разнобой был только в сокращении моего сообщения. 3 новых теста (правило против печати, запрет поля в записи, листы вместо прямых импортов). Итого 896 зелёные (85 файлов), tsc --noEmit чистый, оба генератора --check чистые.

## Микропатч — Материалы не разбираются никогда; состав лупы закреплён тестом (патч 268)

> Канон владельца (2026-09-17): хлам — всё, что разбирается на конечные материалы; материалы — ингредиенты крафта и одновременно составные части хлама (materials.json — это именно содержимое junk.json); материалы обладают редкостью; могут жить отдельно от хлама или быть его содержимым; материалы не разбираются.

- Пятый пункт стал жёстким гейтом в операциях разбора: isScrapMaterial проверяется раньше типа «хлам» и раньше печатного состава — даже если будущие данные припишут материалу состав или занесут его в хлам, разбор останется невозможен (причина 'material'), а в карточке предмета не будет ни подстроки, ни кнопки, хоть и серой. Для прямых вызовов (ГМ, отладка) в отчёте — человеческая строка «Материалы не разбираются: они и есть конечные части».
- Сверка с владельцем по лупе: данные уже верны — один вариант состава стекло ×2 + медь ×1 + кристалл ×2, редкости медь/стекло — uncommon, кристалл — rare; в игре без «Мусорщика» лупа не разбирается (+3 скрыто), ранг 1 даёт стекло и медь, ранг 2 — и кристаллы. Теперь это зафиксировано тестом: расхождение с книгой не пройдёт тихо ни при какой перегенерации данных.
- Гварды данных: пересечение справочника материалов с файлом хлама — ноль; материалов среди владельцев составов — ноль; редкость каждого материала в 0..2. 9 новых тестов. Итого 890 зелёные, tsc --noEmit чистый.

## Микропатч — Кнопка разбора есть у всего хлама; без условий — затемнена (патч 267)

> Решение владельца: кнопка «Разобрать» принадлежит хламу целиком, а не только предметам с печатным составом. Работает — когда соблюдены условия разборки; не соблюдены — кнопка на месте, но затемнена и не срабатывает.

- Хлам без печатного состава (желёзы, жало…) теперь тоже с кнопкой: его разбор идёт общим пулом (правила 261) — раньше механика это умела, а кнопки не было. Подстрока состава по-прежнему только у предметов с печатным составом (264), и прячется на надетом.
- Активность кнопки считает тот же общий расчёт доступности (salvagePreview): предмет надет или в чехле PA, состав срезан потолком «Мусорщика» целиком, нечего брать из пула — кнопка серая (opacity) и не нажимается; молчание вместо отказа-алерта (решение владельца). Состояние всегда совпадает с реальным гейтом salvageItem, расчёта «на глаз» нет.
- Предметы вне хлама без печатного состава — без кнопки, как раньше. 4 новых теста на состояние кнопки (хлам без состава, срезанный состав, надетый, не-хлам). Итого 881 зелёные, tsc --noEmit чистый.

## Микропатч — Кнопка крафта неактивна до перестройки окна (патч 266)

> Решение владельца: «верстаки» — неверная ось деления. Окно крафта 265 будет перестроено по типу изготавливаемого: еда/напитки, оружие, взрывчатка, броня, силовая броня, препараты, роботы (реальных верстаков в данных пять). До этого крупного патча крафт не открываем.

- Кнопка «Крафт» в инвентаре снова не открывает окно: нажатие честно сообщает, что крафт перестраивается (текст в обоих локалях обновлён). Окно, модель и все тесты 265 на месте — следующий патч подключит те же механики к новым вкладкам; движок (251/262/263) не менялся.
- Гвард-тест обвязки перевернут под новое состояние: `setCraftModalVisible(true)` на кнопке запрещён, обвязка модалки обязана оставаться в экране. 877 зелёные, tsc --noEmit чистый.

## Обновление — Окно крафта: верстаки, материалы, пакет (патч 265)

> Шаг 3 плана: крафт из кнопки «Крафт» в инвентаре. Дизайн утверждён владельцем (2026-09-17): невыученные рецепты видно серыми с причиной; окно рецепта знает только требуемые материалы — подсказок про замены нет; отчёт ровно три строки; пакет разрешён, неудачная попытка сжигает только своё.

- Кнопка «Крафт» открывает окно с вкладками верстаков (оружейник / химлаборатория / кухня — по факту данных, гард-тест следит, чтобы ни один рецепт не остался без вкладки). Строка рецепта: что получится, «Сложность N · навык · время»; нельзя — серая строка и честная причина: «Нужен перк „Химик“, ранг 1» или «Не хватает материалов» (решение Б: невыученное видно, а не спрятано).
- Окно рецепта: построчно материалы «есть N · нужно M» (числа уже с учётом того, чем сумка может закрыть), сколько раз крафтить (до максимума по дефицитному материалу), кнопка прогона. Про замены — ни слова: рецепт знает, что просит, а чем закрывать — решает сумка.
- Пакет: N попыток подряд каждой своей проверкой на настоящих костях; провал (напр. третьей из пяти) сжигает материалы только третьей, остальные идут дальше; когда сумка пуста на M-й — попытки останавливаются и в отчёте видно «Остановлено: … N попыток не началось».
- Отчёт — формат владельца: «Интеллект + Ремонт = 8» / «Выпало 12, 3. Успехов 1» (или «Неудача» с пометкой, сгорело или цело) / «Получено: … ×n. Потрачено: … Время: …». Осложнение помечено в строке кубиков и удваивает время в итоговой. На пакете строка 1 общая, у каждой попытки своя строка, итог свёрнут.
- Переименование по слову владельца: «пачек» больше нет нигде — обычные/необычные/редкие материалы теперь «обычный материал / необычный материал / редкий материал» (en: Common/Uncommon/Rare material) — в сумке, магазине и всех списках; технические id не тронуты. Генератор данных крафта связывает книжные «Common Materials» с этими id через явные алиасы — перегенерация рецептов байт-в-байт совпала.
- Новая модель modules/fallout/crafting/windowModel.js (без React — списки, пакет, отчёт), модалка modules/fallout/screens/InventoryScreen/modals/CraftingModal.js, 15 тестов. Итого 877 зелёные, tsc --noEmit чистый.

## Обновление — Время крафта и разбора идёт по часам выживания (патч 262)

> Переоткрыто владельцем: привязку времени к часам выживания делаем сейчас, цвета и UI инвентаря — после. Контракт «время держим, не считаем» закрыт: минуты из ответов крафта и разбора тратят часы выживания.

- Новая операция пакета: applyActivityMinutes(минуты) — гоняет лестницы через те же часовые тики, что реальный контур (дробь копится в timeCarried: шесть разборов по 10 минут складываются ровно в час), двигает таймеры временных эффектов и фиксирует потерю текущих ОЗ от усталости (то же, что тик часов, 232). Гейты общие с выживанием: выключенное настройкой — заморожено (applied:false, в ответе остаётся причина), у персонажа без шкал — нечего двигать; сам крафт/разбор при этом работает как работал.
- Разбор: 10 минут на предмет идут в часы после любой попытки, дошедшей до кубиков, — и на успехе, и на провале (сорванный вечер считается); гейт-отказ кубиков не видит, не тратится. Осложнение домножает время до записи в часы (20 минут на провале с двойным осложнением).
- Крафт: базовое время по печатной Complexity рецепта (книга с. 210, воля ГМ зафиксирована таблицей): 0–1 — 10 минут, 2–3 — час, 4+ — сутки (ступени владельца из 259); лежит в CRAFT_RULES.craftTimeTiers, правится без данных. Время тратится на успехе (автоуспех — множитель 1) и на провале проверки (провал = зря потраченное время, а на кухне/химии ещё и сгоревшие материалы); отказ гейта или стора — ни минут. Ответ крафта теперь несёт time:{minutes,durationMultiplier} — тот же контракт, что у разбора с 260.
- Две числовые защиты, без которых дробь ломалась: перенос часов в advanceHours получает допуск 1e-9 (шесть шагов по 1/6 часа в float дают 0.9999999999999999 — без допуска час из минут не тикал бы никогда), а мост эффектов округляет сцены до целых (1/6 ч × 12 = 1.999… и домен бросал на нецелом; целые часы сна считаются как раньше).
- 14 новых тестов (лестницы, перенос дроби, гейты, мост эффектов, крафт автоуспех/провал/гейт/заморозка, разбор успех/провал с осложнением/гейт, шесть разборов = час). Итого 837 зелёные, tsc --noEmit чистый. Надбавка «+1/ОД» по-прежнему только в контракте: счётчика ОД нет, и с подключением часов он не появился (решение владельца).

## Обновление — Разбор в инвентаре: подстрока состава и кнопка (патч 264)

> Из плана владельца: пункты 1 и 2. Из двух вариантов подсветки (цвета редкости или скрытие недостижимого) выбрано скрытие как более дешёвый и честный — цвета поедут в общий слой палитры позже.

- Строка предмета с печатным составом разбора получила подстроку «Разбор: …» — какие материалы и сколько можно вынуть герою прямо сейчас. Материалы выше потолка «Мусорщика» из списка убраны (263); вместо названий — счётчик «+N недоступно»: намёк есть, таблицы в UI не торчат. У предметов без состава подстроки нет; у составов с альтернативой «или» показывается объединение вариантов без обещания количества.
- Рядом с кнопкой продажи — кнопка «Разобрать»: один предмет из стека, проверка ИНТ+Ремонт и броски по-настоящему, всё через готовые операции пакета (260–263) с их списанием, выдачей и часами (262). Кнопка не показывается, когда вынимать нечего (перка нет), и на надетых предметах — попытка и так была бы отказом.
- Результат — алерт по месту: что получено (имена материалов по локали, количество), сколько времени ушло, отметка «осложнение удвоило время» (когда ×2), честный рассказ про сорванный разбор (хлам цел, вечер потерян) и про отказ без «Мусорщика». Тексты — в слое локалей экранов Fallout (ru/en), экран их только печатает.
- Модуль получил чистый хелпер salvageSublineForItem (для строк списка) и экспорт фильтра/потолка — превью модалки, подстрока и гейт разбора считают доступность одной функцией, разойтись не могут. 11 тестов (подстрока по рангам, «или», отчёт на все ветки результата, обвязка экрана по исходнику и ключи обеих локалей). Итого 862 зелёные, tsc --noEmit чистый. Пункт 3 (окна крафта) — следующим сообщением, проектом.

## Обновление — Потолок «Мусорщика» для состава и дуальность пачек в крафте (патч 263)

> Владелец закрыл этап 4 и добавил правило: редкость материалов, получаемых из хлама, ограничивается «Мусорщиком» и для печатного состава тоже.

- Разбор: без «Мусорщика» из хлама вынимаются только common-материалы, ранг 1 открывает unusual, ранг 2 — rare. Строки состава выше потолка не бросаются и не выдаются; если достижимых строк не осталось — отказ no-materials, предмет и 10 минут не тратятся. Сверх состава ничего не надбавляется: в хламе только common+unusual — ранг 2 даёт ровно то, что есть. Строка с отрезанным материалом, но достижимым эффектом сохраняется и бросает кости ради эффекта. Исключение по печатному составу (радио) под тем же потолком.
- Превью разбора несёт rarityCeiling и число срезанных строк (gatedRows) — основа для подсветки в модалке, когда дойдут руки до UI.
- Крафт, дуальность материалов: именованные материалы закрывают пачковые слоты рецептов (item_common/uncommon/rare_materials) своей редкости — редкости не смешиваются. Порядок списания: сначала сами пачки, затем закрытие по возрастанию каталожной цены (дешёвое сгорает в работе первым); в контракте spent/burned — фактические строки списания, а не план движка. Точные строки рецептов резервируют своё количество до закрытия (в печатных 93 рецептах пересечений нет — проверка-гард; резерв страхует домашние рецепты). Флаг CRAFT_RULES.packSubstitutionByRarity выключает режим обратно. have пачки в превью включает покрытие, поэтому «чего не хватает» считается честно.
- Тесты: 7 на потолок состава (аккумулятор common/unusual/rare по рангам, отказ с целым предметом, радиостанция как линк-исключение, эффект-строка, превью), 7 на дуальность (закрытие, приоритет пачек, порядок цены, отказ при выключенном флаге, превью, гард данных); 5 существующих тестов печатного состава переведены на «Мусорщик 2» — иначе они падали бы ровно так, как теперь должна падать игра. Итого 851 зелёная, tsc --noEmit чистый.

## Исправление — Разбирается только хлам, если не указано иного (патч 261)

> Владелец зафиксировал корневое правило разбора. Реализация жила чёрным списком (расходники, боеприпасы, броня, моды) и молча разрешала разбирать всё остальное; теперь отказ по умолчанию.

- Не-хлам разбирается только если для него напечатан состав в scrap/salvage.json (например радио) — тогда работает и табличная ветка с проверками и весом выхода. Всё, что не хлам и без состава, получает отказ not-salvageable и не расходуется.
- Общий выход (1 материал common, кап по весу, «Мусорщик» поднимает потолок до необычных/редких, клей и масло никогда) применяется только хламу без печатного состава: легасийные записи вроде желёзы бычьи мухи и жал жалокрыла остаются разборными как мусор.
- Адаптер больше не догадывается о типе по префиксу id (исторический костыл для боеприпасов) — тип знает реестр; в rules.js чёрный список заменён на salvageableItemType: 'junk'.
- Тест переведён на новый смысл: бита отказывает и не тратится; потолок редкости проверяется на хламе без таблицы. Suite 823 зелёные, tsc --noEmit чистый.

## Обновление — Разбор: реестр, операция и видимость в окнах (патч 260)

> Данные разбора (256–257) подключены к игре: предметы узнаёт реестр, герои могут разбирать сумку, а новый хлам и материалы видны в окне добычи и в окне покупки в нужных категориях.

- Реестр пакета получил геттеры разбора: список хлама, список материалов, печатный состав разбора по id предмета, d20-таблицы для ГМ-секции (категории, девять таблиц, добыча).
- Каталог снаряжения (источник окон) отдаёт два новых списка: «Хлам» — 130 предметов с ценами и весами из таблиц, «Материалы» — 36 позиций (три пачки + именованные). Окно добычи (выдача) и окно покупки работают с ними как с остальными предметами: поиск по дереву категорий находит и хлам, и материалы; продажа в окне продажи считает цену по cost из данных.
- Окно выдачи: категория «Хлам» добавлена в дерево и в подписи обеих локалей; категория «Материалы», пустовавшая как заглушка, заполнена справочником.
- Операция разбора — новым модулем пакета по образцу крафта: универсальный движок (форма состава: фиксированные единицы, DC-кости с законным нулём, эффект-грани, «или», режим не-хлама с пулом по редкости) + адаптер сеттинга (реестр, стор, кубики, «Мусорщик»).
- Правила разбора (настройки пакета): проверка ИНТ + Ремонт, сложность 0 (пасть может только осложнение); разбор идёт 10 минут на предмет; осложнение успех не отменяет, а помножает время на два (то же правило, что 259 для крафта); при провале предмет не расходуется и выход не бросается; расходники и боеприпасы не разбираются; из не-хлама никогда не выпадают клей и масло; «Мусорщик» поднимает потолок редкости выхода (ранг 1 — необычные, ранг 2 — и редкие); надбавка «+1 за ОД» держится в контракте, но не считается до появления счёта ОД/времени.
- Списание и выдача — тот же атомарный канал стора, что у крафта (spendItemStacks/addNewItem): отказ на любом шаге не меняет баланс. Предметы без каталожной записи не разбираются (гейт unknown-item).
- 31 новый тест (движок на стабах, операции на реальном сторе и настоящих данных, видимость в каталоге и обвязке модалки). Итого 822 зелёные, tsc --noEmit чистый.
- Вне патча (следующие шаги): подстроки состава и редкости в списке инвентаря, цвета редкости, ГМ-броски по таблицам добычи, привязка времени к часам выживания.

## Исправление — Осложнение не отменяет успех: оно помножает время (патч 259)

> Откат правила 258 «осложнение портит работу»: оно противоречило корневой системе — при успехе и осложнении одновременно успех остаётся в силе. Замечание владельца от 2026-09-16 принято.

- Новое правило (распространяется на все проверки, где есть время): если на проверке есть осложнение, длительность работы умножается на два. Десять минут крафта становятся двадцатью, час — двумя, сутки — двумя. То же касается времени негативных эффектов.
- Реализация: универсальный движок крафта отдаёт в ответе множитель `durationMultiplier` (2 при осложнении, иначе 1); куда именно его прикладывать, решает сеттинг. Число множителя — настройка пакета (`complicationDurationMultiplier`), данные рецептов не тронуты. Пока часы выживания не считают минуты крафта, множитель никуда не применяется — правило заведено заранее, как договорились.
- Сгорание материалов при ПРОВАЛЕ на кухне и в химлаборатории остаётся (патч 251) — это провал, а не «успех с осложнением». Успех с осложнением: предмет создаётся, материалы израсходованы, время удвоено.
- Карта верстаков (навыки и перки) из 258 не изменена — под неё только переписаны тесты.
- 10 тестов пересобраны под новую семантику. Итого 791 зелёные, `tsc --noEmit` чистый.

## Обновление — Крафт: карта навыков верстаков и порча при осложнении (патч 258)

> По тексту владельца (2026-09-16, кн. с. 210–212): какие навыки проверяются за каким верстаком и какие перки при этом «применяются», плюс уточнённое правило: при осложнении на готовке еды/питья и препаратах ингредиенты **пропадают** — без «могут».

- В настройки крафта легла карта «верстак → навыки/перки»: броня — Ремонт (+«Бронник»); химла — Наука (+«Химик»), взрывчатка — Взрывчатка (+«Эксперт-подрывник») — разделение держится на рецептах, карта это фиксирует; кухня — Выживание; станция обслуживания СИ — Ремонт+Наука (+«Бронник», «Наука!»); роботы — Ремонт+Наука (+«Эксперт по робототехнике»); оружие — Ремонт+Наука (+«Оружейник», «Наука!», «Кузнец»). Строки брони/СИ/роботов — на будущее: рецептов этих категорий в каталоге пока нет. Тест-инвариант: навык каждого рецепта обязан быть в списке своего верстака.
- Перковая часть текста уже реализована тем, чем перки являются в наших данных: гейтами рецептов («Химик»/«Эксперт-подрывник» обязательны там, где требует книга) и доступом к рангам модификаций. Новых бонус-кубиков карта не вводит.
- Правило порчи: на кухне и в химлаборатории любое осложнение на проверке портит изделие даже при достаточных успехах — материал списан, предмет не выдаётся (новая причина результата complication-spoiled). Провал сжигает материалы, как и раньше; двойное осложнение остаётся автопровалом. Автоуспех (ранг навыка покрывает сложность) проверкой не считается и не портится.
- Превью рецепта для экранов получило benchView: навыки верстака, релевантные перки, флаги сгорания/порчи — экран сможет показать это без обращения к правилам.
- 10 новых тестов (поведение движка на стаб-портах + целостность карты с данными). Итого 791 зелёные, tsc --noEmit чистый.

## Исправление — Идентификаторы разбора: имя предмета вместо типа в id (патч 257)

> По замечанию владельца: то, что предмет — хлам или материал, знает только реестр; в идентификаторе типа быть не должно. Один предмет — один id: «предмет по рецепту = предмет в каталоге», дубликатов «junk_чего-то» и «просто чего-то» нет.

- Все хлам-предметы и руды переименованы из `junk_*` в слаг имени (abraxo_cleaner, blood_sac, iron_ore…); именованные материалы — из `mat_*` в bare (steel, wood, nuclear_material…); антисептик и асбест — antiseptic/asbestos. Ссылки рецептов, составов разбора и d20-таблиц перегенерированы за идентификаторами; имена в зеркалах сохранены.
- Радио и остальные линки на реальные предметы каталога — без изменений (item_radio остаётся item_radio: это предмет каталога, а не хлам-копия).
- Генератор получил страж: новый id, уже занятый предметом основного каталога, — падение с требованием линковать, а не дублировать.
- Миграция сейвов не требуется: id разбора ещё не выдавались игроку (реестр их не показывал). Пачковые материалы (item_common_materials и т.д.) не тронуты — они продаются в магазине и живут в сейвах.
- Тесты: 781 зелёные, `tsc --noEmit` чистый; детерминизм генераторов (`--check`) подтверждён.

## Обновление — Разбор, часть 1: хлам, материалы и таблицы ГМ как данные (патч 256)

> Большой шаг начат срезом данных: таблицы разбора, переданные владельцем 2026-09-16, перенесены в каталоги один-в-один. Поведение игры пока не изменилось — это данные под последующие патчи.

- Справочник именованных материалов: 33 позиции. Девять рецептов крафта на пачковых материалах не тронуты; для четырёх материалов, отсутствовавших в книге (кристалл, золото, уголь, железо), числа по правилу редкости (1/3/5 крышек, вес 1 фунт). У остальных цена — печатная за штуку, вес единицы = книжный «вес за 10 штук» ÷ 10 (сталь 0.2, бетон 3, кость 1).
- Антисептик и асбест переехали из хлама в материалы: идентификаторы сохранены, ссылки рецептов целы; антисептик подешевел до книжных 3 крышек, весят теперь 0.1 за штуку.
- Каталог хлама: 130 записей — все предметы девяти книжных таблиц (находки «<1» по принятому соглашению весят 0.2), шесть руд (вес 1, цена по редкости выхода) и два владельца-легаси без изменений. Радио синхронизировано с таблицей: вес 3, цена 10 (решение владельца «числа из таблицы побеждают»).
- Составы разбора: у каждого предмета — альтернативы («или»), в альтернативах фиксированные количества, DC-компоненты (броски кастомного d6 по механике единого модуля кубиков), эффект-добавки при выпадении эффект-грани, разовый эффект «any» (высокотехнологичное устройство). Руды: разбор единицы даёт один материал, железная — случайно (2 DC).
- Таблицы для ГМ-секции: d20 категорий поиска, все девять таблиц по граням, таблица добычи с формулами количества («2+3<cd>» — язык парсера единого модуля). Два перенаправления книги сохранены явно: 12–20 «животных» — бросок по каталогу еды, 19–20 «бытовых-4» — повтор броска по таблице рабочих предметов.
- Хозяйство среза: источник истины `scripts/scrap-source.json` (транскрипция таблиц владельца), генератор `scripts/build-scrap-data.mjs` с режимом `--check`; i18n-зеркала ru/en; 17 новых тестов (полнота граней 1..20, разрешаемость ссылок, инварианты каталогов и зеркал). Итого 781 тест зелёные, `tsc --noEmit` чистый.
- Вне этого патча (по плану шага): реестр, операция разбора с проверкой ИНТ+Ремесло, отображение составов в инвентаре, цвета редкости, «осложнение сжигает продукты» в правилах крафта — следующие патчи.

## Обновление — Стили оформления переехали в модуль сеттинга (патч 255)

> Унификация по требованию владельца: оформление Fallout больше не живёт в движковых папках.

- В модуле завелась своя папка стилей: туда переехали все оформления экранов, которые уже принадлежат сеттингу (персонаж, перки, оружие-броня и их модалки — десять файлов), и получили копию оформления инвентаря, ряда валюты и окна добавления предмета — на случай, когда эти экраны переедут в сеттинг, крутить их внешний вид можно будет не трогая движок.
- Экранам, которые остались в движке (главный, инвентарь, окно добавления предмета), оставлена копия текущих стилей на прежнем пути: движковые файлы не изменены ни на пиксель, путаницы «чей стиль» нет — у движка своё, у сеттинга своё, расхождение после этого патча — норма, а не авария.
- Исправлен единственный тест, который читал файл оформления оружия напрямую: он смотрит теперь в папку сеттинга.
- Твои ручной подбор из патча 254 (колпачок 48px, кегли 14) — внутри копий, не потерялся; цепочка `./apply-patch.sh` поставит 254 и 255 по порядку.
- Тесты: 764 зелёные, `tsc --noEmit` чистый; импорты проверяет тест-предохранитель разрешения путей.

## Обновление — Инвентарь: итоговые размеры ряда валюты (патч 254)

> Владелец подобрал числа руками на своём чекауте — закатаны в ветку, чтобы никто не терял настройку.

- Колпачок рисуется 48×48 (источник 64×64), подписи «−»/«+» и слово «Крафт» подняты с 12 до 14 — рядом с крупной иконкой мелкий текст тонул.
- Остальное не менялось: лимит 40%, отступы и ключи словарей — как в патчах 252–253. Ключ у кнопки «Крафт» остался 14: с текстом 14 он теперь одного роста.
- Если 48-пиксельному колпачку с 14-м кеглем станет тесно под лимитом 40% — поднять `maxWidth` у `currencyContainer` (или убрать).

## Обновление — Инвентарь: ряд валюты получает нейтральные имена (патч 253)

> Продолжение унификации общего слоя: то, что в Fallout зовётся крышками, в движковых файлах больше не названо крышками.

- Строка в экране инвентаря, блок стилей и ключи словаря переименованы в нейтральное «currency»: в другом сеттинге (или при нескольких валютах) общего кода не переименовывать — он сразу зовёт это валютой, а не крышками.
- Игрок не видит ничего: как называть валюту и какую иконку показывать, по-прежнему решает модуль сеттинга — у Fallout те же «Крышки» и тот же колпачок.
- Кнопка «Крафт» съехала из валютного блока в свой: крафт — не про деньги, у него теперь собственные подписи в обоих языках.
- Числа (32px и 40%) не менялись — крути их вручную в стилях, как договаривались; переименование не ломает ручную подгонку, только имя строки.
- Тесты: 764 зелёные, `tsc --noEmit` чистый.

## Обновление — Инвентарь: компактная строка крышек и кнопка «Крафт» (патч 252)

> Быстрая правка по требованию владельца: ряд крышек в экране инвентаря больше не съедает место.

- Область крышек уменьшена до 40%: блок теперь занимает не больше 40% ширины строки, отступы и кегли сокращены, рядом с ним вписалась новая кнопка.
- Слово «Крышки» заменено иконкой: тот же капсюль из ассетов модуля (64×64) рисуется в 32×32, как и договаривались.
- Кнопки «↓ Списать» / «↑ Внести» стали «−» и «+» — порядок и поведение прежние, тексты-заглушки убраны из обоих языков.
- Справа от области крышек появилась кнопка «Крафт» с векторной иконкой гаечного ключа (из уже установленного набора иконок). Экрана крафта ещё нет, поэтому сейчас она сообщает, что раздел следующий; заглушка на месте, wiring — за экраном.
- Тесты: 764 зелёные, `tsc --noEmit` чистый.

## Обновление — Крафт, часть 3: универсальная механика крафта (патч 251)

> Игра умеет крафтить по-настоящему: универсальный движок проверяет рецепт, атомарно списывает материалы и выдаёт предмет. Экрана ещё нет — решение и причины отказа готовы для него, экран следующий шаг.

- Механика живёт в движке и знает только ФОРМУ рецепта: идентификаторы предметов, особенностей и верстаков для неё непрозрачны. Сеттинг подсказывает, где смотреть рецепты (реестр данных), какие бросать кубики и что значит «списать» и «выдать». Тест-охрана следит, что в движок не заползло знание файлов сеттинга.
- Контракт решения: «сделано» (что списано, что выдано, итог проверки — или автоуспех) либо «отказ с причиной» (чего не хватает и на сколько; сумка не тронута). Тот же трюк, что с покупками за крышки: отказ не меняет баланс; списание стоков — новая атомарная операция стора «всё или ничего», надетое и запертое комплектом не расходуется, паспорт операций обновлён в этом же патче.
- Проверка — по книге (с. 210–211): сложность = сложность рецепта минус ранг навыка, пол — ноль; ноль означает автоуспех без броска; иначе — обычный движковый тест 2к20 (успех — кость не выше атрибут+навык, «20» — осложнение, два осложнения автопровал). Каким атрибутом мерить, книга не закрепляет (воля ГМ): по умолчанию INT, это число в блоке правил модуля, не в рецепте и не в сейве.
- Количество результата: 1 или база + боевые кубики (порт rollCD). Форма количества проверяется ДО списания — битые данные не успеют стереть сумку.
- Провал: на кухне и в химии материалы сгорают (правило книги), на других верстаках — только сорванная работа. Список «горячих» верстаков — число правил модуля.
- Часов и ОД (длительность, сокращение за 2 ОД, минуты за осложнения) здесь намеренно нет: это интеграция с часом выживания, отдельный шаг. Видимость хлама и материалов в инвентаре — тоже отдельное решение владельца.
- 764 теста зелёные (новые: 12 на движке без сеттинга, 11 на адаптере с настоящими рецептами), `tsc --noEmit` чистый. Документ контракта: `docs/architecture/crafting-engine.md`.

## Обновление — Крафт, часть 2: закрытие дыр каталога (патч 250)

> Патч 249 (его содержимое уже в main) поселил рецепты в данные, но упёрся в отсутствующие предметы. Вы дописали их числа — недостающее встало в справочники, и 19 запертых рецептов переехали в данные.

- Рецептов в категории теперь **93**: боеприпасы 28 (добавлены 9 дротиков шприцера), препараты 21 (все девять «проблемных»: от антибиотиков до ультра-джета), остальное без изменений.
- Каталог еды пополнился тремя сырыми дикоросами (Светящийся гриб, Центроцвет, Кроволист: вес 0.5, цена 4, +3 HP, +1d6 радиации, риск сырой пищи); таблица сбора `modules/fallout/data/loot/foraging.json` (d20 → id еды, 20 бросков без пропусков). Правила самого сбора — за патчем выживания.
- Заведены два новых справочника: хлам `modules/fallout/data/junk.json` (семь компонентов разбора; «Абраксо» — из таблицы Household Items 1 (вес 1, цена 10), остальные — оценка владельца по аналогии; антисептик и асбест остаются предметами: «достаётся разбором» — ветка правил, не данных) и материалы `modules/fallout/data/materials.json` (вес 1, цена 1/3/5). У обоих i18n-зеркала; движок увидит их через реестр в своих патчах.
- Дротики записаны в каталог боеприпасов с текстами карточек (ru — ваши, en — по канону). Поле `effect` инертно: заготовка под «заряженный шприц», `mapsToQuality` проставлен только там, где эффект — прямой алиас существующего качества.
- Дротики крафтятся по одному: их нет в таблице находки, единицу зафиксировал владелец. Обычные боеприпасы — как раньше, по объёму находки.
- Для чисел, которые владелец диктует поверх книги, в генераторе завелась таблица исправлений: первое — «Ментаты» = Необычные ×3, Редкие ×2, Мозговой гриб ×2. Правка, не нашедшая свою строку в источнике, роняет генератор.
- В файле-обменнике `docs/reference-data/Missing_craft.json` осталась одна позиция: «Cooking Station» — предмет верстака пока не описан.
- Файла патча 249 в цепочке нет: его содержимое влито в main («Add recipes»), дублировать нечем. 741 тест зелёный, `tsc --noEmit` чистый; предохранитель `__tests__/crafting/` — 22 проверки, включая таблицу дикоросов и ваши числа «Ментат».

## Обновление — Крафт, часть 1: рецепты как данные (патч 249)

> Первый шаг системы крафта: рецепты поселились в данных как отдельная категория. Игрок пока ничего не видит — скрафчивать начнёт следующий патч, когда движок научится их проверять, списывать и выдавать.

### Что появилось в данных
- В пакете Fallout завелась категория «рецепты»: `modules/fallout/data/crafting/` — пять файлов по тому, что крафт даёт (боеприпасы, взрывчатка, препараты, еда, напитки), и индекс категории. Всего **75 рецептов**: 19 боеприпасов, 9 гранат и мин, 12 препаратов, 27 блюд, 8 напитков.
- Идентификатор рецепта = **id предмета, который он даёт**: «скрафтить ягодные ментаты» — это `chem_mentats_berry`, а не отдельный ключ `craft_chems_berry_mentats`. Второй таблицы соответствий «рецепт ↔ предмет» в программе нет: она была бы вторым источником правды и умерла бы при первом же переименовании. Если один предмет умеют давать несколько рецептов (у книги так у боеприпасов: другой ранг перка — другой рецепт), различие дописывается к id после подчёркивания — например `ammo_45_ammosmith2`; сейчас таких мест нет, все 75 рецептов дают 75 разных предметов.
- Каждый рецепт — только ссылки: что получится (предмет из каталога), из чего (предметы и количество), каким навыком и какой сложности, какая особенность разрешает и с какого ранга, у какого станка делается. Имён, описаний и «названий материалов» в рецептах нет намеренно: имя, вес и цену предмету по-прежнему даёт каталог, а не рецепт.
- Ингредиенты бывают двух видов: выписанные таблицей книги (например, «ягодные ментаты» = Ментаты ×1 + Смоляника ×2 + Редкие материалы ×1) или взятые по печатному правилу «материалы определяет сложность» (с. 210). Вторые помечены в данных, и тест сверяет метку с настоящей кривой сложности — она уже была проставлена на 143 модах оружия, расхождений ноль.
- Боеприпасы крафтятся в том же объёме, в котором находятся: количество берётся из нашей таблицы находки (.38 → 10+5 БК, термоядерное ядро — ровно 1, бросок к ядрам неприменим).
- Ни одно число правила не зашито в данные: длительность работы, 2 ОД, осложнения и «сгорает ли сырьё при провале» — это будущие настройки, а не поля рецепта.

### Чего в этом патче нет и почему
- **Модификации.** Крафт выдаёт предметы; ветка «установить мод на предмет» настраивается отдельно (ваше решение). Колонки сложности/перков/материалов уже проставлены на 205 модах оружия и на модах брони, так что их рецепты переложатся в тот же формат без новых сведений.
- **Дыры в каталоге.** Девять препаратов упираются в ингредиенты, которых у нас нет как предметов (светящийся гриб, хаб-цвет, «Абраксо», мешок с кровью, антисептик, кроволист, шприц берсерка); девять рецептов дротиков шприцера и «верстак-кухня» — в то, что самих этих предметов в каталоге нет. Генератор не придумывает предметы и не решает за владельца, какой книгой их описывать: все 19 таких позиций выгружены в `docs/reference-data/Missing_craft.json` — файл-обменник с полями как у рецептов, где неизвестное записано строкой `unknown`. Владелец дописывает числа по своим книгам и возвращает файл для слияния.
- **Совпадения названий закрыты сразу**, потому что это не новые сведения, а одно и то же, названное иначе: «Tato Juice» = наш «Potato Juice», «Mutant Hound Chops» = наше «Mutant Hound Ribs» (эффект слово в слово), «Mutt Chops» = «Dog Chops», «Queen Mirelurk Meat» = «Mirelurk Queen Meat» — десять соответствий, каждое с причиной в отчёте `docs/reference-data/CRAFTING-MAPPING.md`.
- **Формат сейва и хранилище не тронуты**, экраны тоже: данные пока читает только тест-предохранитель `__tests__/crafting/` (18 проверок: индекс, ссылки, отсутствие имён, кривая материалов, совпадение файлов и обменника с генератором). 737 тестов зелёные, `tsc --noEmit` чистый.

### Как это править дальше
Справочный датасет (`docs/reference-data/pipboyapp_crafting.json`) → `node scripts/build-crafting-data.mjs` → файлы категории `crafting` + отчёт + обменник. Править JSON рецептов руками бесполезно: тест сверяет их с выводом генератора и падает на расхождении.

---

## Обновление — Переустройство «сердца» игры: единое хранилище, сохранения, паспорта данных (патчи 219–247)

> Большая закулисная работа: всё, что игра помнит о персонаже, переехало в единое хранилище, сохранения стали самостоятельным модулем, у данных и операций появились машинные «описи». Игроку — тише, надёжнее и несколько игровых фиксов; формат сейвов не менялся (старые сейвы загружаются).

### Единое хранилище персонажа (219–243)
- Всё состояние персонажа — профиль (имя, происхождение, черта, уровень), атрибуты/навыки, здоровье/радиация, крышки, инвентарь и надетое, комплект, робот, силовая броня, перки, болезни и состояния, счётчик сцен, временные эффекты, изменённые предметы, отметки «сохранено» — переехало из React-обёртки в единое хранилище (Zustand). Экраны читают и пишут напрямую, без зеркал и посредников.
- Переезд шёл шагами (каждый патч — группа полей, приложение рабочее после каждого), в финале (243) старый файл-«раздатчик» и его обёртка в точке входа удалены целиком — минус ~1300 строк и целый класс ошибок рассинхрона.
- Правила операций закреплены в хранилище: списание возвращает «куплено/отказ с причиной» и не трогает кошелёк при отказе («нельзя купить на больше, чем есть»); все сеттеры принимают и «от текущего значения»; таймер Ядерного блока тикает только при открытом приложении.

### Игровые фиксы и решения по книге (229–241)
- 229 — «фальшивая вода»: битый предмет очищенной воды из старых сейвов чинится при загрузке.
- 230 — предметы без цены в правилах продаются за разумный дефолт, а не за 0.
- 231 — лестница усталости показывает уровень цветом, без текстовых подсказок.
- 232 — усталость по книге: −⌊N/2⌋ текущих ОЗ за игровой час активности; сон из правила исключён (лечение — постелью).
- 237 — схема предмета «база + id модов на предмете»: имя собирает каталог (пример: `weapon_huntingRifle_05_76_48_35` → «.50 длинноствольная охотничья высокоемкая тихая винтовка»); верстак обновляет предмет на месте.
- 238 — подтверждение атрибутов: единая операция с проверкой границ и пересчётом здоровья.
- 239–240 — сцены и время: эффекты тикают от сцен и игровых часов; расходники применяются одной операцией (лечение → радиация → эффекты → риск заражения); превью радиации до применения; «Сопротивляться» болезни — одна попытка в сутки; лечение снижает ранги болезни.
- 241 — «смена комплекта» больше не обнуляет здоровье/радиацию/сцены/условия/журнал доз; tagged-навыки при смене комплекта сохраняются (а полный сброс честно чистит всё).

### Сохранения (242)
- Блокнот сохранений (сохранить/загрузить/список/удалить/автосохранение) — отдельный модуль без привязки к React. Автосейв пишет только уже сохранённого героя, с паузой полсекунды, и не перезаписывает сейв, если ничего не изменилось; облако синхронизируется фоном.

### «Паспорта» данных и операций (244–246, TypeScript)
- У хранилища и формата сейва появились машинные «описи» со строгой проверкой: новое поле или операция без записи в опись роняет контрактный тест — «тихо проскочить мимо» больше нельзя.
- Опись сразу нашла живой дефект (244): журнал «подарки за отмеченные навыки выданы» писался в сейв, но при загрузке не читался — цепочка «сохранился → загрузился → подтвердил навыки» могла вручить стартовые подарки второй раз. Исправлено (формат сейва не тронут).
- 246 — опись операций доведена до полного покрытия (все 98 действий хранилища), введён единый контракт ответа «сделано / отказ с причиной».

### Формат-v2: сейвы без картотеки модов (247)
- Из записи сейвов убрана старая картотека изменённых предметов — дублирующий учёт, оставшийся от прежней схемы (запись прекратилась ещё в 237, теперь ключа в сейве нет вовсе). Ничего видимого не меняется: имя и моды восстанавливаются каталогом из id предмета.
- Мост для старых героев: при загрузке сейва до-237 эпохи содержимое картотеки один раз переносится на сами предметы; записи о потерянных/проданных предметах остаются в read-only картотеке инвентаря. После пересохранения сейв уже без картотеки. Версия схемы и цепочка миграций не тронуты.

---

## Обновление — Переустройство «сердца» игры: единое хранилище, сохранения, паспорта данных (патчи 219–247)

> Большая закулисная работа: всё, что игра помнит о персонаже, переехало в единое хранилище, сохранения стали самостоятельным модулем, у данных и операций появились машинные «описи». Игроку — тише, надёжнее и несколько игровых фиксов; формат сейвов не менялся (старые сейвы загружаются).

### Единое хранилище персонажа (219–243)
- Всё состояние персонажа — профиль (имя, происхождение, черта, уровень), атрибуты/навыки, здоровье/радиация, крышки, инвентарь и надетое, комплект, робот, силовая броня, перки, болезни и состояния, счётчик сцен, временные эффекты, изменённые предметы, отметки «сохранено» — переехало из React-обёртки в единое хранилище (Zustand). Экраны читают и пишут напрямую, без зеркал и посредников.
- Переезд шёл шагами (каждый патч — группа полей, приложение рабочее после каждого), в финале (243) старый файл-«раздатчик» и его обёртка в точке входа удалены целиком — минус ~1300 строк и целый класс ошибок рассинхрона.
- Правила операций закреплены в хранилище: списание возвращает «куплено/отказ с причиной» и не трогает кошелёк при отказе («нельзя купить на больше, чем есть»); все сеттеры принимают и «от текущего значения»; таймер Ядерного блока тикает только при открытом приложении.

### Игровые фиксы и решения по книге (229–241)
- 229 — «фальшивая вода»: битый предмет очищенной воды из старых сейвов чинится при загрузке.
- 230 — предметы без цены в правилах продаются за разумный дефолт, а не за 0.
- 231 — лестница усталости показывает уровень цветом, без текстовых подсказок.
- 232 — усталость по книге: −⌊N/2⌋ текущих ОЗ за игровой час активности; сон из правила исключён (лечение — постелью).
- 237 — схема предмета «база + id модов на предмете»: имя собирает каталог (пример: `weapon_huntingRifle_05_76_48_35` → «.50 длинноствольная охотничья высокоемкая тихая винтовка»); верстак обновляет предмет на месте.
- 238 — подтверждение атрибутов: единая операция с проверкой границ и пересчётом здоровья.
- 239–240 — сцены и время: эффекты тикают от сцен и игровых часов; расходники применяются одной операцией (лечение → радиация → эффекты → риск заражения); превью радиации до применения; «Сопротивляться» болезни — одна попытка в сутки; лечение снижает ранги болезни.
- 241 — «смена комплекта» больше не обнуляет здоровье/радиацию/сцены/условия/журнал доз; tagged-навыки при смене комплекта сохраняются (а полный сброс честно чистит всё).

### Сохранения (242)
- Блокнот сохранений (сохранить/загрузить/список/удалить/автосохранение) — отдельный модуль без привязки к React. Автосейв пишет только уже сохранённого героя, с паузой полсекунды, и не перезаписывает сейв, если ничего не изменилось; облако синхронизируется фоном.

### «Паспорта» данных и операций (244–246, TypeScript)
- У хранилища и формата сейва появились машинные «описи» со строгой проверкой: новое поле или операция без записи в опись роняет контрактный тест — «тихо проскочить мимо» больше нельзя.
- Опись сразу нашла живой дефект (244): журнал «подарки за отмеченные навыки выданы» писался в сейв, но при загрузке не читался — цепочка «сохранился → загрузился → подтвердил навыки» могла вручить стартовые подарки второй раз. Исправлено (формат сейва не тронут).
- 246 — опись операций доведена до полного покрытия (все 98 действий хранилища), введён единый контракт ответа «сделано / отказ с причиной».

### Формат-v2: сейвы без картотеки модов (247)
- Из записи сейвов убрана старая картотека изменённых предметов — дублирующий учёт, оставшийся от прежней схемы (запись прекратилась ещё в 237, теперь ключа в сейве нет вовсе). Ничего видимого не меняется: имя и моды восстанавливаются каталогом из id предмета.
- Мост для старых героев: при загрузке сейва до-237 эпохи содержимое картотеки один раз переносится на сами предметы; записи о потерянных/проданных предметах остаются в read-only картотеке инвентаря. После пересохранения сейв уже без картотеки. Версия схемы и цепочка миграций не тронуты.

---

## Обновление — Переустройство «сердца» игры: единое хранилище, сохранения, паспорта данных (патчи 219–247)

> Большая закулисная работа: всё, что игра помнит о персонаже, переехало в единое хранилище, сохранения стали самостоятельным модулем, у данных и операций появились машинные «описи». Игроку — тише, надёжнее и несколько игровых фиксов; формат сейвов не менялся (старые сейвы загружаются).

### Единое хранилище персонажа (219–243)
- Всё состояние персонажа — профиль (имя, происхождение, черта, уровень), атрибуты/навыки, здоровье/радиация, крышки, инвентарь и надетое, комплект, робот, силовая броня, перки, болезни и состояния, счётчик сцен, временные эффекты, изменённые предметы, отметки «сохранено» — переехало из React-обёртки в единое хранилище (Zustand). Экраны читают и пишут напрямую, без зеркал и посредников.
- Переезд шёл шагами (каждый патч — группа полей, приложение рабочее после каждого), в финале (243) старый файл-«раздатчик» и его обёртка в точке входа удалены целиком — минус ~1300 строк и целый класс ошибок рассинхрона.
- Правила операций закреплены в хранилище: списание возвращает «куплено/отказ с причиной» и не трогает кошелёк при отказе («нельзя купить на больше, чем есть»); все сеттеры принимают и «от текущего значения»; таймер Ядерного блока тикает только при открытом приложении.

### Игровые фиксы и решения по книге (229–241)
- 229 — «фальшивая вода»: битый предмет очищенной воды из старых сейвов чинится при загрузке.
- 230 — предметы без цены в правилах продаются за разумный дефолт, а не за 0.
- 231 — лестница усталости показывает уровень цветом, без текстовых подсказок.
- 232 — усталость по книге: −⌊N/2⌋ текущих ОЗ за игровой час активности; сон из правила исключён (лечение — постелью).
- 237 — схема предмета «база + id модов на предмете»: имя собирает каталог (пример: `weapon_huntingRifle_05_76_48_35` → «.50 длинноствольная охотничья высокоемкая тихая винтовка»); верстак обновляет предмет на месте.
- 238 — подтверждение атрибутов: единая операция с проверкой границ и пересчётом здоровья.
- 239–240 — сцены и время: эффекты тикают от сцен и игровых часов; расходники применяются одной операцией (лечение → радиация → эффекты → риск заражения); превью радиации до применения; «Сопротивляться» болезни — одна попытка в сутки; лечение снижает ранги болезни.
- 241 — «смена комплекта» больше не обнуляет здоровье/радиацию/сцены/условия/журнал доз; tagged-навыки при смене комплекта сохраняются (а полный сброс честно чистит всё).

### Сохранения (242)
- Блокнот сохранений (сохранить/загрузить/список/удалить/автосохранение) — отдельный модуль без привязки к React. Автосейв пишет только уже сохранённого героя, с паузой полсекунды, и не перезаписывает сейв, если ничего не изменилось; облако синхронизируется фоном.

### «Паспорта» данных и операций (244–246, TypeScript)
- У хранилища и формата сейва появились машинные «описи» со строгой проверкой: новое поле или операция без записи в опись роняет контрактный тест — «тихо проскочить мимо» больше нельзя.
- Опись сразу нашла живой дефект (244): журнал «подарки за отмеченные навыки выданы» писался в сейв, но при загрузке не читался — цепочка «сохранился → загрузился → подтвердил навыки» могла вручить стартовые подарки второй раз. Исправлено (формат сейва не тронут).
- 246 — опись операций доведена до полного покрытия (все 98 действий хранилища), введён единый контракт ответа «сделано / отказ с причиной».

### Формат-v2: сейвы без картотеки модов (247)
- Из записи сейвов убрана старая картотека изменённых предметов — дублирующий учёт, оставшийся от прежней схемы (запись прекратилась ещё в 237, теперь ключа в сейве нет вовсе). Ничего видимого не меняется: имя и моды восстанавливаются каталогом из id предмета.
- Мост для старых героев: при загрузке сейва до-237 эпохи содержимое картотеки один раз переносится на сами предметы; записи о потерянных/проданных предметах остаются в read-only картотеке инвентаря. После пересохранения сейв уже без картотеки. Версия схемы и цепочка миграций не тронуты.

---

## Обновление — Комплекты снаряжения Штурмотрона

### Ориджин «Штурмотрон» (контент)
- Три комплекта снаряжения по книге (Wanderer's Guide): «Модель армии США», «Штурмотрон-дьявол», «Караванный охранник» (`modules/fallout/data/equipmentKits/assaultron.json`).
- Каждый комплект выдаёт голову «Голова Ассолтрона (лазер)»; у головы появилось встроенное оружие `robot_weapon_assaultron_head_laser` (`builtinWeaponId`) — лазер головы отображается на экране «Снаряжение» карточкой со статами (5 {CD} энергия, Пробивание 1, энергоячейки).
- «Штурмотрон-дьявол»: два строительных когтя занимают слоты рук как оружие-конечности — карточки со статами (4 {CD} физический, «Вплотную», Разрушение); плюс зубчатая обшивка (корпус или рука+нога на выбор — по книге одна рука и одна нога на выбор игрока, в системе реализовано как руки+ноги из-за обобщения локаций Arms/Thruster), модуль обнаружения опасности, 6+6 {CD} энергоячеек, ремкомплект и новый головной убор «Маска-череп» (`headwear_skull_mask`).
- «Модель армии США»: лазерный пистолет, встроенный в руку, приводная рама (корпус или рука+нога на выбор — по книге одна рука и одна нога, в системе — руки+ноги) + стандартная обшивка на остальные зоны, разведсенсоры, 8+7 {CD} энергоячеек, 15 крышек.
- «Караванный охранник»: встроенный лазерный пистолет, грузовой корпус и заводская броня ног, модуль анализа поведения, товары (3 броска аптечных предметов и 3 броска безделушек), 14+7 {CD} энергоячеек, 20 крышек.

### Движок (роботы)
- `domain/robotEquip.js`: рамы (`robotFrame`) из комплекта теперь ложатся в слой `frame` слотов (раньше падали «мёртвым» предметом в инвентарь); локация «Thruster» у обшивки/рам/брони распределяется и на ноги/гусеницы (протектрон, штурмотрон, робомозг), а не только на трастер/колесо.
- Модалка комплекта: `robotFrame`/`robotArmor` относятся к «Стандартной конструкции» и не дублируются в инвентарь.
- Тесты: `__tests__/robot/assaultron-kits.test.js` — комплекты существуют и локализованы, голова даёт лазер со статами, когти дают карточки оружия со статами, рамы/обшивка ложатся в слои.

---

## Обновление — Комплекты снаряжения, Осколок Анклава, экран «Снаряжение» (патчи 69–89)

> Свежие изменения за эту неделю. Старые записи (до 68) идут ниже в обычном порядке.

### Комплекты снаряжения (движок)
- Единая модалка комплекта для всех трейтов/ориджинов: выборы (оружие, навыки) делает игрок, авто-выдача больше не подставляет `choice`-записи молча (71, 73).
- Комплекты переехали в сеттинг: `modules/fallout/data/equipmentKits/*.json` (отдельный файл на фракцию) + индекс; `data/` остаётся пустой (74, 75).
- Комплект может требовать трейт: поле `requiresTraitIds` — комплект доступен только выбранной семье/подчерту. Движковый `domain/equipmentKits.js` фильтрует комплекты одинаково для всех ориджинов (77).
- Строка «Комплект снаряжения» неактивна, пока нет доступных комплектов (например, у семьи не выбрана семья) — пустая модалка больше не открывается (78, 79).
- Модалка комплекта: одежда и броня человека вынесены в отдельную категорию «Броня и одежда»; «Стандартная конструкция» осталась только для роботских частей (88).

### Ориджин «Осколок Анклава» (контент)
- Новый ориджин `enclaveRemnant` и трейт «Скрытный и преследуемый»: отметьте один доп. навык — Скрытность или Выживание (механикой `skillPickChoice`) (84).
- Два комплекта: «Бывший учёный» (лабораторный халат, противогаз, лазерный пистолет + 6+3 {CD} энергоячеек) и «Бывший солдат» (военная униформа, нагрудник боевой брони, выбор лазерной винтовки или штурмовой винтовки с боезапасом 8+4 {CD}) (85).
- Новый мод оружия `mod_204` «Длинный ствол» (для лазерного/плазменного/институтского оружия): +1 Дистанция, убирает «Вплотную», префикс «Длинноствольный» (88).
- Ложа меняет пистолет на винтовку: если на оружии есть мод из слота Stocks, имя берётся из `stockNames.with` («Лазерный пистолет» → «Лазерная винтовка»); префикс самой ложи при этом не дублируется. Итог для солдата — «Длинноствольная Лазерная винтовка» (88).
- В модалке трейта убран заголовок-имя ориджина — остаётся имя самой черты (например, «Скрытный и преследуемый») (87).
- Описания трейтов поддерживают простую разметку: `**жирный**`, `*курсив*`, маркеры `- ` и переносы строк — через общий `renderTextWithIcons` (86).

### Экран «Снаряжение»
- Тумблеры вида оружия на самом экране: линии = список (спойлеры), две черты = карточки. Убрано из настроек (76).
- Виртуальная рукопашная атака (кулаки/манипулятор) всегда в первом слоте. Кнопка-ладонь слева в тулбаре показывает/скрывает её карточку; когда скрыта — первый слот занимает оружие. Настройка сохраняется между сессиями (80, 81, 82).
- Порядок атак: Unarmed (рукопашная) → Melee (рукопашное оружие) → встроенное → Mk II → остальное (82).

### Надёжность и чистка
- ФИКС: `getWeaponModById is not defined` при выдаче комплекта с модами оружия (касалось всех комплектов с модами, не только Анклава). В сторе теперь синхронный `catalogGetWeaponModById` (89).
- В каталог (`buildWeaponRow`) проброшено поле `stockNames` — без него имя «…винтовка» не доходило до резолвера (88).
- Удалены разовые отладочные сценарии в корне репозитория (`debug-android-save.js`, `enhanced-debug.js`, `test-android-*.js`, `test-file-transfer.js`).

### Ещё
- 69 — комплекты Омерты и Общества «Белая перчатка» (полные составы, авто-переброс, выбор в модалке).
- 70 — первый этап разделения «движок/сеттинг»: еда, напитки и моды оружия целиком переехали в модуль.

---

## 68 — Качество «Элегантная» в старых сейвах (миграция v7→v8) + фикс потери полей в модалке комплекта

- ПРИЧИНА: одежда, выданная комплектом Председателей ДО патча 67, лежит в сейве без `uniqQualities` — инвентарь и экран «Снаряжение» показывают её как «Формальная одежда» (качество есть только у новых выдач).
- Миграция v7→v8: если персонаж — Председатель (trait id/ids содержит treefamilies-chairmen), формальная одежда и шляпа без качества получают `uniqQualities: ['elegant']`, имя («Элегантная Формальная одежда/шляпа») и пересобранные id/stackKey (`clothing_fancy_clothes_uniq_elegant` — склеятся с новыми выдачами). Уже с качеством — не дублируется; персонажи других семей — не трогаются.
- ФИКС: ручная модалка комплекта (EquipmentKitModal → toInventoryItems, ветка оружия) теряла `baseName` и `uniqQualities` — бритва/«Дерзкая …» при выборе комплекта из модалки остались бы без имени и стека. Поля переносятся явно.
- Тесты: полный путь выдачи (resolveKitItems → addNewItem: одежда с elegant в сторе), миграция v7→v8 (3 кейса: Председатели, не Председатели, уже с качеством).

---

## 67 — Экран «Снаряжение» знает имя варианта; качество «Элегантная» у Председателей

- ФИКС: экран «Снаряжение» (WeaponsAndArmorScreen) не знал о замене имени — для него бритва была «Складным ножом»: `storeItemToWeaponDisplay` подставляет `id = weaponId` (истинный id ножа), `findLocalizedWeapon` находил в каталоге нож и затирал имя. Теперь предметы с собственной идентичностью (вариант `baseName` или уникальные качества `uniqQualities`) показываются под своим именем из стора: «Опасная бритва», «Дерзкая …»; имя с модами собирается от заменённого («Зазубренное лезвие Опасная бритва»). Обычное оружие — как раньше (каталог, i18n).
- КОНТЕНТ: качество `elegant` («Элегантная» / "Elegant") в каталоге уникальных качеств модуля; комплект Председателей выдаёт формальную одежду и шляпу с этим качеством — «Элегантная Формальная одежда», «Элегантная Формальная шляпа». Качество пока ничего не делает — только имя (и стек: элегантная ≠ без качества).
- Тесты: рендер экрана «Снаряжение» с экипированной бритвой (имя «Опасная бритва», не «Складной нож»), комплект Председателей (одежда/шляпа с elegant в имени).

---

## 66 — Фикс синтаксических ошибок сборки (Metro/Babel)

- `domain/traits.js`: коллизия имён после рефакторинга реестра (патч 62) — локальная `getTraitI18n(id)` (имя/описание трейта по id) конфликтовала с импортом `getTraitI18n(locale)` из реестра (словарь локали). Локальная переименована в `getTraitI18nById`; обновлены модалки (GenericTraitModal, SurvivorModal, TribalModal, TreeFamiliesModal). Ошибку ловил только Metro при сборке (`Identifier has already been declared`), vitest её терпел.
- `WeaponModificationModal.js`: смешение `||` и `??` без скобок в `baseWeaponName` (патч 64) — синтаксическая ошибка Babel (`Nullish coalescing operator requires parens`). Обёрнуто в скобки.
- ПРОЦЕСС: теперь перед выпуском патча обязательна проверка `CI=1 npx expo export --platform web` (Metro ловит то, что vitest пропускает).

---

## 65 — Уникальные качества (uniq qualities): навешиваемые модификаторы экипировки

- МОДЕЛЬ (владелец): «свойства» экипировки называются **уникальными качествами** (uniq quality, в духе uniq_armor_mods). Качество — навешиваемый модификатор: у него id (учёт/механика) и имя (i18n), имя добавляется к имени предмета: «Дерзкая» + «Формальная шляпа» = «Дерзкая Формальная шляпа». Эффекты качества (как у модов) — зарезервированы на будущее.
- Качество НЕ вшито в предмет: крепится к экземпляру в инвентаре (поле `uniqQualities`, как `appliedMods` — список id). Крепление: данными (запись комплекта: `"uniqQualities": ["dashing"]`), в будущем — крафтом или вручную. Механизм общий для любой экипировки: одежда, шляпы, броня, спецодежда, оружие, патроны.
- Стек: качество — параметр стека (закон: два 100% идентичных предмета — один стек). Формула: id + прочность + моды + качества + имя — `clothing_fancy_clothes_uniq_dashing` ≠ `..._uniq_elegant` при одном базовом id («элегантная» и «дерзкая» одежда — разные стеки). Ключ по id качества (не по имени) — не зависит от локали; порядок качеств не влияет.
- Каталог: `data/equipment/uniq_qualities.json` (база, пустой) + `modules/fallout/data/uniq_qualities.json` (модуль — сюда владелец добавляет определения) + имена в i18n модуля (`uniqQualities`). Реестр: getUniqQualities/getUniqQualityName.
- Композиция имени: domain/uniqQuality.js — чистая функция (готова для крафта/ручного крепления).
- Тесты: uniq-quality.test.js (10) — каталог, композиция имени, стек-ключи, addNewItem, крепление из комплекта (шляпа и оружие).

---

## 64 — Варианты предметов (trueItemId + modifiers): «Опасная бритва» — не самостоятельный предмет

- МОДЕЛЬ (владелец): бритва — НЕ клон и НЕ отдельное оружие. Это **вариант** выкидного ножа: запись читается как модификаторы —
  `{ "id": "weapon_straight_razor", "trueItemId": "weapon_switchblade", "modifiers": { "replaceOriginalNameTo": "weapon_straight_razor" } }`
  — «истинный id предмета» (механика: статы, качество, моды — всё от ножа) и «перезаписать оригинальное имя» (имя из i18n-ключа: «Опасная бритва» / Straight Razor; в данных имя не хардкодится — только i18n).
- Будущее: `modifiers` поддерживает точечные правки статов — `{ "fireRateModifier": { "op": "+", "value": 1 } }` (поля damage/fireRate/weight/cost/rarity, операции + − × ÷). Быстрый револьвер = револьвер + имя + одна правка, без копирования стат-блока.
- Движок: `expandTrueItems` в domain/packMerge.js разворачивает запись в полную (deep merge с истинным предметом + модификаторы); имя варианта — из i18n каталога. `domain/itemIdentity.js` — единая логика id/стек-ключа.
- Инвентарь: предмет живёт под ИСТИННЫМ id (для программы это нож — моды ножа доступны без алиасов, обвязка catalogSource не нужна). Стек-ключ — простая склейка: id + прочность + моды + имя, без маркеров (`weapon_switchblade_dur_50_mods_mod_113_опасная_бритва`). ЗАКОН стека: два 100% идентичных предмета — один стек; любой отличающийся параметр (прочность, моды, имя) разделяет: бритва и нож — разные стеки, две бритвы — один, меч 50 и меч 100 прочности — разные.
- Случайная прочность (randomWeaponQualityEnabled/rollWeaponDurability): убрано принудительное «никогда не стакается» (уникальный ключ на каждый экземпляр) — теперь два экземпляра с одинаковой прочностью склеиваются (купленные — все 100, один стек).
- Имя с модом собирается от перезаписанного: «Зазубренное лезвие Опасная бритва» (механически нож).
- Миграция v6→v7: старые сейвы с weapon_straight_razor (патч 63) → истинный id + baseName (имя очищается от префиксов модов), id/stackKey пересобираются.
- Тесты: item-variant.test.js (13) — запись, разворачивание (ru/en), точечные модификаторы, выдача под истинным id, стек-ключи, addNewItem, миграция.

---

## 63 — Комплекты семей (Три семьи) + механика «комплект от трейта»

- Комплект семьи Председателей (treefamilies_chairmen): формальная одежда и шляпа (существующие clothing_fancy_clothes/headwear_fancy_hat), 9-мм пистолет с 12+4 CD патронов, Опасная бритва (клон выкидного ножа, id weapon_straight_razor, характеристики ножа), фишки казино «Топс» (CasinoTopsChip, value 100), личная безделушка (бросок по таблице oddity), крапленая колода карт (item_marked_cards, 5 крышек).
- Комплекты Омерты и Белой перчатки — временные заглушки (100 крышек), составы добавим по вашим данным.
- ПРАВИЛО (владелец): комплект зависит от выбранной семьи — трейт несёт equipmentKitId, при выборе семьи комплект выбирается автоматически (applyKitById → resolveKitItems → handleSelectKit).
- Каталог (equipmentCatalog) подмешивает модульные данные: weapons/generalGoods/equipmentKits из modules/fallout + i18n модуля. Новые предметы: Опасная бритва, фишки, колода.
- Тесты: комплект Председателей (резолв: одежда, 9мм+патроны, бритва, фишки, колода, безделушка), каталог с новыми предметами, equipmentKitId у семей.

---

## 62 — Ориджин «Три семьи» (первый контент в модуле) + рефакторинг реестра

- Ориджин «Три семьи» (TreeFamilies / Strip Families): мульти-трейт — персонаж выбирает ОДНУ из трёх семей Стрипа. Черты:
  - Председатели: раз за сцену перебросить 1d20 при проверке навыка на основе Харизмы (эффект-метка chairmen_reroll_cha);
  - Омерта: препараты, применяемые к другим, получают/увеличивают «Зависимость» (эффект-метка omerta_addiction_boost);
  - Общество «Белая перчатка»: еда с Пункта приготовления +1 ОЗ, разделка трупа — +1 мясо, диапазон осложнений ХАР +1 с чужими (эффекты-метки white_glove_*).
  Механики крафта/лута — на будущее (метки готовы).
- ПРАВИЛО (владелец): новый контент пишется в МОДУЛЬ modules/fallout/ (origins.json, traits.json, i18n/), а не в data/.
- Рефакторинг реестра: getOrigins/getTraits подмешивают модуль поверх базы (merge по id, приоритет модуля); добавлены getOriginI18n/getTraitI18n (база + модуль, deep merge). origins.js/traits.js читают i18n через реестр.
- Ассет: assets/origins/3families.png (копия survivor, по указанию владельца).
- Модалка TreeFamiliesModal — выбор одной семьи из трёх.
- Тесты: treefamilies-origin.test.js (8).

---

## 61 — Реестр данных движка (шаг 1 к модулям)

- Добавлен `domain/registry.js` — единая точка чтения данных сеттинга (origins, traits, bodyplans, каталог). Пока возвращает те же файлы `data/`, поведение не меняется.
- Домен-модули (origins/traits/characterCreation/bodyplan) переключены на реестр — больше не импортируют JSON напрямую.
- Это фундамент для подключения модулей (`.posm`): когда появится модуль, реестр подмешает его данные (deepMerge по id), не требуя правок в потребителях.
- Тесты: registry.test.js (реестр отдаёт данные, каталог для локали).

---

## 60 — Фикс дубля оружия при экипировке роботу

- Исправлен баг: при добавлении 1 оружия и экипировке роботу в руку предмет дублировался (оставался в инвентаре + копия в руке).
- Причина: разные форматы stackKey — в сторе `weapon_10mm_pistol`, в UI `weapon:weapon_10mm_pistol:mods:none`; поиск по UI-ключу не находил предмет в сторе, и он не удалялся.
- Фикс: `findUnequippedStoreItemByStackKey` теперь нормализует UI-ключ (извлекает каталожный id) и находит предмет в неэкипированных.

---

## 59 — Модалка Дикаря: кнопка «Отмена»

- На первом экране модалки выбора черт Дикаря (выбор режима: «2 черты» / «1 черта + 1 перк») добавлена кнопка «Отмена» — закрывает модалку без изменений, если пользователь случайно её открыл.
- Ранее: после открытия модалки нельзя было выйти без выбора (кнопки не было). Теперь поведение как в остальных модалках.

---

## 58 — Ориджин Дикарь (Tribal) + фундамент пакетов правил

- Ориджин «Дикарь» (Tribal): мультивыбор — 2 черты (Дикарь или Выживший) ИЛИ 1 черта (Дикарь/Выживший/НКР) + 1 дополнительный перк. Черты: Мать Пустошь (описание), Кочевник (запрет на отметку навыка «Наука» — механика bannedTagSkills), Обряд Посвящения (механика: при трате очка удачи бросок 1 {/CD}, эффект — очко не тратится + алерт «Духи к вам благосклонны»), Инструменты Старого Света (описание), Избранный (описание). Комплект временно — стандартный (100 крышек).
- Фундамент пакетов правил (этап 0, не подключён к поведению): domain/packMerge.js (deepMerge, applyOverridesById, findUnknownOverrideIds), src/store/packStore.js (загрузка/хранение пакета, persist).

---

## 48 — Ориджин Секьюритрон

- Ориджин «Секьюритрон» наполнен: трейт «Секьюритрон Mk I» (иммунитет к яду и радиации, переносимый вес 150 фунтов без учёта Силы и перков, запрет расходников/еды/питья/отдыха, восстановление только ремонтом).
- Свой план тела `securitron`: голова / корпус / левая рука / правая рука / колесо, строение 1-3-1, таблица попаданий 1-2 / 3-11 / 12-14 / 15-17 / 18-20.
- Новые робо-части: голова, корпус (вес 150), рука-манипулятор со встроенной безоружной атакой, колесо — со статами по аналогии с протектроном и переводами ru/en.
- Комплект `securitron_standard`: автоматический лазерный пистолет (лазерный пистолет с автоматическим стволом, встроен в левую руку, 14 + 7 CD энергоячеек), пистолет-пулемёт (встроен в правую руку, 8 + 4 CD патронов .45), ракетная установка и гранатомёт (нерабочие, до ОС Mk II — инертные предметы инвентаря с флагом `requiresMkII`), заводская броня корпуса/головы/рук, принтер (новый робо-предмет, экипирован по умолчанию, снять нельзя).
- RU-префикс мода `mod_053` («Автоматический ствол») приведён к EN-конвенции: префикс «Автоматический» — встроенный пистолет теперь называется «Автоматический лазерный пистолет».
- Поддержка слота `wheel` в логике роботов (установка конечностей, распределение брони, пикер брони, подписи слотов) и отображение встроенного оружия конечности рядом с оружием в руке.
- Ладонные орудия (автоматический лазерный пистолет и пистолет-пулемёт) теперь встроены в слоты рук (левая/правая) с применёнными модами, снять их нельзя; в списке атак карточки подписаны рукой.
- Гранатомёт комплекта — «Гранатомёт M79».
- Новый робо-предмет «Драйвер ОС Mk II» (уникальный, применим только к Секьюритрону): кнопка «Применить» в инвентаре потребляет драйвер и активирует нерабочие оружия. До установки ракетница и гранатомёт показываются в списке атак как неактивные карточки с пометкой «Требуется ОС Mk II» (и в инвентаре).
- Флаг установки ОС Mk II хранится в robot-срезе стора и переживает сохранение/загрузку.
- Ладони Секьюритрона свободны: встроенное оружие (лазерный пистолет в левой, пистолет-пулемёт в правой) находится ВНУТРИ руки (встроенное оружие конечности), а манипуляторы-ладони могут держать любое другое оружие (пистолет, винтовку, предметы) — обычное оружие экипируется в руку через инвентарь, карточка подписывается рукой.
- Порядок атак на экране снаряжения: рукопашные первыми (все ориджины), затем встроенное оружие, затем нерабочее Mk II, затем экипированное из инвентаря.
- Ячейки головы и колеса Секьюритрона — размером 1/3 ширины (как ячейка ряда из трёх), по центру строки.
- Оружие, экипируемое из инвентаря, встаёт в первую СВОБОДНУЮ руку (вторая экипировка — в другую руку), а не всегда в первую по расположению.
- Робо-оружие перенесено в слоты стора (единый источник): встроенное (манипулятор, лазер, ПП) и оружие в ладонях (экипированное из инвентаря) хранятся в слот-состоянии робота, а не в React-контексте. Экраны читают атаки из слотов — исчез баг, когда после сохранения/перезагрузки встроенное оружие пропадало из списка атак (оставались только Mk II).
- Исправлен краш экрана инвентаря: isRobot использовался в useMemo до своего объявления (TDZ «Cannot access 'isRobot' before initialization»). Добавлены рендер-смок тесты экранов InventoryScreen/WeaponsAndArmorScreen — такой класс ошибок теперь ловится тестами.
- Правило смены ориджина/трейта/комплекта: до распределения атрибутов/навыков (locked=false) смена свободна, без предупреждений; комплект сбрасывается вместе с ориджином. После locked смена ориджина/трейта — полный сброс с подтверждением; смена комплекта — сброс инвентаря, навыков и наград (атрибуты сохраняются). Просмотр списков всегда свободен (блок «Черта уже выбрана» убран).
- Настройка интерфейса «Отображение карточек оружия» (раздел «Настройки интерфейса» в настройках): Карточки (по умолчанию) / Спойлеры / Табы. Спойлеры: каждая карточка в сворачиваемом заголовке, ширина 2/3, по умолчанию закрыты. Табы: табы со скошенным левым верхним углом под конечностями, активный таб показывает карточку 2/3 ширины, стрелки << >> меняют активный таб, свайп влево/вправо по всей сцене табов (строка + карточка) тоже, тап по табу тоже.
- Исправлен баг: выбранный комплект снаряжения не сохранялся между сессиями (после перезагрузки комплект не отображался, клик «предлагал сбросить»). Причина: слияние снапшота отдавало предпочтение предметам стора и теряло метаданные комплекта (id/name). Теперь метаданные берутся из снапшота, предметы — из стора (купленные/лут не теряются); confirm сброса — только когда комплект реально выбран (есть id).
- Системный фикс сохранения (класс багов «потерянные id»): снапшот больше не затирает метаданные выбранного комплекта и встроенное оружие. Метаданные комплекта (id/name) берутся из снапшота, предметы — из стора (купленные/лут не теряются); встроенное оружие (кулаки у людей) объединяется по id со стором без дублей. origin/trait уже хранятся по id (serializeState) — этот класс багов закрыт на корню.
- Миграция сохранений v4→v5: старым сейвам, где комплект потерял id/name (объект {items} без метаданных), ставится заглушка «Комплект снаряжения» — предметы на месте, а какой комплект был выбран, неизвестно. По нажатию на заглушку открывается список комплектов; выбор конкретного предложит сброс (персонаж зафиксирован).
- Флаг «ОС Mk II установлена» теперь сохраняется в снапшот персонажа и восстанавливается при загрузке (раньше после перезагрузки Mk II оружие снова становилось нерабочим).

## 47 — Поддержка множественных типов урона

- Добавлена поддержка комбинированного типа урона для оружия (например, энергетический + физический).
- Плазменное оружие теперь наносит урон обоих типов: энергетический и физический.
- Модификации оружия могут изменять тип урона (замена или добавление).
- Добавлена миграция сохранений (v3 → v4) для преобразования строковых типов урона в массивы.
- Исправлена ошибка синтаксиса в модальном окне модификации оружия.
- Исправлен перевод названий дистанций в списке оружия (ранее отображались английские названия).

---

## 46 — Каталоги персонажей

- Добавлены каталоги для организации сохранённых персонажей.
- Добавлены создание, открытие, удаление каталогов и отображение количества персонажей в каждом каталоге.
- Персонажей можно перемещать между каталогами перетаскиванием.
- В открытом каталоге появилась drop-зона «Вернуть в список персонажей» для возврата персонажа в корневой каталог.
- Исправлен перенос персонажа из папки в корневой список при отпускании над зоной «назад».
- Настройка «Каталоги персонажей» сохраняется между запусками.
- При выключенной настройке кнопка «Создать каталог» полностью скрывается, а персонажи занимают освободившееся место.
- Существующие каталоги и распределение персонажей не удаляются при выключении настройки.
- Добавлена поддержка русского и английского языков для интерфейса каталогов.

---

## 45 — Миграция сохранений при обновлении

- Добавлена единая версионированная система миграции сохранений персонажей.
- Изменения формата сохранений оформляются последовательными миграциями между версиями схемы.
- Миграции применяются как при загрузке персонажа из SQLite, так и при восстановлении состояния из AsyncStorage.
- Добавлен единый источник текущей версии схемы и документация по добавлению новых миграций.
- Старые сохранения не требуют ручной конвертации после обновления приложения.

---

## 44 — Конфликт рукояти и ложа

- Рукоять и ложе оружия теперь являются взаимоисключающими модификациями.
- Установка рукояти автоматически снимает ложе, а установка ложа снимает рукоять.
- Исправлено русское название слота: «Приклад» заменён на «Ложе».

---

## 42 — Исправление старых сохранений

- Исправлены старые сохранения, в которых оружие могло одновременно иметь взаимоисключающие качества.
- После загрузки сохранения остаётся только одно качество из каждой конфликтующей пары.

---

## 41 — Взаимоисключающие качества оружия

- Исправлено применение взаимоисключающих качеств оружия.
- «Точное» заменяет «Неточное», и наоборот.
- «Надёжное» заменяет «Ненадёжное», и наоборот.

---

## 40 — Награды за отмеченные навыки

- Исправлено повторное начисление наград за отмеченные навыки.
- Повторное сохранение тех же навыков больше не добавляет предметы повторно.
- Если персонаж получает новый отмеченный навык позже, награда будет выдана только за новый навык.

---

## 39 — Тип боеприпаса в инвентаре

- В инвентаре у оружия отображается подходящий тип боеприпаса.
- Например: `Патрон: 10-мм` или `Патрон: Энергоячейка`.

---

## 38 — Настраиваемая прочность оружия

- Добавлено окно настроек.
- Добавлена опциональная механика случайной прочности оружия.
- При включённой опции оружие, полученное как добыча, имеет случайную прочность от 1 до 100.
- Купленное оружие всегда получает 100% прочности.
- Добавлена настройка расхода прочности за 10 выстрелов.
- Ненадёжное оружие изнашивается вдвое быстрее, а надёжное — вдвое медленнее.
- На экране экипировки отображается прочность оружия.
- Оружие с нулевой прочностью нельзя использовать до ремонта.
- Оружие можно бесплатно починить через инвентарь.

---

## 37 — Английская локализация оружия и случайной добычи

- Исправлены русские фрагменты, случайно попавшие в английские названия, описания и дальности оружия.
- Названия предметов случайной добычи теперь корректно переключаются вместе с языком приложения.
- В английской локали предмет «Косметичка» отображается как `Cosmetics Bag`.

---

## 36 — Английская локализация перков

- Исправлена английская локализация всех 94 перков.
- Названия и описания перков теперь отображаются на английском языке при выборе английской локали.
