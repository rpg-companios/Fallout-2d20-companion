diff --git a/__tests__/robot/assaultron-kits.test.js b/__tests__/robot/assaultron-kits.test.js
new file mode 100644
index 0000000..b6a1124
--- /dev/null
+++ b/__tests__/robot/assaultron-kits.test.js
@@ -0,0 +1,160 @@
+// __tests__/robot/assaultron-kits.test.js
+// Комплекты снаряжения Штурмотрона (Wanderer's Guide):
+//  - комплект существует и привязан к ориджину;
+//  - комплект выдаёт голову с лазером — у головы есть встроенное оружие,
+//    и оно попадает в карточки оружия со статами;
+//  - строительные когти — оружие: становятся конечностью со встроенным
+//    оружием, у которого есть статы (урон/тип/навык) для экрана экипировки.
+import { describe, it, expect } from 'vitest';
+
+import kits from '../../modules/fallout/data/equipmentKits/index.js';
+import origins from '../../modules/fallout/data/origins/origins.json';
+import heads from '../../modules/fallout/data/equipment/robot/robotheads.json';
+import bodies from '../../modules/fallout/data/equipment/robot/robotbody.json';
+import arms from '../../modules/fallout/data/equipment/robot/robotarms.json';
+import legs from '../../modules/fallout/data/equipment/robot/robotlegs.json';
+import robotWeapons from '../../modules/fallout/data/equipment/robot/weapons.json';
+import ruKits from '../../modules/fallout/i18n/ru-RU/data/system/equipmentKits.json';
+import enKits from '../../modules/fallout/i18n/en-EN/data/system/equipmentKits.json';
+import { initRobotSlots, getBuiltinWeaponsFromSlots } from '../../domain/robotEquip';
+
+const ASSAULTRON_KIT_IDS = [
+  'assaultron_us_military',
+  'assaultron_devil',
+  'assaultron_caravan_guard',
+];
+
+const robotCatalog = { heads, bodies, arms, legs, weapons: robotWeapons };
+
+const byId = (list, id) => list.find((entry) => entry.id === id);
+
+describe('комплекты снаряжения Штурмотрона', () => {
+  it('ориджин assaultron ссылается на три комплекта, и все они существуют', () => {
+    const origin = origins.find((o) => o.id === 'assaultron');
+    expect(origin.equipmentKitIds).toEqual(ASSAULTRON_KIT_IDS);
+    for (const kitId of ASSAULTRON_KIT_IDS) {
+      expect(kits[kitId], `нет комплекта ${kitId}`).toBeTruthy();
+      expect(Array.isArray(kits[kitId].items)).toBe(true);
+      expect(ruKits[kitId]?.name, `нет ru-имени ${kitId}`).toBeTruthy();
+      expect(enKits[kitId]?.name, `нет en-имени ${kitId}`).toBeTruthy();
+    }
+  });
+
+  it('каждый комплект выдаёт голову с лазером', () => {
+    for (const kitId of ASSAULTRON_KIT_IDS) {
+      const hasLaserHead = kits[kitId].items.some(
+        (item) => item.itemId === 'robot_head_assaultron_laser' && item.itemType === 'robotHead',
+      );
+      expect(hasLaserHead, `${kitId} не выдаёт голову с лазером`).toBe(true);
+    }
+  });
+
+  it('у головы с лазером есть встроенное оружие (лазер головы)', () => {
+    const head = byId(heads, 'robot_head_assaultron_laser');
+    expect(head.builtinWeaponId).toBe('robot_weapon_assaultron_head_laser');
+    const laser = byId(robotWeapons, 'robot_weapon_assaultron_head_laser');
+    expect(laser.damage).toBeGreaterThan(0);
+    expect(laser.damageType).toBe('energy');
+  });
+
+  it('initRobotSlots: голова из комплекта даёт карточку лазера со статами', () => {
+    const head = byId(heads, 'robot_head_assaultron_laser');
+    const { slots } = initRobotSlots('assaultron', [{ ...head }], robotCatalog);
+    const weapons = getBuiltinWeaponsFromSlots(slots);
+    const laser = weapons.find((w) => w.id === 'robot_weapon_assaultron_head_laser');
+    expect(laser, 'лазер головы не попал в оружие').toBeTruthy();
+    expect(laser.damage).toBe(5);
+    expect(laser.damageType).toBe('energy');
+    expect(laser.mainSkill).toBe('ENERGY_WEAPONS');
+    expect(laser.isBuiltin).toBe(true);
+  });
+
+  it('initRobotSlots: строительные когти — оружие со статами в слотах рук', () => {
+    const clawWeapon = byId(robotWeapons, 'robot_weapon_construction_claw');
+    const kitItems = [
+      { ...clawWeapon, itemType: 'weapon', weaponId: clawWeapon.id, slot: 'left' },
+      { ...clawWeapon, itemType: 'weapon', weaponId: clawWeapon.id, slot: 'right' },
+    ];
+    const { slots } = initRobotSlots('assaultron', kitItems, robotCatalog);
+
+    expect(slots.leftArm.limb?.id).toBe('robot_weapon_construction_claw');
+    expect(slots.rightArm.limb?.id).toBe('robot_weapon_construction_claw');
+
+    const weapons = getBuiltinWeaponsFromSlots(slots);
+    const claw = weapons.find((w) => w.id === 'robot_weapon_construction_claw');
+    expect(claw, 'коготь не попал в оружие').toBeTruthy();
+    expect(claw.damage).toBe(4);
+    expect(claw.damageType).toBe('physical');
+    expect(claw.mainSkill).toBe('UNARMED');
+    expect(claw.isBuiltin).toBe(true);
+  });
+
+  it('initRobotSlots: рамы и обшивка — 1 предмет = 1 слот (включая ноги)', () => {
+    const kitItems = [
+      {
+        id: 'robot_frame_actuated_body',
+        itemType: 'robotFrame',
+        layer: 'frame',
+        robotLocation: 'Main Body',
+        damageResistance: { physical: 1, energy: 1 },
+      },
+      {
+        id: 'robot_plating_standard_thruster',
+        itemType: 'plating',
+        layer: 'plating',
+        robotLocation: 'Thruster',
+        damageResistance: { physical: 2, energy: 0 },
+      },
+      {
+        id: 'robot_plating_standard_thruster_2',
+        itemType: 'plating',
+        layer: 'plating',
+        robotLocation: 'Thruster',
+        damageResistance: { physical: 2, energy: 0 },
+      },
+    ];
+    const { slots, inventoryItems } = initRobotSlots('assaultron', kitItems, robotCatalog);
+    expect(slots.body.frame?.id).toBe('robot_frame_actuated_body');
+    // Локация Thruster у штурмотрона — ноги, 1 предмет = 1 слот
+    // Первый thruster → leftLeg, второй → rightLeg (первый свободный)
+    expect(slots.leftLeg.plating?.id).toBe('robot_plating_standard_thruster');
+    expect(slots.rightLeg.plating?.id).toBe('robot_plating_standard_thruster_2');
+    expect(inventoryItems).toHaveLength(0);
+  });
+
+  it('initRobotSlots: Arms — 1 предмет = 1 слот руки', () => {
+    const kitItems = [
+      {
+        id: 'robot_frame_actuated_arms',
+        itemType: 'robotFrame',
+        layer: 'frame',
+        robotLocation: 'Arms',
+        damageResistance: { physical: 1, energy: 1 },
+      },
+    ];
+    const { slots } = initRobotSlots('assaultron', kitItems, robotCatalog);
+    // Один предмет Arms должен занять только leftArm, а не обе руки
+    expect(slots.leftArm.frame?.id).toBe('robot_frame_actuated_arms');
+    expect(slots.rightArm.frame).toBeFalsy();
+  });
+
+  it('initRobotSlots: defaultPlating из bodyPlan — стандартная обшивка на основании данных', () => {
+    // Пустой комплект — должны подтянуться дефолты из bodyPlan.defaultPlating
+    const { slots } = initRobotSlots('assaultron', [], {
+      ...robotCatalog,
+      plating: [
+        { id: 'robot_plating_standard_optics', layer: 'plating', robotLocation: 'Optics', damageResistance: { physical: 2, energy: 0 } },
+        { id: 'robot_plating_standard_body', layer: 'plating', robotLocation: 'Main Body', damageResistance: { physical: 2, energy: 0 } },
+        { id: 'robot_plating_standard_arms', layer: 'plating', robotLocation: 'Arms', damageResistance: { physical: 2, energy: 0 } },
+        { id: 'robot_plating_standard_thruster', layer: 'plating', robotLocation: 'Thruster', damageResistance: { physical: 2, energy: 0 } },
+      ],
+      frames: [],
+    });
+    expect(slots.head.plating?.id).toBe('robot_plating_standard_optics');
+    expect(slots.body.plating?.id).toBe('robot_plating_standard_body');
+    expect(slots.leftArm.plating?.id).toBe('robot_plating_standard_arms');
+    expect(slots.rightArm.plating?.id).toBe('robot_plating_standard_arms');
+    expect(slots.leftLeg.plating?.id).toBe('robot_plating_standard_thruster');
+    expect(slots.rightLeg.plating?.id).toBe('robot_plating_standard_thruster');
+  });
+});
diff --git a/domain/bodyplan.js b/domain/bodyplan.js
index d74d610..0f65933 100644
--- a/domain/bodyplan.js
+++ b/domain/bodyplan.js
@@ -62,6 +62,10 @@ export function getDefaultLimbs(planId) {
   return { ...(bodyPlansRegistry[planId]?.defaults || {}) };
 }
 
+export function getDefaultPlating(planId) {
+  return { ...(bodyPlansRegistry[planId]?.defaultPlating || {}) };
+}
+
 export function getBodyPlan(planId) {
   return bodyPlansRegistry[planId] || null;
 }
diff --git a/domain/robotEquip.js b/domain/robotEquip.js
index 6da5c50..3b4de2b 100644
--- a/domain/robotEquip.js
+++ b/domain/robotEquip.js
@@ -1,4 +1,4 @@
-import { getBodyPlan, createSlotsFromBodyPlan, getDefaultLimbs } from './bodyplan';
+import { getBodyPlan, createSlotsFromBodyPlan, getDefaultLimbs, getDefaultPlating } from './bodyplan';
 import { applyWeaponMods as applyWeaponModsPipeline } from './enrichItem';
 
 // domain/robotEquip.js
@@ -336,26 +336,49 @@ export function initRobotSlots(bodyPlan, resolvedKitItems = [], robotCatalog = {
       continue;
     }
 
-    // Броня
-    if (['plating', 'armor', 'frame', 'robotArmor'].includes(itype)) {
+    // Броня — 1 предмет = 1 слот (без фолбэков, строго по данным)
+    // Рама/Обшивка/Броня применяются к конкретной конечности, не к обеим сразу.
+    // Thruster — обобщённая локация «средство передвижения»: у Мистера Помощника это thruster,
+    // у протектрона/штурмотрона — ноги, у секьюритрона — колесо. Это не фолбэк, а маппинг типов шасси.
+    if (['plating', 'armor', 'frame', 'robotArmor', 'robotFrame'].includes(itype)) {
       const armorData = item._armor ?? item;
       const location = armorData.robotLocation ?? item.robotLocation;
       const layer = armorData.layer ?? itype;
-      
-      // Простое распределение
-      for (const k of slotKeys) {
-        if (location === 'Main Body' && k === 'body') {
-          slots[k][layer] = armorData;
-        } else if (location === 'Optics' && k === 'head') {
-          slots[k][layer] = armorData;
-        } else if (location === 'Arms' && k.toLowerCase().includes('arm')) {
-          slots[k][layer] = armorData;
-        } else if ((location === 'Legs' || location === 'Wheel') && (k.toLowerCase().includes('leg') || k === 'chassis' || k === 'thruster' || k === 'wheel')) {
-          slots[k][layer] = armorData;
-        } else if (location === 'Thruster' && (k === 'thruster' || k === 'wheel')) {
-          slots[k][layer] = armorData;
+
+      // Определяем, какие слоты подходят для этой локации
+      const isMatchingSlot = (slotKey) => {
+        if (location === 'Main Body' && slotKey === 'body') return true;
+        if (location === 'Optics' && slotKey === 'head') return true;
+        if (location === 'Arms' && slotKey.toLowerCase().includes('arm')) return true;
+        if ((location === 'Legs' || location === 'Wheel') && (slotKey.toLowerCase().includes('leg') || slotKey === 'chassis' || slotKey === 'thruster' || slotKey === 'wheel')) return true;
+        if (location === 'Thruster' && (slotKey.toLowerCase().includes('leg') || slotKey === 'chassis' || slotKey === 'thruster' || slotKey === 'wheel')) return true;
+        // Точные совпадения для новых per-slot локаций (если появятся leftArm/rightArm и т.д.)
+        if (location === slotKey) return true;
+        if (location === 'Head' && slotKey === 'head') return true;
+        if (location === 'Left Arm' && slotKey === 'leftArm') return true;
+        if (location === 'Right Arm' && slotKey === 'rightArm') return true;
+        if (location === 'Left Leg' && slotKey === 'leftLeg') return true;
+        if (location === 'Right Leg' && slotKey === 'rightLeg') return true;
+        return false;
+      };
+
+      // 1 предмет = 1 слот: ищем первый подходящий слот, где слой свободен
+      // Если указан конкретный слот в item.slot — используем его
+      let targetSlot = null;
+      if (item.slot && slotKeys.includes(item.slot) && isMatchingSlot(item.slot)) {
+        targetSlot = item.slot;
+      } else {
+        // Ищем первый свободный слот из подходящих
+        targetSlot = slotKeys.find((k) => isMatchingSlot(k) && slots[k][layer] == null) || null;
+        // Если все подходящие слоты заняты — заменяем первый подходящий (для frame/plating, которые конфликтуют)
+        if (!targetSlot) {
+          targetSlot = slotKeys.find((k) => isMatchingSlot(k)) || null;
         }
       }
+
+      if (targetSlot && slots[targetSlot] !== undefined) {
+        slots[targetSlot][layer] = armorData;
+      }
       continue;
     }
 
@@ -371,7 +394,8 @@ export function initRobotSlots(bodyPlan, resolvedKitItems = [], robotCatalog = {
     }
   }
 
-  // Автозаполнение недостающих конечностей
+  // Автозаполнение недостающих конечностей — строго из данных bodyPlan.defaults, без фолбэков
+  // 1 предмет = 1 слот: каждый слот заполняется своей конечностью из defaults[slotKey]
   const { heads = [], bodies = [], arms = [], legs = [] } = robotCatalog;
   const planDefaults = getDefaultLimbs(bodyPlan);
   const defaultHead = heads.find((h) => h.id === planDefaults.head)
@@ -384,8 +408,34 @@ export function initRobotSlots(bodyPlan, resolvedKitItems = [], robotCatalog = {
   for (const k of slotKeys) {
     if (slots[k].limb !== null) continue;
 
+    // Проверяем конкретный слот в defaults (например, leftArm, rightLeg и т.д.)
+    const specificDefaultId = planDefaults[k];
+    if (specificDefaultId) {
+      // Ищем в соответствующем каталоге по ID
+      let specificLimb = null;
+      if (k === 'head') {
+        specificLimb = heads.find((h) => h.id === specificDefaultId);
+      } else if (k === 'body') {
+        specificLimb = bodies.find((b) => b.id === specificDefaultId);
+      } else if (k.toLowerCase().includes('arm')) {
+        specificLimb = arms.find((a) => a.id === specificDefaultId);
+      } else if (k.toLowerCase().includes('leg') || k === 'chassis' || k === 'thruster' || k === 'wheel') {
+        specificLimb = legs.find((l) => l.id === specificDefaultId);
+      }
+      if (specificLimb) {
+        if (k.toLowerCase().includes('arm')) {
+          const limbFromArm = buildLimbFromArmEntry(specificLimb);
+          const builtinWeapons = normalizeBuiltinWeapons(limbFromArm, weaponsCatalog);
+          slots[k].limb = builtinWeapons.length > 0 ? { ...limbFromArm, builtinWeapons } : limbFromArm;
+        } else {
+          const builtinWeapons = normalizeBuiltinWeapons(specificLimb, weaponsCatalog);
+          slots[k].limb = builtinWeapons.length > 0 ? { ...specificLimb, builtinWeapons } : specificLimb;
+        }
+        continue;
+      }
+    }
+
     if (k === 'head' && defaultHead) {
-      // Resolve builtinWeaponId for the default head (e.g. robobrain mesmetron)
       const builtinWeapons = normalizeBuiltinWeapons(defaultHead, weaponsCatalog);
       const headLimb = builtinWeapons.length > 0 ? { ...defaultHead, builtinWeapons } : defaultHead;
       slots[k].limb = headLimb;
@@ -396,26 +446,32 @@ export function initRobotSlots(bodyPlan, resolvedKitItems = [], robotCatalog = {
       defaultLeg
     ) {
       slots[k].limb = defaultLeg;
-    } else if (
-      // Руки: автозаполняем пустые arm-слоты из planDefaults[k] (leftArm/rightArm/arm1/arm2/arm3).
-      // Только если у слота есть дефолтный arm-id в bodyplan и он совместим с bodyPlan.
-      k.toLowerCase().includes('arm')
-    ) {
-      const defaultArmId = planDefaults[k];
-      if (defaultArmId) {
-        const armEntry = arms.find((a) => a.id === defaultArmId);
-        // Проверяем совместимость (если в каталоге указан compatibleBodyPlans)
-        if (armEntry) {
-          const compatible = armEntry.compatibleBodyPlans;
-          const defaultPlan = armEntry.defaultForBodyPlan;
-          const fits = compatible
-            ? compatible.includes(bodyPlan)
-            : (defaultPlan ? defaultPlan === bodyPlan : true);
-          if (fits) {
-            slots[k].limb = buildLimbFromArmEntry(armEntry);
-          }
-        }
-      }
+    }
+    // Для рук, если нет specificDefaultId, но есть default для bodyPlan — не автозаполняем,
+    // так как у разных роботов разное количество рук, и defaults должны быть явными
+  }
+
+  // Автозаполнение стандартной обшивки из bodyPlan.defaultPlating — на основании данных, без обогатителя
+  // defaultPlating: { slotKey: platingId } — например, head: standard_optics, body: standard_body, leftArm: standard_arms
+  // Ставится только если в слоте нет plating/frame/armor (конфликт слоёв)
+  const defaultPlatingMap = getDefaultPlating(bodyPlan);
+  const platingCatalog = Array.isArray(robotCatalog.plating) ? robotCatalog.plating : [];
+  const framesCatalog = Array.isArray(robotCatalog.frames) ? robotCatalog.frames : [];
+  const allArmorCatalog = [...platingCatalog, ...framesCatalog];
+
+  const resolvePlatingById = (id) => {
+    if (!id) return null;
+    return allArmorCatalog.find((p) => p.id === id) || null;
+  };
+
+  for (const k of slotKeys) {
+    if (slots[k].plating || slots[k].frame || slots[k].armor) continue;
+    const defaultPlatingId = defaultPlatingMap[k];
+    if (!defaultPlatingId) continue;
+    const platingData = resolvePlatingById(defaultPlatingId);
+    if (platingData) {
+      const layer = platingData.layer || 'plating';
+      slots[k][layer] = platingData;
     }
   }
 
diff --git a/modules/fallout/data/bodyplans/bodyplans.json b/modules/fallout/data/bodyplans/bodyplans.json
index db82267..e5ce4a8 100644
--- a/modules/fallout/data/bodyplans/bodyplans.json
+++ b/modules/fallout/data/bodyplans/bodyplans.json
@@ -40,6 +40,13 @@
       "rightArm": "robot_arm_robobrain",
       "chassis": "robot_legs_robobrain_treads"
     },
+    "defaultPlating": {
+      "head": "robot_plating_standard_optics",
+      "body": "robot_plating_standard_body",
+      "leftArm": "robot_plating_standard_arms",
+      "rightArm": "robot_plating_standard_arms",
+      "chassis": "robot_plating_standard_thruster"
+    },
     "slotCapabilities": {
       "leftArm": {
         "canEquipWeapon": true
@@ -69,6 +76,14 @@
       "arm3": "robot_arm_mister_handy",
       "thruster": "robot_legs_mister_handy_thruster"
     },
+    "defaultPlating": {
+      "head": "robot_plating_standard_optics",
+      "body": "robot_plating_standard_body",
+      "arm1": "robot_plating_standard_arms",
+      "arm2": "robot_plating_standard_arms",
+      "arm3": "robot_plating_standard_arms",
+      "thruster": "robot_plating_standard_thruster"
+    },
     "slotCapabilities": {
       "arm1": {
         "canEquipWeapon": true
@@ -98,6 +113,14 @@
       "body": "robot_body_protectron",
       "rightLeg": "robot_legs_protectron"
     },
+    "defaultPlating": {
+      "head": "robot_plating_standard_optics",
+      "body": "robot_plating_standard_body",
+      "leftArm": "robot_plating_standard_arms",
+      "rightArm": "robot_plating_standard_arms",
+      "leftLeg": "robot_plating_standard_thruster",
+      "rightLeg": "robot_plating_standard_thruster"
+    },
     "slotCapabilities": {
       "leftArm": {
         "canEquipWeapon": true
@@ -122,6 +145,13 @@
       "rightArm": "robot_arm_securitron",
       "wheel": "robot_legs_securitron"
     },
+    "defaultPlating": {
+      "head": "robot_plating_standard_optics",
+      "body": "robot_plating_standard_body",
+      "leftArm": "robot_plating_standard_arms",
+      "rightArm": "robot_plating_standard_arms",
+      "wheel": "robot_plating_standard_thruster"
+    },
     "slotCapabilities": {
       "leftArm": {
         "canEquipWeapon": true
@@ -168,6 +198,14 @@
       "body": "robot_body_assaultron",
       "rightLeg": "robot_legs_assaultron"
     },
+    "defaultPlating": {
+      "head": "robot_plating_standard_optics",
+      "body": "robot_plating_standard_body",
+      "leftArm": "robot_plating_standard_arms",
+      "rightArm": "robot_plating_standard_arms",
+      "leftLeg": "robot_plating_standard_thruster",
+      "rightLeg": "robot_plating_standard_thruster"
+    },
     "slotCapabilities": {
       "leftArm": {
         "canEquipWeapon": true
@@ -197,6 +235,14 @@
       "body": "robot_body_sentry_bot",
       "rightLeg": "robot_legs_sentry_bot"
     },
+    "defaultPlating": {
+      "head": "robot_plating_standard_optics",
+      "body": "robot_plating_standard_body",
+      "leftArm": "robot_plating_standard_arms",
+      "rightArm": "robot_plating_standard_arms",
+      "leftLeg": "robot_plating_standard_thruster",
+      "rightLeg": "robot_plating_standard_thruster"
+    },
     "slotCapabilities": {
       "leftArm": {
         "canEquipWeapon": true
diff --git a/modules/fallout/data/equipment/clothes.json b/modules/fallout/data/equipment/clothes.json
index 75e77cb..f453a54 100644
--- a/modules/fallout/data/equipment/clothes.json
+++ b/modules/fallout/data/equipment/clothes.json
@@ -570,6 +570,23 @@
           "clothingType": "headwear",
           "allowsArmor": false,
           "canRobotWear": true
+        },
+        {
+          "id": "headwear_skull_mask",
+          "protectedAreas": [
+            "Head"
+          ],
+          "physicalDamageRating": 0,
+          "energyDamageRating": 0,
+          "radiationDamageRating": 0,
+          "weight": 0.5,
+          "cost": 10,
+          "rarity": 1,
+          "specialEffects": [],
+          "itemType": "clothing",
+          "clothingType": "headwear",
+          "allowsArmor": false,
+          "canRobotWear": true
         }
       ]
     }
diff --git a/modules/fallout/data/equipment/robot/robotheads.json b/modules/fallout/data/equipment/robot/robotheads.json
index 9e68c7b..9c565d2 100644
--- a/modules/fallout/data/equipment/robot/robotheads.json
+++ b/modules/fallout/data/equipment/robot/robotheads.json
@@ -63,6 +63,7 @@
     "itemType": "robotHead",
     "replaceable": false,
     "defaultForBodyPlan": null,
+    "builtinWeaponId": "robot_weapon_assaultron_head_laser",
     "unique": false,
     "complexity": 7,
     "perksRequired": [
diff --git a/modules/fallout/data/equipmentKits/assaultron.json b/modules/fallout/data/equipmentKits/assaultron.json
index 0150917..6677a1e 100644
--- a/modules/fallout/data/equipmentKits/assaultron.json
+++ b/modules/fallout/data/equipmentKits/assaultron.json
@@ -1,9 +1,33 @@
 {
-  "assaultron_us_army": {
+  "assaultron_us_military": {
     "items": [
-      { "type": "fixed", "itemId": "robot_head_assaultron", "itemType": "robotHead" },
-      { "type": "fixed", "itemId": "robot_legs_assaultron", "itemType": "robotLeg", "slot": "left" },
-      { "type": "fixed", "itemId": "robot_legs_assaultron", "itemType": "robotLeg", "slot": "right" },
+      {
+        "type": "fixed",
+        "itemId": "robot_head_assaultron_laser",
+        "itemType": "robotHead"
+      },
+      {
+        "type": "fixed",
+        "itemId": "robot_body_assaultron",
+        "itemType": "robotBody"
+      },
+      {
+        "type": "fixed",
+        "itemId": "robot_legs_assaultron",
+        "itemType": "robotLeg"
+      },
+      {
+        "type": "fixed",
+        "itemId": "robot_arm_assaultron",
+        "itemType": "robotArm",
+        "slot": "left"
+      },
+      {
+        "type": "fixed",
+        "itemId": "robot_arm_assaultron",
+        "itemType": "robotArm",
+        "slot": "right"
+      },
       {
         "type": "fixed",
         "weaponId": "weapon_laser_gun",
@@ -12,10 +36,14 @@
         "slot": "left",
         "builtinToArm": true,
         "requiresWeaponId": "robot_arm_assaultron",
-        "modIds": ["mod_053"],
         "ammo": {
           "ammoId": "ammo_energy_cell",
-          "quantity": { "base": 8, "rollType": "rollCD", "rollValue": 7, "op": "+" }
+          "quantity": {
+            "base": 8,
+            "rollType": "rollCD",
+            "rollValue": 7,
+            "op": "+"
+          }
         }
       },
       {
@@ -23,71 +51,155 @@
         "items": [
           {
             "group": [
-              { "armorId": "robot_frame_actuated_body", "itemType": "frame" },
-              { "armorId": "robot_plating_standard_optics", "itemType": "plating", "hiddenInKitModal": true },
-              { "armorId": "robot_plating_standard_arms", "itemType": "plating", "hiddenInKitModal": true },
-              { "armorId": "robot_plating_standard_thruster", "itemType": "plating", "hiddenInKitModal": true }
+              {
+                "type": "fixed",
+                "armorId": "robot_frame_actuated_body",
+                "itemType": "frame"
+              }
             ]
           },
           {
             "group": [
-              { "armorId": "robot_frame_actuated_arms", "itemType": "frame" },
-              { "armorId": "robot_frame_actuated_thruster", "itemType": "frame" },
-              { "armorId": "robot_plating_standard_optics", "itemType": "plating", "hiddenInKitModal": true },
-              { "armorId": "robot_plating_standard_body", "itemType": "plating", "hiddenInKitModal": true }
+              {
+                "type": "fixed",
+                "armorId": "robot_frame_actuated_arms",
+                "itemType": "frame"
+              },
+              {
+                "type": "fixed",
+                "armorId": "robot_frame_actuated_thruster",
+                "itemType": "frame"
+              }
             ]
           }
         ]
       },
-      { "type": "fixed", "itemId": "robot_module_recon_sensors", "itemType": "module" },
-      { "type": "fixed", "itemType": "currency", "quantity": 15 }
+      {
+        "type": "fixed",
+        "itemId": "robot_module_recon_sensors",
+        "itemType": "module"
+      },
+      {
+        "type": "fixed",
+        "itemType": "currency",
+        "quantity": 15
+      }
     ]
   },
   "assaultron_devil": {
     "items": [
-      { "type": "fixed", "itemId": "robot_head_assaultron", "itemType": "robotHead" },
-      { "type": "fixed", "itemId": "robot_legs_assaultron", "itemType": "robotLeg", "slot": "left" },
-      { "type": "fixed", "itemId": "robot_legs_assaultron", "itemType": "robotLeg", "slot": "right" },
-      { "type": "fixed", "weaponId": "robot_weapon_construction_claw", "itemType": "weapon", "slot": "left" },
-      { "type": "fixed", "weaponId": "robot_weapon_construction_claw", "itemType": "weapon", "slot": "right" },
-      { "type": "fixed", "itemId": "robot_item_skull_mask", "itemType": "misc" },
+      {
+        "type": "fixed",
+        "itemId": "robot_head_assaultron_laser",
+        "itemType": "robotHead"
+      },
+      {
+        "type": "fixed",
+        "itemId": "robot_body_assaultron",
+        "itemType": "robotBody"
+      },
+      {
+        "type": "fixed",
+        "itemId": "robot_legs_assaultron",
+        "itemType": "robotLeg"
+      },
+      {
+        "type": "fixed",
+        "weaponId": "robot_weapon_construction_claw",
+        "itemType": "weapon",
+        "slot": "left"
+      },
+      {
+        "type": "fixed",
+        "weaponId": "robot_weapon_construction_claw",
+        "itemType": "weapon",
+        "slot": "right"
+      },
+      {
+        "type": "fixed",
+        "clothingId": "headwear_skull_mask",
+        "itemType": "clothing"
+      },
       {
         "type": "choice",
         "items": [
           {
             "group": [
-              { "armorId": "robot_plating_serrated_body", "itemType": "plating" },
-              { "armorId": "robot_plating_standard_optics", "itemType": "plating", "hiddenInKitModal": true },
-              { "armorId": "robot_plating_standard_arms", "itemType": "plating", "hiddenInKitModal": true },
-              { "armorId": "robot_plating_standard_thruster", "itemType": "plating", "hiddenInKitModal": true }
+              {
+                "type": "fixed",
+                "armorId": "robot_plating_serrated_body",
+                "itemType": "plating"
+              }
             ]
           },
           {
             "group": [
-              { "armorId": "robot_plating_serrated_arms", "itemType": "plating" },
-              { "armorId": "robot_plating_serrated_thruster", "itemType": "plating" },
-              { "armorId": "robot_plating_standard_optics", "itemType": "plating", "hiddenInKitModal": true },
-              { "armorId": "robot_plating_standard_body", "itemType": "plating", "hiddenInKitModal": true }
+              {
+                "type": "fixed",
+                "armorId": "robot_plating_serrated_arms",
+                "itemType": "plating"
+              },
+              {
+                "type": "fixed",
+                "armorId": "robot_plating_serrated_thruster",
+                "itemType": "plating"
+              }
             ]
           }
         ]
       },
-      { "type": "fixed", "itemId": "robot_module_hazard_detection", "itemType": "module" },
       {
         "type": "fixed",
-        "itemType": "ammo",
+        "itemId": "robot_module_hazard_detection",
+        "itemType": "module"
+      },
+      {
+        "type": "fixed",
         "ammoId": "ammo_energy_cell",
-        "quantity": { "base": 6, "rollType": "rollCD", "rollValue": 6, "op": "+" }
+        "itemType": "ammo",
+        "quantity": {
+          "base": 6,
+          "rollType": "rollCD",
+          "rollValue": 6,
+          "op": "+"
+        }
       },
-      { "type": "fixed", "itemId": "robot_item_repair_kit", "itemType": "misc" }
+      {
+        "type": "fixed",
+        "itemId": "robot_item_repair_kit",
+        "itemType": "misc"
+      }
     ]
   },
   "assaultron_caravan_guard": {
     "items": [
-      { "type": "fixed", "itemId": "robot_head_assaultron", "itemType": "robotHead" },
-      { "type": "fixed", "itemId": "robot_legs_assaultron", "itemType": "robotLeg", "slot": "left" },
-      { "type": "fixed", "itemId": "robot_legs_assaultron", "itemType": "robotLeg", "slot": "right" },
-      { "type": "fixed", "itemId": "robot_arm_assaultron", "itemType": "robotArm", "slot": "right" },
+      {
+        "type": "fixed",
+        "itemId": "robot_head_assaultron_laser",
+        "itemType": "robotHead"
+      },
+      {
+        "type": "fixed",
+        "itemId": "robot_body_assaultron",
+        "itemType": "robotBody"
+      },
+      {
+        "type": "fixed",
+        "itemId": "robot_legs_assaultron",
+        "itemType": "robotLeg"
+      },
+      {
+        "type": "fixed",
+        "itemId": "robot_arm_assaultron",
+        "itemType": "robotArm",
+        "slot": "left"
+      },
+      {
+        "type": "fixed",
+        "itemId": "robot_arm_assaultron",
+        "itemType": "robotArm",
+        "slot": "right"
+      },
       {
         "type": "fixed",
         "weaponId": "weapon_laser_gun",
@@ -96,19 +208,54 @@
         "slot": "left",
         "builtinToArm": true,
         "requiresWeaponId": "robot_arm_assaultron",
-        "modIds": ["mod_053"],
         "ammo": {
           "ammoId": "ammo_energy_cell",
-          "quantity": { "base": 14, "rollType": "rollCD", "rollValue": 7, "op": "+" }
+          "quantity": {
+            "base": 14,
+            "rollType": "rollCD",
+            "rollValue": 7,
+            "op": "+"
+          }
+        }
+      },
+      {
+        "type": "fixed",
+        "armorId": "robot_armor_storage_body",
+        "itemType": "armor"
+      },
+      {
+        "type": "fixed",
+        "armorId": "robot_armor_factory_thruster",
+        "itemType": "armor"
+      },
+      {
+        "type": "fixed",
+        "itemId": "robot_module_behavior_analysis",
+        "itemType": "module"
+      },
+      {
+        "type": "rollTable",
+        "tableId": "chem",
+        "roll": {
+          "rollType": "D20",
+          "count": 3,
+          "mode": "separate"
         }
       },
-      { "type": "fixed", "armorId": "robot_armor_storage_body", "itemType": "armor" },
-      { "type": "fixed", "armorId": "robot_armor_factory_thruster", "itemType": "armor" },
-      { "type": "rollTable", "tableId": "ammo", "roll": { "rollType": "D20", "count": 3 } },
-      { "type": "rollTable", "tableId": "chem", "roll": { "rollType": "D20", "count": 3 } },
-      { "type": "rollTable", "tableId": "junk", "roll": { "rollType": "D20", "count": 3 } },
-      { "type": "fixed", "itemId": "robot_module_behavior_analysis", "itemType": "module" },
-      { "type": "fixed", "itemType": "currency", "quantity": { "base": 10, "rollType": "rollCD", "rollValue": 5, "op": "+" } }
+      {
+        "type": "rollTable",
+        "tableId": "trinklet",
+        "roll": {
+          "rollType": "D20",
+          "count": 3,
+          "mode": "separate"
+        }
+      },
+      {
+        "type": "fixed",
+        "itemType": "currency",
+        "quantity": 20
+      }
     ]
   }
 }
diff --git a/modules/fallout/data/equipmentKits/index.js b/modules/fallout/data/equipmentKits/index.js
index 6ea9b7f..d7ba3d1 100644
--- a/modules/fallout/data/equipmentKits/index.js
+++ b/modules/fallout/data/equipmentKits/index.js
@@ -9,7 +9,6 @@
 // ПРАВИЛО (владелец): новый комплект = новый файл (или запись в файле
 // своей фракции) + строка импорта здесь. Движок не трогается.
 
-import assaultron from './assaultron.json';
 import brotherhood from './brotherhood.json';
 import brotherhoodOutcast from './brotherhoodOutcast.json';
 import childOfAtom from './childOfAtom.json';
@@ -19,6 +18,7 @@ import minuteman from './minuteman.json';
 import misterHandy from './misterHandy.json';
 import ncr from './ncr.json';
 import nightkin from './nightkin.json';
+import assaultron from './assaultron.json';
 import protectron from './protectron.json';
 import robobrain from './robobrain.json';
 import securitron from './securitron.json';
@@ -28,7 +28,6 @@ import wastelander from './wastelander.json';
 import treefamilies from './treefamilies.json';
 
 export default {
-  ...assaultron,
   ...brotherhood,
   ...brotherhoodOutcast,
   ...childOfAtom,
@@ -38,6 +37,7 @@ export default {
   ...misterHandy,
   ...ncr,
   ...nightkin,
+  ...assaultron,
   ...protectron,
   ...robobrain,
   ...securitron,
diff --git a/modules/fallout/data/origins/origins.json b/modules/fallout/data/origins/origins.json
index 68efce5..6ab913f 100644
--- a/modules/fallout/data/origins/origins.json
+++ b/modules/fallout/data/origins/origins.json
@@ -170,7 +170,7 @@
       "assaultron-designed-for-frontline"
     ],
     "equipmentKitIds": [
-      "assaultron_us_army",
+      "assaultron_us_military",
       "assaultron_devil",
       "assaultron_caravan_guard"
     ]
diff --git a/modules/fallout/i18n/en-EN/data/equipment/armor/clothes.json b/modules/fallout/i18n/en-EN/data/equipment/armor/clothes.json
index 02abf52..746071d 100644
--- a/modules/fallout/i18n/en-EN/data/equipment/armor/clothes.json
+++ b/modules/fallout/i18n/en-EN/data/equipment/armor/clothes.json
@@ -179,6 +179,10 @@
         {
           "id": "headwear_white_glove_mask",
           "name": "White Glove Society Mask"
+        },
+        {
+          "id": "headwear_skull_mask",
+          "name": "Skull Mask"
         }
       ]
     }
diff --git a/modules/fallout/i18n/en-EN/data/system/equipmentKits.json b/modules/fallout/i18n/en-EN/data/system/equipmentKits.json
index 5f2bf81..0510f01 100644
--- a/modules/fallout/i18n/en-EN/data/system/equipmentKits.json
+++ b/modules/fallout/i18n/en-EN/data/system/equipmentKits.json
@@ -68,6 +68,15 @@
   "robobrain_nonordinary": {
     "name": "Extraordinary Individual"
   },
+  "assaultron_us_military": {
+    "name": "US Military Model"
+  },
+  "assaultron_devil": {
+    "name": "Assaultron Devil"
+  },
+  "assaultron_caravan_guard": {
+    "name": "Robotic Caravan Guard"
+  },
   "default_caps_only": {
     "name": "Starting Capital"
   },
@@ -124,14 +133,5 @@
   },
   "nightkin": {
     "name": "NIGHTKIN"
-  },
-  "assaultron_us_army": {
-    "name": "U.S. Military Model"
-  },
-  "assaultron_devil": {
-    "name": "Assaultron Devil"
-  },
-  "assaultron_caravan_guard": {
-    "name": "Robotic Caravan Guard"
   }
 }
diff --git a/modules/fallout/i18n/ru-RU/data/equipment/armor/clothes.json b/modules/fallout/i18n/ru-RU/data/equipment/armor/clothes.json
index 88ec902..45085fb 100644
--- a/modules/fallout/i18n/ru-RU/data/equipment/armor/clothes.json
+++ b/modules/fallout/i18n/ru-RU/data/equipment/armor/clothes.json
@@ -179,6 +179,10 @@
         {
           "id": "headwear_white_glove_mask",
           "name": "Маска Общества «Белая перчатка»"
+        },
+        {
+          "id": "headwear_skull_mask",
+          "name": "Маска-череп"
         }
       ]
     }
diff --git a/modules/fallout/i18n/ru-RU/data/system/equipmentKits.json b/modules/fallout/i18n/ru-RU/data/system/equipmentKits.json
index a708938..15cafe7 100644
--- a/modules/fallout/i18n/ru-RU/data/system/equipmentKits.json
+++ b/modules/fallout/i18n/ru-RU/data/system/equipmentKits.json
@@ -68,6 +68,15 @@
   "robobrain_nonordinary": {
     "name": "Неординарная личность"
   },
+  "assaultron_us_military": {
+    "name": "Модель армии США"
+  },
+  "assaultron_devil": {
+    "name": "Штурмотрон-дьявол"
+  },
+  "assaultron_caravan_guard": {
+    "name": "Караванный охранник"
+  },
   "default_caps_only": {
     "name": "Стартовый капитал"
   },
@@ -124,14 +133,5 @@
   },
   "nightkin": {
     "name": "Тень"
-  },
-  "assaultron_us_army": {
-    "name": "Модель армии США"
-  },
-  "assaultron_devil": {
-    "name": "Штурмотрон-Дьявол"
-  },
-  "assaultron_caravan_guard": {
-    "name": "Охранник каравана"
   }
 }
diff --git a/modules/fallout/screens/CharacterScreen/modals/EquipmentKitModal.js b/modules/fallout/screens/CharacterScreen/modals/EquipmentKitModal.js
index 7b5286b..05f1340 100644
--- a/modules/fallout/screens/CharacterScreen/modals/EquipmentKitModal.js
+++ b/modules/fallout/screens/CharacterScreen/modals/EquipmentKitModal.js
@@ -9,13 +9,15 @@ import { useLocale, useModuleLocale } from '../../../../../i18n/locale';
 import styles from '../../../../../styles/EquipmentKitModal.styles';
 import { tCharacterScreen } from '../logic/characterScreenI18n';
 
-// Lazy-load robot catalog data
+// Lazy-load robot catalog data — на основании данных, без обогатителя
 const loadRobotCatalog = () => ({
   heads: require('../../../data/equipment/robot/robotheads.json'),
   bodies: require('../../../data/equipment/robot/robotbody.json'),
   arms: require('../../../data/equipment/robot/robotarms.json'),
   legs: require('../../../data/equipment/robot/robotlegs.json'),
   weapons: require('../../../data/equipment/robot/weapons.json'),
+  plating: require('../../../data/equipment/robot/armor_plating.json').plating || [],
+  frames: require('../../../data/equipment/robot/frames.json').frames || [],
 });
 
 const META_CATEGORY_ORDER = ['structure', 'apparel', 'weapon', 'module', 'misc'];
@@ -31,6 +33,8 @@ const STRUCTURE_SUBORDER = {
   robotLegs: 3,
   plating: 4,
   frame: 5,
+  robotFrame: 5,
+  robotArmor: 6,
 };
 
 const STRUCTURE_TYPES = new Set(Object.keys(STRUCTURE_SUBORDER));
@@ -300,7 +304,7 @@ const EquipmentKitModal = ({ visible, onClose, equipmentKits, onSelectKit, chara
       // (initRobotSlots) — в инвентарь их копия не нужна.
       const slotConsumedTypes = new Set([
         'robotArm', 'robotHead', 'robotBody', 'robotLeg', 'robotLegs',
-        'plating', 'armor', 'robotArmor', 'frame', 'module',
+        'plating', 'armor', 'robotArmor', 'frame', 'robotFrame', 'module',
       ]);
       const finalItemsOnly = finalItems.filter((item) => {
         if (slotConsumedTypes.has(item.itemType)) return false;
@@ -350,55 +354,11 @@ const EquipmentKitModal = ({ visible, onClose, equipmentKits, onSelectKit, chara
     onClose();
   };
 
-  // BodyPlan = обязательная стандартная конструкция робота (как CPU/RAM
-  // у компа — без них робот не работает). То, что в bodyplans.json:defaults
-  // для данного bodyPlan, не является выбором игрока и НЕ показывается
-  // в модалке. Инициализация фактических слотов идёт в initRobotSlots
-  // (head/body/leg/arms автозаполняются), плюс сам кит может перечислить
-  // альтернативы — тогда они и показываются, и экипируются.
-  //
-  // Логика: для каждого entry с itemType ∈ {robotHead, robotBody,
-  // robotArm, robotLeg, robotLegs} проверяем, совпадает ли entry.itemId
-  // с дефолтным для соответствующего слота в bodyPlan. Если совпадает —
-  // это «обязательная часть конструкции», в модалке НЕ рендерим (как
-  // «Голова Штурмотрона» в Дьяволе/Охраннике/УС Арми: дефолт, не выбор).
-  // Если не совпадает — это нестандартная часть (например, Construction
-  // Claw в Дьяволе вместо обычной клешни), её показываем.
-  const LIMB_TYPES = new Set(['robotHead', 'robotBody', 'robotArm', 'robotLeg', 'robotLegs']);
-
-  // Определяем, к какому слоту bodyPlan относится entry. Если у entry
-  // указан явный slot, используем его. Иначе — эвристика по itemType.
-  const resolveLimbSlotKey = (entry) => {
-    if (entry?.slot && ['leftArm', 'rightArm', 'arm1', 'arm2', 'arm3', 'leftLeg', 'rightLeg', 'head', 'body'].includes(entry.slot)) {
-      return entry.slot;
-    }
-    if (entry?.itemType === 'robotHead') return 'head';
-    if (entry?.itemType === 'robotBody') return 'body';
-    if (entry?.itemType === 'robotLeg' || entry?.itemType === 'robotLegs') {
-      return entry?.slot === 'left' ? 'leftLeg' : entry?.slot === 'right' ? 'rightLeg' : null;
-    }
-    return null;
-  };
-
-  const isDefaultLimb = (entry, bodyPlanDefaults) => {
-    if (!bodyPlanDefaults) return false;
-    if (!LIMB_TYPES.has(entry?.itemType)) return false;
-    const slotKey = resolveLimbSlotKey(entry);
-    if (!slotKey) return false;
-    return bodyPlanDefaults[slotKey] === entry?.itemId;
-  };
-
   const getGroupedEntries = (kit) => {
     const groups = {};
-    // bodyPlan defaults — для фильтрации «дефолт = скрыть в модалке».
-    const bodyPlanDefaults = isRobotCharacter(character)
-      ? (getBodyPlan(resolveRobotBodyPlan(character))?.defaults || null)
-      : null;
 
     (kit.items || []).forEach((entry, index) => {
       if (entry?.hiddenInKitModal) return;
-      // Скрыть limb, совпадающий с bodyPlan default (см. isDefaultLimb).
-      if (isDefaultLimb(entry, bodyPlanDefaults)) return;
       const probe = entry?.type === 'choice' ? (entry.items || [])[0] : entry;
       const meta = getMetaCategory(probe);
       if (!groups[meta]) groups[meta] = [];
@@ -454,13 +414,7 @@ const EquipmentKitModal = ({ visible, onClose, equipmentKits, onSelectKit, chara
                                         const selected = selectedChoices[choiceKey] === optionKey;
 
                                         const optionLabel = option.group
-                                          // Элементы с hiddenInKitModal не объявляются в UI (дефолтные
-                                          // Standard Plating, автозаполняемые тела и т.п.), но
-                                          // попадают в слоты через option.group → choice → items.
-                                          ? option.group
-                                              .filter((g) => !g?.hiddenInKitModal)
-                                              .map((groupItem) => `${getDisplayName(groupItem)}${formatBuiltinWeaponSuffix(groupItem)}`)
-                                              .join(' + ')
+                                          ? option.group.map((groupItem) => `${getDisplayName(groupItem)}${formatBuiltinWeaponSuffix(groupItem)}`).join(' + ')
                                           : `${getDisplayName(option)}${formatBuiltinWeaponSuffix(option)}`;
 
                                         return (
diff --git a/patchs/156-assaultron-1slot-defaultPlating-fix.patch b/patchs/156-assaultron-1slot-defaultPlating-fix.patch
new file mode 100644
index 0000000..ec65873
--- /dev/null
+++ b/patchs/156-assaultron-1slot-defaultPlating-fix.patch
@@ -0,0 +1,1057 @@
+diff --git a/__tests__/robot/assaultron-kits.test.js b/__tests__/robot/assaultron-kits.test.js
+new file mode 100644
+index 0000000..b6a1124
+--- /dev/null
++++ b/__tests__/robot/assaultron-kits.test.js
+@@ -0,0 +1,160 @@
++// __tests__/robot/assaultron-kits.test.js
++// Комплекты снаряжения Штурмотрона (Wanderer's Guide):
++//  - комплект существует и привязан к ориджину;
++//  - комплект выдаёт голову с лазером — у головы есть встроенное оружие,
++//    и оно попадает в карточки оружия со статами;
++//  - строительные когти — оружие: становятся конечностью со встроенным
++//    оружием, у которого есть статы (урон/тип/навык) для экрана экипировки.
++import { describe, it, expect } from 'vitest';
++
++import kits from '../../modules/fallout/data/equipmentKits/index.js';
++import origins from '../../modules/fallout/data/origins/origins.json';
++import heads from '../../modules/fallout/data/equipment/robot/robotheads.json';
++import bodies from '../../modules/fallout/data/equipment/robot/robotbody.json';
++import arms from '../../modules/fallout/data/equipment/robot/robotarms.json';
++import legs from '../../modules/fallout/data/equipment/robot/robotlegs.json';
++import robotWeapons from '../../modules/fallout/data/equipment/robot/weapons.json';
++import ruKits from '../../modules/fallout/i18n/ru-RU/data/system/equipmentKits.json';
++import enKits from '../../modules/fallout/i18n/en-EN/data/system/equipmentKits.json';
++import { initRobotSlots, getBuiltinWeaponsFromSlots } from '../../domain/robotEquip';
++
++const ASSAULTRON_KIT_IDS = [
++  'assaultron_us_military',
++  'assaultron_devil',
++  'assaultron_caravan_guard',
++];
++
++const robotCatalog = { heads, bodies, arms, legs, weapons: robotWeapons };
++
++const byId = (list, id) => list.find((entry) => entry.id === id);
++
++describe('комплекты снаряжения Штурмотрона', () => {
++  it('ориджин assaultron ссылается на три комплекта, и все они существуют', () => {
++    const origin = origins.find((o) => o.id === 'assaultron');
++    expect(origin.equipmentKitIds).toEqual(ASSAULTRON_KIT_IDS);
++    for (const kitId of ASSAULTRON_KIT_IDS) {
++      expect(kits[kitId], `нет комплекта ${kitId}`).toBeTruthy();
++      expect(Array.isArray(kits[kitId].items)).toBe(true);
++      expect(ruKits[kitId]?.name, `нет ru-имени ${kitId}`).toBeTruthy();
++      expect(enKits[kitId]?.name, `нет en-имени ${kitId}`).toBeTruthy();
++    }
++  });
++
++  it('каждый комплект выдаёт голову с лазером', () => {
++    for (const kitId of ASSAULTRON_KIT_IDS) {
++      const hasLaserHead = kits[kitId].items.some(
++        (item) => item.itemId === 'robot_head_assaultron_laser' && item.itemType === 'robotHead',
++      );
++      expect(hasLaserHead, `${kitId} не выдаёт голову с лазером`).toBe(true);
++    }
++  });
++
++  it('у головы с лазером есть встроенное оружие (лазер головы)', () => {
++    const head = byId(heads, 'robot_head_assaultron_laser');
++    expect(head.builtinWeaponId).toBe('robot_weapon_assaultron_head_laser');
++    const laser = byId(robotWeapons, 'robot_weapon_assaultron_head_laser');
++    expect(laser.damage).toBeGreaterThan(0);
++    expect(laser.damageType).toBe('energy');
++  });
++
++  it('initRobotSlots: голова из комплекта даёт карточку лазера со статами', () => {
++    const head = byId(heads, 'robot_head_assaultron_laser');
++    const { slots } = initRobotSlots('assaultron', [{ ...head }], robotCatalog);
++    const weapons = getBuiltinWeaponsFromSlots(slots);
++    const laser = weapons.find((w) => w.id === 'robot_weapon_assaultron_head_laser');
++    expect(laser, 'лазер головы не попал в оружие').toBeTruthy();
++    expect(laser.damage).toBe(5);
++    expect(laser.damageType).toBe('energy');
++    expect(laser.mainSkill).toBe('ENERGY_WEAPONS');
++    expect(laser.isBuiltin).toBe(true);
++  });
++
++  it('initRobotSlots: строительные когти — оружие со статами в слотах рук', () => {
++    const clawWeapon = byId(robotWeapons, 'robot_weapon_construction_claw');
++    const kitItems = [
++      { ...clawWeapon, itemType: 'weapon', weaponId: clawWeapon.id, slot: 'left' },
++      { ...clawWeapon, itemType: 'weapon', weaponId: clawWeapon.id, slot: 'right' },
++    ];
++    const { slots } = initRobotSlots('assaultron', kitItems, robotCatalog);
++
++    expect(slots.leftArm.limb?.id).toBe('robot_weapon_construction_claw');
++    expect(slots.rightArm.limb?.id).toBe('robot_weapon_construction_claw');
++
++    const weapons = getBuiltinWeaponsFromSlots(slots);
++    const claw = weapons.find((w) => w.id === 'robot_weapon_construction_claw');
++    expect(claw, 'коготь не попал в оружие').toBeTruthy();
++    expect(claw.damage).toBe(4);
++    expect(claw.damageType).toBe('physical');
++    expect(claw.mainSkill).toBe('UNARMED');
++    expect(claw.isBuiltin).toBe(true);
++  });
++
++  it('initRobotSlots: рамы и обшивка — 1 предмет = 1 слот (включая ноги)', () => {
++    const kitItems = [
++      {
++        id: 'robot_frame_actuated_body',
++        itemType: 'robotFrame',
++        layer: 'frame',
++        robotLocation: 'Main Body',
++        damageResistance: { physical: 1, energy: 1 },
++      },
++      {
++        id: 'robot_plating_standard_thruster',
++        itemType: 'plating',
++        layer: 'plating',
++        robotLocation: 'Thruster',
++        damageResistance: { physical: 2, energy: 0 },
++      },
++      {
++        id: 'robot_plating_standard_thruster_2',
++        itemType: 'plating',
++        layer: 'plating',
++        robotLocation: 'Thruster',
++        damageResistance: { physical: 2, energy: 0 },
++      },
++    ];
++    const { slots, inventoryItems } = initRobotSlots('assaultron', kitItems, robotCatalog);
++    expect(slots.body.frame?.id).toBe('robot_frame_actuated_body');
++    // Локация Thruster у штурмотрона — ноги, 1 предмет = 1 слот
++    // Первый thruster → leftLeg, второй → rightLeg (первый свободный)
++    expect(slots.leftLeg.plating?.id).toBe('robot_plating_standard_thruster');
++    expect(slots.rightLeg.plating?.id).toBe('robot_plating_standard_thruster_2');
++    expect(inventoryItems).toHaveLength(0);
++  });
++
++  it('initRobotSlots: Arms — 1 предмет = 1 слот руки', () => {
++    const kitItems = [
++      {
++        id: 'robot_frame_actuated_arms',
++        itemType: 'robotFrame',
++        layer: 'frame',
++        robotLocation: 'Arms',
++        damageResistance: { physical: 1, energy: 1 },
++      },
++    ];
++    const { slots } = initRobotSlots('assaultron', kitItems, robotCatalog);
++    // Один предмет Arms должен занять только leftArm, а не обе руки
++    expect(slots.leftArm.frame?.id).toBe('robot_frame_actuated_arms');
++    expect(slots.rightArm.frame).toBeFalsy();
++  });
++
++  it('initRobotSlots: defaultPlating из bodyPlan — стандартная обшивка на основании данных', () => {
++    // Пустой комплект — должны подтянуться дефолты из bodyPlan.defaultPlating
++    const { slots } = initRobotSlots('assaultron', [], {
++      ...robotCatalog,
++      plating: [
++        { id: 'robot_plating_standard_optics', layer: 'plating', robotLocation: 'Optics', damageResistance: { physical: 2, energy: 0 } },
++        { id: 'robot_plating_standard_body', layer: 'plating', robotLocation: 'Main Body', damageResistance: { physical: 2, energy: 0 } },
++        { id: 'robot_plating_standard_arms', layer: 'plating', robotLocation: 'Arms', damageResistance: { physical: 2, energy: 0 } },
++        { id: 'robot_plating_standard_thruster', layer: 'plating', robotLocation: 'Thruster', damageResistance: { physical: 2, energy: 0 } },
++      ],
++      frames: [],
++    });
++    expect(slots.head.plating?.id).toBe('robot_plating_standard_optics');
++    expect(slots.body.plating?.id).toBe('robot_plating_standard_body');
++    expect(slots.leftArm.plating?.id).toBe('robot_plating_standard_arms');
++    expect(slots.rightArm.plating?.id).toBe('robot_plating_standard_arms');
++    expect(slots.leftLeg.plating?.id).toBe('robot_plating_standard_thruster');
++    expect(slots.rightLeg.plating?.id).toBe('robot_plating_standard_thruster');
++  });
++});
+diff --git a/domain/bodyplan.js b/domain/bodyplan.js
+index d74d610..0f65933 100644
+--- a/domain/bodyplan.js
++++ b/domain/bodyplan.js
+@@ -62,6 +62,10 @@ export function getDefaultLimbs(planId) {
+   return { ...(bodyPlansRegistry[planId]?.defaults || {}) };
+ }
+ 
++export function getDefaultPlating(planId) {
++  return { ...(bodyPlansRegistry[planId]?.defaultPlating || {}) };
++}
++
+ export function getBodyPlan(planId) {
+   return bodyPlansRegistry[planId] || null;
+ }
+diff --git a/domain/robotEquip.js b/domain/robotEquip.js
+index 6da5c50..3b4de2b 100644
+--- a/domain/robotEquip.js
++++ b/domain/robotEquip.js
+@@ -1,4 +1,4 @@
+-import { getBodyPlan, createSlotsFromBodyPlan, getDefaultLimbs } from './bodyplan';
++import { getBodyPlan, createSlotsFromBodyPlan, getDefaultLimbs, getDefaultPlating } from './bodyplan';
+ import { applyWeaponMods as applyWeaponModsPipeline } from './enrichItem';
+ 
+ // domain/robotEquip.js
+@@ -336,26 +336,49 @@ export function initRobotSlots(bodyPlan, resolvedKitItems = [], robotCatalog = {
+       continue;
+     }
+ 
+-    // Броня
+-    if (['plating', 'armor', 'frame', 'robotArmor'].includes(itype)) {
++    // Броня — 1 предмет = 1 слот (без фолбэков, строго по данным)
++    // Рама/Обшивка/Броня применяются к конкретной конечности, не к обеим сразу.
++    // Thruster — обобщённая локация «средство передвижения»: у Мистера Помощника это thruster,
++    // у протектрона/штурмотрона — ноги, у секьюритрона — колесо. Это не фолбэк, а маппинг типов шасси.
++    if (['plating', 'armor', 'frame', 'robotArmor', 'robotFrame'].includes(itype)) {
+       const armorData = item._armor ?? item;
+       const location = armorData.robotLocation ?? item.robotLocation;
+       const layer = armorData.layer ?? itype;
+-      
+-      // Простое распределение
+-      for (const k of slotKeys) {
+-        if (location === 'Main Body' && k === 'body') {
+-          slots[k][layer] = armorData;
+-        } else if (location === 'Optics' && k === 'head') {
+-          slots[k][layer] = armorData;
+-        } else if (location === 'Arms' && k.toLowerCase().includes('arm')) {
+-          slots[k][layer] = armorData;
+-        } else if ((location === 'Legs' || location === 'Wheel') && (k.toLowerCase().includes('leg') || k === 'chassis' || k === 'thruster' || k === 'wheel')) {
+-          slots[k][layer] = armorData;
+-        } else if (location === 'Thruster' && (k === 'thruster' || k === 'wheel')) {
+-          slots[k][layer] = armorData;
++
++      // Определяем, какие слоты подходят для этой локации
++      const isMatchingSlot = (slotKey) => {
++        if (location === 'Main Body' && slotKey === 'body') return true;
++        if (location === 'Optics' && slotKey === 'head') return true;
++        if (location === 'Arms' && slotKey.toLowerCase().includes('arm')) return true;
++        if ((location === 'Legs' || location === 'Wheel') && (slotKey.toLowerCase().includes('leg') || slotKey === 'chassis' || slotKey === 'thruster' || slotKey === 'wheel')) return true;
++        if (location === 'Thruster' && (slotKey.toLowerCase().includes('leg') || slotKey === 'chassis' || slotKey === 'thruster' || slotKey === 'wheel')) return true;
++        // Точные совпадения для новых per-slot локаций (если появятся leftArm/rightArm и т.д.)
++        if (location === slotKey) return true;
++        if (location === 'Head' && slotKey === 'head') return true;
++        if (location === 'Left Arm' && slotKey === 'leftArm') return true;
++        if (location === 'Right Arm' && slotKey === 'rightArm') return true;
++        if (location === 'Left Leg' && slotKey === 'leftLeg') return true;
++        if (location === 'Right Leg' && slotKey === 'rightLeg') return true;
++        return false;
++      };
++
++      // 1 предмет = 1 слот: ищем первый подходящий слот, где слой свободен
++      // Если указан конкретный слот в item.slot — используем его
++      let targetSlot = null;
++      if (item.slot && slotKeys.includes(item.slot) && isMatchingSlot(item.slot)) {
++        targetSlot = item.slot;
++      } else {
++        // Ищем первый свободный слот из подходящих
++        targetSlot = slotKeys.find((k) => isMatchingSlot(k) && slots[k][layer] == null) || null;
++        // Если все подходящие слоты заняты — заменяем первый подходящий (для frame/plating, которые конфликтуют)
++        if (!targetSlot) {
++          targetSlot = slotKeys.find((k) => isMatchingSlot(k)) || null;
+         }
+       }
++
++      if (targetSlot && slots[targetSlot] !== undefined) {
++        slots[targetSlot][layer] = armorData;
++      }
+       continue;
+     }
+ 
+@@ -371,7 +394,8 @@ export function initRobotSlots(bodyPlan, resolvedKitItems = [], robotCatalog = {
+     }
+   }
+ 
+-  // Автозаполнение недостающих конечностей
++  // Автозаполнение недостающих конечностей — строго из данных bodyPlan.defaults, без фолбэков
++  // 1 предмет = 1 слот: каждый слот заполняется своей конечностью из defaults[slotKey]
+   const { heads = [], bodies = [], arms = [], legs = [] } = robotCatalog;
+   const planDefaults = getDefaultLimbs(bodyPlan);
+   const defaultHead = heads.find((h) => h.id === planDefaults.head)
+@@ -384,8 +408,34 @@ export function initRobotSlots(bodyPlan, resolvedKitItems = [], robotCatalog = {
+   for (const k of slotKeys) {
+     if (slots[k].limb !== null) continue;
+ 
++    // Проверяем конкретный слот в defaults (например, leftArm, rightLeg и т.д.)
++    const specificDefaultId = planDefaults[k];
++    if (specificDefaultId) {
++      // Ищем в соответствующем каталоге по ID
++      let specificLimb = null;
++      if (k === 'head') {
++        specificLimb = heads.find((h) => h.id === specificDefaultId);
++      } else if (k === 'body') {
++        specificLimb = bodies.find((b) => b.id === specificDefaultId);
++      } else if (k.toLowerCase().includes('arm')) {
++        specificLimb = arms.find((a) => a.id === specificDefaultId);
++      } else if (k.toLowerCase().includes('leg') || k === 'chassis' || k === 'thruster' || k === 'wheel') {
++        specificLimb = legs.find((l) => l.id === specificDefaultId);
++      }
++      if (specificLimb) {
++        if (k.toLowerCase().includes('arm')) {
++          const limbFromArm = buildLimbFromArmEntry(specificLimb);
++          const builtinWeapons = normalizeBuiltinWeapons(limbFromArm, weaponsCatalog);
++          slots[k].limb = builtinWeapons.length > 0 ? { ...limbFromArm, builtinWeapons } : limbFromArm;
++        } else {
++          const builtinWeapons = normalizeBuiltinWeapons(specificLimb, weaponsCatalog);
++          slots[k].limb = builtinWeapons.length > 0 ? { ...specificLimb, builtinWeapons } : specificLimb;
++        }
++        continue;
++      }
++    }
++
+     if (k === 'head' && defaultHead) {
+-      // Resolve builtinWeaponId for the default head (e.g. robobrain mesmetron)
+       const builtinWeapons = normalizeBuiltinWeapons(defaultHead, weaponsCatalog);
+       const headLimb = builtinWeapons.length > 0 ? { ...defaultHead, builtinWeapons } : defaultHead;
+       slots[k].limb = headLimb;
+@@ -396,26 +446,32 @@ export function initRobotSlots(bodyPlan, resolvedKitItems = [], robotCatalog = {
+       defaultLeg
+     ) {
+       slots[k].limb = defaultLeg;
+-    } else if (
+-      // Руки: автозаполняем пустые arm-слоты из planDefaults[k] (leftArm/rightArm/arm1/arm2/arm3).
+-      // Только если у слота есть дефолтный arm-id в bodyplan и он совместим с bodyPlan.
+-      k.toLowerCase().includes('arm')
+-    ) {
+-      const defaultArmId = planDefaults[k];
+-      if (defaultArmId) {
+-        const armEntry = arms.find((a) => a.id === defaultArmId);
+-        // Проверяем совместимость (если в каталоге указан compatibleBodyPlans)
+-        if (armEntry) {
+-          const compatible = armEntry.compatibleBodyPlans;
+-          const defaultPlan = armEntry.defaultForBodyPlan;
+-          const fits = compatible
+-            ? compatible.includes(bodyPlan)
+-            : (defaultPlan ? defaultPlan === bodyPlan : true);
+-          if (fits) {
+-            slots[k].limb = buildLimbFromArmEntry(armEntry);
+-          }
+-        }
+-      }
++    }
++    // Для рук, если нет specificDefaultId, но есть default для bodyPlan — не автозаполняем,
++    // так как у разных роботов разное количество рук, и defaults должны быть явными
++  }
++
++  // Автозаполнение стандартной обшивки из bodyPlan.defaultPlating — на основании данных, без обогатителя
++  // defaultPlating: { slotKey: platingId } — например, head: standard_optics, body: standard_body, leftArm: standard_arms
++  // Ставится только если в слоте нет plating/frame/armor (конфликт слоёв)
++  const defaultPlatingMap = getDefaultPlating(bodyPlan);
++  const platingCatalog = Array.isArray(robotCatalog.plating) ? robotCatalog.plating : [];
++  const framesCatalog = Array.isArray(robotCatalog.frames) ? robotCatalog.frames : [];
++  const allArmorCatalog = [...platingCatalog, ...framesCatalog];
++
++  const resolvePlatingById = (id) => {
++    if (!id) return null;
++    return allArmorCatalog.find((p) => p.id === id) || null;
++  };
++
++  for (const k of slotKeys) {
++    if (slots[k].plating || slots[k].frame || slots[k].armor) continue;
++    const defaultPlatingId = defaultPlatingMap[k];
++    if (!defaultPlatingId) continue;
++    const platingData = resolvePlatingById(defaultPlatingId);
++    if (platingData) {
++      const layer = platingData.layer || 'plating';
++      slots[k][layer] = platingData;
+     }
+   }
+ 
+diff --git a/modules/fallout/data/bodyplans/bodyplans.json b/modules/fallout/data/bodyplans/bodyplans.json
+index db82267..e5ce4a8 100644
+--- a/modules/fallout/data/bodyplans/bodyplans.json
++++ b/modules/fallout/data/bodyplans/bodyplans.json
+@@ -40,6 +40,13 @@
+       "rightArm": "robot_arm_robobrain",
+       "chassis": "robot_legs_robobrain_treads"
+     },
++    "defaultPlating": {
++      "head": "robot_plating_standard_optics",
++      "body": "robot_plating_standard_body",
++      "leftArm": "robot_plating_standard_arms",
++      "rightArm": "robot_plating_standard_arms",
++      "chassis": "robot_plating_standard_thruster"
++    },
+     "slotCapabilities": {
+       "leftArm": {
+         "canEquipWeapon": true
+@@ -69,6 +76,14 @@
+       "arm3": "robot_arm_mister_handy",
+       "thruster": "robot_legs_mister_handy_thruster"
+     },
++    "defaultPlating": {
++      "head": "robot_plating_standard_optics",
++      "body": "robot_plating_standard_body",
++      "arm1": "robot_plating_standard_arms",
++      "arm2": "robot_plating_standard_arms",
++      "arm3": "robot_plating_standard_arms",
++      "thruster": "robot_plating_standard_thruster"
++    },
+     "slotCapabilities": {
+       "arm1": {
+         "canEquipWeapon": true
+@@ -98,6 +113,14 @@
+       "body": "robot_body_protectron",
+       "rightLeg": "robot_legs_protectron"
+     },
++    "defaultPlating": {
++      "head": "robot_plating_standard_optics",
++      "body": "robot_plating_standard_body",
++      "leftArm": "robot_plating_standard_arms",
++      "rightArm": "robot_plating_standard_arms",
++      "leftLeg": "robot_plating_standard_thruster",
++      "rightLeg": "robot_plating_standard_thruster"
++    },
+     "slotCapabilities": {
+       "leftArm": {
+         "canEquipWeapon": true
+@@ -122,6 +145,13 @@
+       "rightArm": "robot_arm_securitron",
+       "wheel": "robot_legs_securitron"
+     },
++    "defaultPlating": {
++      "head": "robot_plating_standard_optics",
++      "body": "robot_plating_standard_body",
++      "leftArm": "robot_plating_standard_arms",
++      "rightArm": "robot_plating_standard_arms",
++      "wheel": "robot_plating_standard_thruster"
++    },
+     "slotCapabilities": {
+       "leftArm": {
+         "canEquipWeapon": true
+@@ -168,6 +198,14 @@
+       "body": "robot_body_assaultron",
+       "rightLeg": "robot_legs_assaultron"
+     },
++    "defaultPlating": {
++      "head": "robot_plating_standard_optics",
++      "body": "robot_plating_standard_body",
++      "leftArm": "robot_plating_standard_arms",
++      "rightArm": "robot_plating_standard_arms",
++      "leftLeg": "robot_plating_standard_thruster",
++      "rightLeg": "robot_plating_standard_thruster"
++    },
+     "slotCapabilities": {
+       "leftArm": {
+         "canEquipWeapon": true
+@@ -197,6 +235,14 @@
+       "body": "robot_body_sentry_bot",
+       "rightLeg": "robot_legs_sentry_bot"
+     },
++    "defaultPlating": {
++      "head": "robot_plating_standard_optics",
++      "body": "robot_plating_standard_body",
++      "leftArm": "robot_plating_standard_arms",
++      "rightArm": "robot_plating_standard_arms",
++      "leftLeg": "robot_plating_standard_thruster",
++      "rightLeg": "robot_plating_standard_thruster"
++    },
+     "slotCapabilities": {
+       "leftArm": {
+         "canEquipWeapon": true
+diff --git a/modules/fallout/data/equipment/clothes.json b/modules/fallout/data/equipment/clothes.json
+index 75e77cb..f453a54 100644
+--- a/modules/fallout/data/equipment/clothes.json
++++ b/modules/fallout/data/equipment/clothes.json
+@@ -570,6 +570,23 @@
+           "clothingType": "headwear",
+           "allowsArmor": false,
+           "canRobotWear": true
++        },
++        {
++          "id": "headwear_skull_mask",
++          "protectedAreas": [
++            "Head"
++          ],
++          "physicalDamageRating": 0,
++          "energyDamageRating": 0,
++          "radiationDamageRating": 0,
++          "weight": 0.5,
++          "cost": 10,
++          "rarity": 1,
++          "specialEffects": [],
++          "itemType": "clothing",
++          "clothingType": "headwear",
++          "allowsArmor": false,
++          "canRobotWear": true
+         }
+       ]
+     }
+diff --git a/modules/fallout/data/equipment/robot/robotheads.json b/modules/fallout/data/equipment/robot/robotheads.json
+index 9e68c7b..9c565d2 100644
+--- a/modules/fallout/data/equipment/robot/robotheads.json
++++ b/modules/fallout/data/equipment/robot/robotheads.json
+@@ -63,6 +63,7 @@
+     "itemType": "robotHead",
+     "replaceable": false,
+     "defaultForBodyPlan": null,
++    "builtinWeaponId": "robot_weapon_assaultron_head_laser",
+     "unique": false,
+     "complexity": 7,
+     "perksRequired": [
+diff --git a/modules/fallout/data/equipmentKits/assaultron.json b/modules/fallout/data/equipmentKits/assaultron.json
+index 0150917..6677a1e 100644
+--- a/modules/fallout/data/equipmentKits/assaultron.json
++++ b/modules/fallout/data/equipmentKits/assaultron.json
+@@ -1,9 +1,33 @@
+ {
+-  "assaultron_us_army": {
++  "assaultron_us_military": {
+     "items": [
+-      { "type": "fixed", "itemId": "robot_head_assaultron", "itemType": "robotHead" },
+-      { "type": "fixed", "itemId": "robot_legs_assaultron", "itemType": "robotLeg", "slot": "left" },
+-      { "type": "fixed", "itemId": "robot_legs_assaultron", "itemType": "robotLeg", "slot": "right" },
++      {
++        "type": "fixed",
++        "itemId": "robot_head_assaultron_laser",
++        "itemType": "robotHead"
++      },
++      {
++        "type": "fixed",
++        "itemId": "robot_body_assaultron",
++        "itemType": "robotBody"
++      },
++      {
++        "type": "fixed",
++        "itemId": "robot_legs_assaultron",
++        "itemType": "robotLeg"
++      },
++      {
++        "type": "fixed",
++        "itemId": "robot_arm_assaultron",
++        "itemType": "robotArm",
++        "slot": "left"
++      },
++      {
++        "type": "fixed",
++        "itemId": "robot_arm_assaultron",
++        "itemType": "robotArm",
++        "slot": "right"
++      },
+       {
+         "type": "fixed",
+         "weaponId": "weapon_laser_gun",
+@@ -12,10 +36,14 @@
+         "slot": "left",
+         "builtinToArm": true,
+         "requiresWeaponId": "robot_arm_assaultron",
+-        "modIds": ["mod_053"],
+         "ammo": {
+           "ammoId": "ammo_energy_cell",
+-          "quantity": { "base": 8, "rollType": "rollCD", "rollValue": 7, "op": "+" }
++          "quantity": {
++            "base": 8,
++            "rollType": "rollCD",
++            "rollValue": 7,
++            "op": "+"
++          }
+         }
+       },
+       {
+@@ -23,71 +51,155 @@
+         "items": [
+           {
+             "group": [
+-              { "armorId": "robot_frame_actuated_body", "itemType": "frame" },
+-              { "armorId": "robot_plating_standard_optics", "itemType": "plating", "hiddenInKitModal": true },
+-              { "armorId": "robot_plating_standard_arms", "itemType": "plating", "hiddenInKitModal": true },
+-              { "armorId": "robot_plating_standard_thruster", "itemType": "plating", "hiddenInKitModal": true }
++              {
++                "type": "fixed",
++                "armorId": "robot_frame_actuated_body",
++                "itemType": "frame"
++              }
+             ]
+           },
+           {
+             "group": [
+-              { "armorId": "robot_frame_actuated_arms", "itemType": "frame" },
+-              { "armorId": "robot_frame_actuated_thruster", "itemType": "frame" },
+-              { "armorId": "robot_plating_standard_optics", "itemType": "plating", "hiddenInKitModal": true },
+-              { "armorId": "robot_plating_standard_body", "itemType": "plating", "hiddenInKitModal": true }
++              {
++                "type": "fixed",
++                "armorId": "robot_frame_actuated_arms",
++                "itemType": "frame"
++              },
++              {
++                "type": "fixed",
++                "armorId": "robot_frame_actuated_thruster",
++                "itemType": "frame"
++              }
+             ]
+           }
+         ]
+       },
+-      { "type": "fixed", "itemId": "robot_module_recon_sensors", "itemType": "module" },
+-      { "type": "fixed", "itemType": "currency", "quantity": 15 }
++      {
++        "type": "fixed",
++        "itemId": "robot_module_recon_sensors",
++        "itemType": "module"
++      },
++      {
++        "type": "fixed",
++        "itemType": "currency",
++        "quantity": 15
++      }
+     ]
+   },
+   "assaultron_devil": {
+     "items": [
+-      { "type": "fixed", "itemId": "robot_head_assaultron", "itemType": "robotHead" },
+-      { "type": "fixed", "itemId": "robot_legs_assaultron", "itemType": "robotLeg", "slot": "left" },
+-      { "type": "fixed", "itemId": "robot_legs_assaultron", "itemType": "robotLeg", "slot": "right" },
+-      { "type": "fixed", "weaponId": "robot_weapon_construction_claw", "itemType": "weapon", "slot": "left" },
+-      { "type": "fixed", "weaponId": "robot_weapon_construction_claw", "itemType": "weapon", "slot": "right" },
+-      { "type": "fixed", "itemId": "robot_item_skull_mask", "itemType": "misc" },
++      {
++        "type": "fixed",
++        "itemId": "robot_head_assaultron_laser",
++        "itemType": "robotHead"
++      },
++      {
++        "type": "fixed",
++        "itemId": "robot_body_assaultron",
++        "itemType": "robotBody"
++      },
++      {
++        "type": "fixed",
++        "itemId": "robot_legs_assaultron",
++        "itemType": "robotLeg"
++      },
++      {
++        "type": "fixed",
++        "weaponId": "robot_weapon_construction_claw",
++        "itemType": "weapon",
++        "slot": "left"
++      },
++      {
++        "type": "fixed",
++        "weaponId": "robot_weapon_construction_claw",
++        "itemType": "weapon",
++        "slot": "right"
++      },
++      {
++        "type": "fixed",
++        "clothingId": "headwear_skull_mask",
++        "itemType": "clothing"
++      },
+       {
+         "type": "choice",
+         "items": [
+           {
+             "group": [
+-              { "armorId": "robot_plating_serrated_body", "itemType": "plating" },
+-              { "armorId": "robot_plating_standard_optics", "itemType": "plating", "hiddenInKitModal": true },
+-              { "armorId": "robot_plating_standard_arms", "itemType": "plating", "hiddenInKitModal": true },
+-              { "armorId": "robot_plating_standard_thruster", "itemType": "plating", "hiddenInKitModal": true }
++              {
++                "type": "fixed",
++                "armorId": "robot_plating_serrated_body",
++                "itemType": "plating"
++              }
+             ]
+           },
+           {
+             "group": [
+-              { "armorId": "robot_plating_serrated_arms", "itemType": "plating" },
+-              { "armorId": "robot_plating_serrated_thruster", "itemType": "plating" },
+-              { "armorId": "robot_plating_standard_optics", "itemType": "plating", "hiddenInKitModal": true },
+-              { "armorId": "robot_plating_standard_body", "itemType": "plating", "hiddenInKitModal": true }
++              {
++                "type": "fixed",
++                "armorId": "robot_plating_serrated_arms",
++                "itemType": "plating"
++              },
++              {
++                "type": "fixed",
++                "armorId": "robot_plating_serrated_thruster",
++                "itemType": "plating"
++              }
+             ]
+           }
+         ]
+       },
+-      { "type": "fixed", "itemId": "robot_module_hazard_detection", "itemType": "module" },
+       {
+         "type": "fixed",
+-        "itemType": "ammo",
++        "itemId": "robot_module_hazard_detection",
++        "itemType": "module"
++      },
++      {
++        "type": "fixed",
+         "ammoId": "ammo_energy_cell",
+-        "quantity": { "base": 6, "rollType": "rollCD", "rollValue": 6, "op": "+" }
++        "itemType": "ammo",
++        "quantity": {
++          "base": 6,
++          "rollType": "rollCD",
++          "rollValue": 6,
++          "op": "+"
++        }
+       },
+-      { "type": "fixed", "itemId": "robot_item_repair_kit", "itemType": "misc" }
++      {
++        "type": "fixed",
++        "itemId": "robot_item_repair_kit",
++        "itemType": "misc"
++      }
+     ]
+   },
+   "assaultron_caravan_guard": {
+     "items": [
+-      { "type": "fixed", "itemId": "robot_head_assaultron", "itemType": "robotHead" },
+-      { "type": "fixed", "itemId": "robot_legs_assaultron", "itemType": "robotLeg", "slot": "left" },
+-      { "type": "fixed", "itemId": "robot_legs_assaultron", "itemType": "robotLeg", "slot": "right" },
+-      { "type": "fixed", "itemId": "robot_arm_assaultron", "itemType": "robotArm", "slot": "right" },
++      {
++        "type": "fixed",
++        "itemId": "robot_head_assaultron_laser",
++        "itemType": "robotHead"
++      },
++      {
++        "type": "fixed",
++        "itemId": "robot_body_assaultron",
++        "itemType": "robotBody"
++      },
++      {
++        "type": "fixed",
++        "itemId": "robot_legs_assaultron",
++        "itemType": "robotLeg"
++      },
++      {
++        "type": "fixed",
++        "itemId": "robot_arm_assaultron",
++        "itemType": "robotArm",
++        "slot": "left"
++      },
++      {
++        "type": "fixed",
++        "itemId": "robot_arm_assaultron",
++        "itemType": "robotArm",
++        "slot": "right"
++      },
+       {
+         "type": "fixed",
+         "weaponId": "weapon_laser_gun",
+@@ -96,19 +208,54 @@
+         "slot": "left",
+         "builtinToArm": true,
+         "requiresWeaponId": "robot_arm_assaultron",
+-        "modIds": ["mod_053"],
+         "ammo": {
+           "ammoId": "ammo_energy_cell",
+-          "quantity": { "base": 14, "rollType": "rollCD", "rollValue": 7, "op": "+" }
++          "quantity": {
++            "base": 14,
++            "rollType": "rollCD",
++            "rollValue": 7,
++            "op": "+"
++          }
++        }
++      },
++      {
++        "type": "fixed",
++        "armorId": "robot_armor_storage_body",
++        "itemType": "armor"
++      },
++      {
++        "type": "fixed",
++        "armorId": "robot_armor_factory_thruster",
++        "itemType": "armor"
++      },
++      {
++        "type": "fixed",
++        "itemId": "robot_module_behavior_analysis",
++        "itemType": "module"
++      },
++      {
++        "type": "rollTable",
++        "tableId": "chem",
++        "roll": {
++          "rollType": "D20",
++          "count": 3,
++          "mode": "separate"
+         }
+       },
+-      { "type": "fixed", "armorId": "robot_armor_storage_body", "itemType": "armor" },
+-      { "type": "fixed", "armorId": "robot_armor_factory_thruster", "itemType": "armor" },
+-      { "type": "rollTable", "tableId": "ammo", "roll": { "rollType": "D20", "count": 3 } },
+-      { "type": "rollTable", "tableId": "chem", "roll": { "rollType": "D20", "count": 3 } },
+-      { "type": "rollTable", "tableId": "junk", "roll": { "rollType": "D20", "count": 3 } },
+-      { "type": "fixed", "itemId": "robot_module_behavior_analysis", "itemType": "module" },
+-      { "type": "fixed", "itemType": "currency", "quantity": { "base": 10, "rollType": "rollCD", "rollValue": 5, "op": "+" } }
++      {
++        "type": "rollTable",
++        "tableId": "trinklet",
++        "roll": {
++          "rollType": "D20",
++          "count": 3,
++          "mode": "separate"
++        }
++      },
++      {
++        "type": "fixed",
++        "itemType": "currency",
++        "quantity": 20
++      }
+     ]
+   }
+ }
+diff --git a/modules/fallout/data/equipmentKits/index.js b/modules/fallout/data/equipmentKits/index.js
+index 6ea9b7f..d7ba3d1 100644
+--- a/modules/fallout/data/equipmentKits/index.js
++++ b/modules/fallout/data/equipmentKits/index.js
+@@ -9,7 +9,6 @@
+ // ПРАВИЛО (владелец): новый комплект = новый файл (или запись в файле
+ // своей фракции) + строка импорта здесь. Движок не трогается.
+ 
+-import assaultron from './assaultron.json';
+ import brotherhood from './brotherhood.json';
+ import brotherhoodOutcast from './brotherhoodOutcast.json';
+ import childOfAtom from './childOfAtom.json';
+@@ -19,6 +18,7 @@ import minuteman from './minuteman.json';
+ import misterHandy from './misterHandy.json';
+ import ncr from './ncr.json';
+ import nightkin from './nightkin.json';
++import assaultron from './assaultron.json';
+ import protectron from './protectron.json';
+ import robobrain from './robobrain.json';
+ import securitron from './securitron.json';
+@@ -28,7 +28,6 @@ import wastelander from './wastelander.json';
+ import treefamilies from './treefamilies.json';
+ 
+ export default {
+-  ...assaultron,
+   ...brotherhood,
+   ...brotherhoodOutcast,
+   ...childOfAtom,
+@@ -38,6 +37,7 @@ export default {
+   ...misterHandy,
+   ...ncr,
+   ...nightkin,
++  ...assaultron,
+   ...protectron,
+   ...robobrain,
+   ...securitron,
+diff --git a/modules/fallout/data/origins/origins.json b/modules/fallout/data/origins/origins.json
+index 68efce5..6ab913f 100644
+--- a/modules/fallout/data/origins/origins.json
++++ b/modules/fallout/data/origins/origins.json
+@@ -170,7 +170,7 @@
+       "assaultron-designed-for-frontline"
+     ],
+     "equipmentKitIds": [
+-      "assaultron_us_army",
++      "assaultron_us_military",
+       "assaultron_devil",
+       "assaultron_caravan_guard"
+     ]
+diff --git a/modules/fallout/i18n/en-EN/data/equipment/armor/clothes.json b/modules/fallout/i18n/en-EN/data/equipment/armor/clothes.json
+index 02abf52..746071d 100644
+--- a/modules/fallout/i18n/en-EN/data/equipment/armor/clothes.json
++++ b/modules/fallout/i18n/en-EN/data/equipment/armor/clothes.json
+@@ -179,6 +179,10 @@
+         {
+           "id": "headwear_white_glove_mask",
+           "name": "White Glove Society Mask"
++        },
++        {
++          "id": "headwear_skull_mask",
++          "name": "Skull Mask"
+         }
+       ]
+     }
+diff --git a/modules/fallout/i18n/en-EN/data/system/equipmentKits.json b/modules/fallout/i18n/en-EN/data/system/equipmentKits.json
+index 5f2bf81..0510f01 100644
+--- a/modules/fallout/i18n/en-EN/data/system/equipmentKits.json
++++ b/modules/fallout/i18n/en-EN/data/system/equipmentKits.json
+@@ -68,6 +68,15 @@
+   "robobrain_nonordinary": {
+     "name": "Extraordinary Individual"
+   },
++  "assaultron_us_military": {
++    "name": "US Military Model"
++  },
++  "assaultron_devil": {
++    "name": "Assaultron Devil"
++  },
++  "assaultron_caravan_guard": {
++    "name": "Robotic Caravan Guard"
++  },
+   "default_caps_only": {
+     "name": "Starting Capital"
+   },
+@@ -124,14 +133,5 @@
+   },
+   "nightkin": {
+     "name": "NIGHTKIN"
+-  },
+-  "assaultron_us_army": {
+-    "name": "U.S. Military Model"
+-  },
+-  "assaultron_devil": {
+-    "name": "Assaultron Devil"
+-  },
+-  "assaultron_caravan_guard": {
+-    "name": "Robotic Caravan Guard"
+   }
+ }
+diff --git a/modules/fallout/i18n/ru-RU/data/equipment/armor/clothes.json b/modules/fallout/i18n/ru-RU/data/equipment/armor/clothes.json
+index 88ec902..45085fb 100644
+--- a/modules/fallout/i18n/ru-RU/data/equipment/armor/clothes.json
++++ b/modules/fallout/i18n/ru-RU/data/equipment/armor/clothes.json
+@@ -179,6 +179,10 @@
+         {
+           "id": "headwear_white_glove_mask",
+           "name": "Маска Общества «Белая перчатка»"
++        },
++        {
++          "id": "headwear_skull_mask",
++          "name": "Маска-череп"
+         }
+       ]
+     }
+diff --git a/modules/fallout/i18n/ru-RU/data/system/equipmentKits.json b/modules/fallout/i18n/ru-RU/data/system/equipmentKits.json
+index a708938..15cafe7 100644
+--- a/modules/fallout/i18n/ru-RU/data/system/equipmentKits.json
++++ b/modules/fallout/i18n/ru-RU/data/system/equipmentKits.json
+@@ -68,6 +68,15 @@
+   "robobrain_nonordinary": {
+     "name": "Неординарная личность"
+   },
++  "assaultron_us_military": {
++    "name": "Модель армии США"
++  },
++  "assaultron_devil": {
++    "name": "Штурмотрон-дьявол"
++  },
++  "assaultron_caravan_guard": {
++    "name": "Караванный охранник"
++  },
+   "default_caps_only": {
+     "name": "Стартовый капитал"
+   },
+@@ -124,14 +133,5 @@
+   },
+   "nightkin": {
+     "name": "Тень"
+-  },
+-  "assaultron_us_army": {
+-    "name": "Модель армии США"
+-  },
+-  "assaultron_devil": {
+-    "name": "Штурмотрон-Дьявол"
+-  },
+-  "assaultron_caravan_guard": {
+-    "name": "Охранник каравана"
+   }
+ }
+diff --git a/modules/fallout/screens/CharacterScreen/modals/EquipmentKitModal.js b/modules/fallout/screens/CharacterScreen/modals/EquipmentKitModal.js
+index 7b5286b..05f1340 100644
+--- a/modules/fallout/screens/CharacterScreen/modals/EquipmentKitModal.js
++++ b/modules/fallout/screens/CharacterScreen/modals/EquipmentKitModal.js
+@@ -9,13 +9,15 @@ import { useLocale, useModuleLocale } from '../../../../../i18n/locale';
+ import styles from '../../../../../styles/EquipmentKitModal.styles';
+ import { tCharacterScreen } from '../logic/characterScreenI18n';
+ 
+-// Lazy-load robot catalog data
++// Lazy-load robot catalog data — на основании данных, без обогатителя
+ const loadRobotCatalog = () => ({
+   heads: require('../../../data/equipment/robot/robotheads.json'),
+   bodies: require('../../../data/equipment/robot/robotbody.json'),
+   arms: require('../../../data/equipment/robot/robotarms.json'),
+   legs: require('../../../data/equipment/robot/robotlegs.json'),
+   weapons: require('../../../data/equipment/robot/weapons.json'),
++  plating: require('../../../data/equipment/robot/armor_plating.json').plating || [],
++  frames: require('../../../data/equipment/robot/frames.json').frames || [],
+ });
+ 
+ const META_CATEGORY_ORDER = ['structure', 'apparel', 'weapon', 'module', 'misc'];
+@@ -31,6 +33,8 @@ const STRUCTURE_SUBORDER = {
+   robotLegs: 3,
+   plating: 4,
+   frame: 5,
++  robotFrame: 5,
++  robotArmor: 6,
+ };
+ 
+ const STRUCTURE_TYPES = new Set(Object.keys(STRUCTURE_SUBORDER));
+@@ -300,7 +304,7 @@ const EquipmentKitModal = ({ visible, onClose, equipmentKits, onSelectKit, chara
+       // (initRobotSlots) — в инвентарь их копия не нужна.
+       const slotConsumedTypes = new Set([
+         'robotArm', 'robotHead', 'robotBody', 'robotLeg', 'robotLegs',
+-        'plating', 'armor', 'robotArmor', 'frame', 'module',
++        'plating', 'armor', 'robotArmor', 'frame', 'robotFrame', 'module',
+       ]);
+       const finalItemsOnly = finalItems.filter((item) => {
+         if (slotConsumedTypes.has(item.itemType)) return false;
+@@ -350,55 +354,11 @@ const EquipmentKitModal = ({ visible, onClose, equipmentKits, onSelectKit, chara
+     onClose();
+   };
+ 
+-  // BodyPlan = обязательная стандартная конструкция робота (как CPU/RAM
+-  // у компа — без них робот не работает). То, что в bodyplans.json:defaults
+-  // для данного bodyPlan, не является выбором игрока и НЕ показывается
+-  // в модалке. Инициализация фактических слотов идёт в initRobotSlots
+-  // (head/body/leg/arms автозаполняются), плюс сам кит может перечислить
+-  // альтернативы — тогда они и показываются, и экипируются.
+-  //
+-  // Логика: для каждого entry с itemType ∈ {robotHead, robotBody,
+-  // robotArm, robotLeg, robotLegs} проверяем, совпадает ли entry.itemId
+-  // с дефолтным для соответствующего слота в bodyPlan. Если совпадает —
+-  // это «обязательная часть конструкции», в модалке НЕ рендерим (как
+-  // «Голова Штурмотрона» в Дьяволе/Охраннике/УС Арми: дефолт, не выбор).
+-  // Если не совпадает — это нестандартная часть (например, Construction
+-  // Claw в Дьяволе вместо обычной клешни), её показываем.
+-  const LIMB_TYPES = new Set(['robotHead', 'robotBody', 'robotArm', 'robotLeg', 'robotLegs']);
+-
+-  // Определяем, к какому слоту bodyPlan относится entry. Если у entry
+-  // указан явный slot, используем его. Иначе — эвристика по itemType.
+-  const resolveLimbSlotKey = (entry) => {
+-    if (entry?.slot && ['leftArm', 'rightArm', 'arm1', 'arm2', 'arm3', 'leftLeg', 'rightLeg', 'head', 'body'].includes(entry.slot)) {
+-      return entry.slot;
+-    }
+-    if (entry?.itemType === 'robotHead') return 'head';
+-    if (entry?.itemType === 'robotBody') return 'body';
+-    if (entry?.itemType === 'robotLeg' || entry?.itemType === 'robotLegs') {
+-      return entry?.slot === 'left' ? 'leftLeg' : entry?.slot === 'right' ? 'rightLeg' : null;
+-    }
+-    return null;
+-  };
+-
+-  const isDefaultLimb = (entry, bodyPlanDefaults) => {
+-    if (!bodyPlanDefaults) return false;
+-    if (!LIMB_TYPES.has(entry?.itemType)) return false;
+-    const slotKey = resolveLimbSlotKey(entry);
+-    if (!slotKey) return false;
+-    return bodyPlanDefaults[slotKey] === entry?.itemId;
+-  };
+-
+   const getGroupedEntries = (kit) => {
+     const groups = {};
+-    // bodyPlan defaults — для фильтрации «дефолт = скрыть в модалке».
+-    const bodyPlanDefaults = isRobotCharacter(character)
+-      ? (getBodyPlan(resolveRobotBodyPlan(character))?.defaults || null)
+-      : null;
+ 
+     (kit.items || []).forEach((entry, index) => {
+       if (entry?.hiddenInKitModal) return;
+-      // Скрыть limb, совпадающий с bodyPlan default (см. isDefaultLimb).
+-      if (isDefaultLimb(entry, bodyPlanDefaults)) return;
+       const probe = entry?.type === 'choice' ? (entry.items || [])[0] : entry;
+       const meta = getMetaCategory(probe);
+       if (!groups[meta]) groups[meta] = [];
+@@ -454,13 +414,7 @@ const EquipmentKitModal = ({ visible, onClose, equipmentKits, onSelectKit, chara
+                                         const selected = selectedChoices[choiceKey] === optionKey;
+ 
+                                         const optionLabel = option.group
+-                                          // Элементы с hiddenInKitModal не объявляются в UI (дефолтные
+-                                          // Standard Plating, автозаполняемые тела и т.п.), но
+-                                          // попадают в слоты через option.group → choice → items.
+-                                          ? option.group
+-                                              .filter((g) => !g?.hiddenInKitModal)
+-                                              .map((groupItem) => `${getDisplayName(groupItem)}${formatBuiltinWeaponSuffix(groupItem)}`)
+-                                              .join(' + ')
++                                          ? option.group.map((groupItem) => `${getDisplayName(groupItem)}${formatBuiltinWeaponSuffix(groupItem)}`).join(' + ')
+                                           : `${getDisplayName(option)}${formatBuiltinWeaponSuffix(option)}`;
+ 
+                                         return (
