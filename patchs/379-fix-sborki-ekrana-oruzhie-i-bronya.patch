diff --git a/__tests__/screens/js-syntax-379.test.js b/__tests__/screens/js-syntax-379.test.js
new file mode 100644
index 0000000..f82c28b
--- /dev/null
+++ b/__tests__/screens/js-syntax-379.test.js
@@ -0,0 +1,48 @@
+// ПРИЁМОЧНЫЙ (патч 379): все JS-файлы приложения обязаны парситься.
+// В 367 при правке WeaponsAndArmorScreen продублировался хвост файла —
+// vitest его не импортировал, Metro упал только при сборке у владельца.
+// Заслон: babel-парс каждого .js (screens, логика, корень) в тесте.
+import { describe, expect, it } from 'vitest';
+import { readFileSync, readdirSync, statSync } from 'node:fs';
+import { join, dirname } from 'node:path';
+import { fileURLToPath } from 'node:url';
+import { parse } from '@babel/parser';
+
+const root = join(dirname(fileURLToPath(import.meta.url)), '..', '..');
+const targets = [join(root, 'modules', 'fallout'), join(root)];
+
+const walkJs = (dir, acc = [], depth = 0) => {
+  if (depth > 12) return acc;
+  for (const name of readdirSync(dir)) {
+    if (name === 'node_modules' || name === '.git' || name === '__tests__' || name.startsWith('.')) continue;
+    const p = join(dir, name);
+    const st = statSync(p);
+    if (st.isDirectory()) walkJs(p, acc, depth + 1);
+    else if (name.endsWith('.js')) acc.push(p);
+  }
+  return acc;
+};
+
+const seen = new Set();
+const files = targets
+  .flatMap((t) => walkJs(t))
+  .filter((f) => !seen.has(f) && seen.add(f))
+  .sort();
+
+describe('патч 379: синтаксис всех JS-файлов приложения', () => {
+  it('файлы найдены (защита от пустого прогона)', () => {
+    expect(files.length).toBeGreaterThan(50);
+  });
+
+  for (const file of files) {
+    it(file.slice(root.length + 1), () => {
+      expect(() =>
+        parse(readFileSync(file, 'utf8'), {
+          sourceType: 'module',
+          plugins: ['jsx', 'flow'],
+          allowReturnOutsideFunction: true,
+        }),
+      ).not.toThrow();
+    });
+  }
+});
diff --git a/docs/agents/state.md b/docs/agents/state.md
index ca92513..3edd94d 100644
--- a/docs/agents/state.md
+++ b/docs/agents/state.md
@@ -8,7 +8,11 @@
 > `docs/agents/README.md` и `docs/agents/data-flows.md`.
 
 Снимок: **2026-09-25** · ветка агента: `arena/01a0c8e2-fallout-2d20-companion`
-· последний сданный патч: **378** · у владельца закоммичено до **365**
+· последний сданный патч: **379** (срочный фикс сборки: в 367 при правке
+WeaponsAndArmorScreen продублировался хвост файла — vitest этот файл не
+импортировал и провала не видел; Metro у владельца упал. Хвост удалён,
+заведён заслон __tests__/screens/js-syntax-379.test.js — babel-парс
+всех 211 JS-файлов кода при каждом прогоне) · у владельца закоммичено до **365**
 (экспорты файлов патчей на main: 364+365); ревизия модов ЗАВЕРШЕНА:
 шаг 1 (375) канон-id + мост сейвов; шаг 2 (376) рецепты всем 164 +
 23 новых улучшения + Посох Атома и Шахтерский перфоратор; шаг 3 (377)
diff --git a/docs/changelog.en.md b/docs/changelog.en.md
index 0599704..6caa78b 100644
--- a/docs/changelog.en.md
+++ b/docs/changelog.en.md
@@ -10,6 +10,14 @@
 ---
 ---
 ---
+---
+## Fixed build of the Weapons & Armor screen (patch 379)
+
+The app failed to bundle: patch 367 accidentally duplicated a chunk of
+code past the end of the screen file. The extra 18 lines are removed.
+A new guard test parses every app JS file on each test run, so a broken
+file now fails tests immediately instead of only at build time.
+
 ## Melee mods completed — full owner coverage (patch 378)
 
 Final touch of the rebuild: 42 melee mods (spiked/barbed/bladed,
diff --git a/docs/changelog.ru.md b/docs/changelog.ru.md
index d287d24..336187d 100644
--- a/docs/changelog.ru.md
+++ b/docs/changelog.ru.md
@@ -1,6 +1,15 @@
 # История изменений
 
 ---
+## Исправлена сборка экрана «Оружие и броня» (патч 379)
+
+Приложение не собиралось: в патче 367 при доработке экрана случайно
+продублировался кусок кода после его конца. Лишние 18 строк удалены,
+экран собирается. Чтобы такое не повторилось, добавлена автопроверка:
+при каждом прогоне тестов каждый файл кода приложения прогоняется
+через парсер — испорченный файл теперь падает в тестах сразу, а не
+только при сборке.
+
 ## Мили-улучшения добиты — у каждого оружия полный ваш набор (патч 378)
 
 Финальный штрих пересборки: 42 улучшения ближнего боя (Шипы, Клинки,
diff --git a/modules/fallout/screens/WeaponsAndArmorScreen/WeaponsAndArmorScreen.js b/modules/fallout/screens/WeaponsAndArmorScreen/WeaponsAndArmorScreen.js
index 8ba0290..448ad12 100644
--- a/modules/fallout/screens/WeaponsAndArmorScreen/WeaponsAndArmorScreen.js
+++ b/modules/fallout/screens/WeaponsAndArmorScreen/WeaponsAndArmorScreen.js
@@ -1745,22 +1745,4 @@ const WeaponsAndArmorScreen = () => {
 };
 
 
-export default WeaponsAndArmorScreen;
-BodyModalText}>
-              {tWeaponsAndArmorScreen('robotBodyUpgrade.comingSoon')}
-            </Text>
-            <TouchableOpacity
-              style={localStyles.robotBodyModalButton}
-              onPress={() => setRobotBodyUpgradeModalVisible(false)}
-            >
-              <Text style={localStyles.robotBodyModalButtonText}>{tWeaponsAndArmorScreen('common.close')}</Text>
-            </TouchableOpacity>
-          </View>
-        </View>
-      </Modal>
-    </ImageBackground>
-  );
-};
-
-
 export default WeaponsAndArmorScreen;
diff --git a/public/version.json b/public/version.json
index c2907ad..0ebb612 100644
--- a/public/version.json
+++ b/public/version.json
@@ -1,5 +1,5 @@
 {
-  "version": "378",
+  "version": "379",
   "release": "1",
   "notes": {
     "ru-RU": [
