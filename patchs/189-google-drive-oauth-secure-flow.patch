diff --git a/components/cloudSync/googleDriveSync.js b/components/cloudSync/googleDriveSync.js
index 212c2f4..db26766 100644
--- a/components/cloudSync/googleDriveSync.js
+++ b/components/cloudSync/googleDriveSync.js
@@ -11,11 +11,15 @@ import { getActiveModuleId } from '../../domain/moduleLocale';
 // Внутри неё — подпапка на каждый сеттинг (модуль): fallout/, heroes/, dnd/ …
 const APP_DATA_FOLDER = 'appDataFolder';
 const SYNC_KEY = 'fallout_cloud_sync_enabled';
-const TOKEN_SCOPE = 'https://www.googleapis.com/auth/drive.file https://www.googleapis.com/auth/drive.metadata.readonly';
+const TOKEN_SCOPE = 'https://www.googleapis.com/auth/drive.file https://www.googleapis.com/auth/drive.appdata';
 const DRIVE_API = 'https://www.googleapis.com/drive/v3';
 const UPLOAD_API = 'https://www.googleapis.com/upload/drive/v3';
 
+// Токены держим только в памяти на время сессии вкладки, чтобы не просить вход
+// повторно при каждой синхронизации. На диск / в localStorage не пишем.
 let pendingToken = null;
+let cachedToken = null;
+let cachedTokenExpiry = 0;
 
 const getClientId = () => {
   if (typeof process !== 'undefined' && process?.env?.EXPO_PUBLIC_GOOGLE_DRIVE_CLIENT_ID) {
@@ -29,6 +33,7 @@ const getClientId = () => {
 
 const ensureWeb = () => Platform.OS === 'web' && typeof window !== 'undefined';
 
+// Загружаем Google Identity Services (GIS) — нужно для initTokenClient.
 const loadGoogleIdentityScript = async () => {
   if (!ensureWeb()) throw new Error('Cloud sync supports only web platform.');
   if (window.google?.accounts?.oauth2) return;
@@ -40,7 +45,6 @@ const loadGoogleIdentityScript = async () => {
       existing.addEventListener('error', () => reject(new Error('Failed to load Google Identity script.')), { once: true });
       return;
     }
-
     const script = document.createElement('script');
     script.src = 'https://accounts.google.com/gsi/client';
     script.async = true;
@@ -52,25 +56,49 @@ const loadGoogleIdentityScript = async () => {
   });
 };
 
+// Получение access_token через GIS Token Client (браузерный token flow).
+// В браузере «секрет» не храним: используем только client_id. Токен держим в
+// памяти на сессию, пока не истечёт expires_in. `prompt: ''` — доступ запрашивается
+// только один раз (а не на каждую синхронизацию). `state` генерируем на запрос
+// и сверяем в ответе (защита от CSRF).
 const requestAccessToken = async () => {
+  if (cachedToken && Date.now() < cachedTokenExpiry) return cachedToken;
+  if (pendingToken) return pendingToken;
+
   const clientId = getClientId();
   if (!clientId) {
     throw new Error('Google Drive client id is not configured. Set EXPO_PUBLIC_GOOGLE_DRIVE_CLIENT_ID.');
   }
-  if (pendingToken) return pendingToken;
+
+  // Простой уникальный state на один запрос (проверяется в callback).
+  const state = `${Math.random().toString(36).slice(2)}_${Date.now().toString(36)}`;
 
   pendingToken = new Promise((resolve, reject) => {
     const tokenClient = window.google.accounts.oauth2.initTokenClient({
       client_id: clientId,
       scope: TOKEN_SCOPE,
-      prompt: 'consent',
+      prompt: '',
+      state,
       callback: (resp) => {
-        if (resp?.access_token) resolve(resp.access_token);
-        else reject(new Error(resp?.error || 'Google auth failed.'));
+        if (resp?.error) {
+          reject(new Error(resp.error_description || resp.error));
+          return;
+        }
+        if (resp?.state !== state) {
+          reject(new Error('OAuth state mismatch (possible CSRF).'));
+          return;
+        }
+        if (!resp?.access_token) {
+          reject(new Error(resp?.error || 'Google auth failed.'));
+          return;
+        }
+        cachedToken = resp.access_token;
+        const ttl = Number(resp.expires_in || 3600) - 60;
+        cachedTokenExpiry = Date.now() + Math.max(ttl, 60) * 1000;
+        resolve(resp.access_token);
       },
-      error_callback: () => reject(new Error('Google auth failed.')),
+      error_callback: (err) => reject(new Error(`Google auth failed: ${err?.message || String(err) || 'unknown'}`)),
     });
-
     tokenClient.requestAccessToken();
   }).finally(() => {
     pendingToken = null;
@@ -79,6 +107,12 @@ const requestAccessToken = async () => {
   return pendingToken;
 };
 
+// Сбрасываем кэш токена (например, при 401), чтобы следующий вызов вошёл заново.
+export const clearCachedCloudToken = () => {
+  cachedToken = null;
+  cachedTokenExpiry = 0;
+};
+
 const driveFetch = async (token, url, options = {}) => {
   const response = await fetch(url, {
     ...options,
@@ -89,6 +123,9 @@ const driveFetch = async (token, url, options = {}) => {
   });
 
   if (!response.ok) {
+    // Протух/отозван токен — сбрасываем кэш, чтобы следующая синхронизация
+    // вошла в Google заново.
+    if (response.status === 401) clearCachedCloudToken();
     const text = await response.text();
     throw new Error(`Google Drive API error (${response.status}): ${text}`);
   }
diff --git a/docs/cloud-sync-setup.md b/docs/cloud-sync-setup.md
index 657e283..51649b4 100644
--- a/docs/cloud-sync-setup.md
+++ b/docs/cloud-sync-setup.md
@@ -15,6 +15,10 @@
 - `components/cloudSync/googleDriveSync.js` → вся логика Drive:
   - `getModuleFolderId` — ищет/создаёт подпапку сеттинга **внутри `appDataFolder`**;
   - `listRemoteCharacterFiles` / `downloadRemoteCharacter` / `uploadCharacterFile`;
+  - `requestAccessToken` — OAuth через **GIS Token Client** (браузерный token
+    flow: попап, только `client_id`, без `client_secret`). Доступ запрашивается
+    один раз (`prompt: ''`), токен кэшируется в памяти на время сессии;
+    при `401` кэш сбрасывается и вход повторяется;
   - `syncAllCharactersWithCloud` — кнопка в меню (полный обмен);
   - `syncCharacterToCloudIfEnabled` — вызывается на каждом автосейве персонажа
     (`CharacterContext`), т.е. изменил персонажа → ушло в Drive.
@@ -79,8 +83,12 @@ Google-аккаунта).
 3. Заполни название приложения и e-mail.
 4. **Scopes**: добавь
    - `.../auth/drive.file` (манипуляции с файлами, которые создаёт приложение);
-   - `.../auth/drive.metadata.readonly` (чтение метаданных списка файлов).
+   - `.../auth/drive.appdata` (доступ к скрытой папке приложения `appDataFolder`;
+     это **non-sensitive** scope — именно он нужен для облачных сейвов в
+     `appDataFolder`).
    Оба уже запрошены в `TOKEN_SCOPE` в `googleDriveSync.js`.
+   > Не используй `.../auth/drive.metadata.readonly` — это **sensitive**-scope и
+   > именно он вызывает плашку «приложение не проверено» / «Use secure flows».
 5. Пока приложение в **Testing** режиме — добавь **Test users**: свой Google
    аккаунт (иначе авторизация отбивает «Access blocked»).
 
@@ -98,9 +106,11 @@ Google-аккаунта).
 4. Нажми **Create** → получишь Client ID вида
    `808936097288-....apps.googleusercontent.com`. Он уже вшит в `index.html`.
 
-> Важно: для браузерного OAuth с `initTokenClient` используется только
-> **Client ID** (не секрет). Секрет нужен только для серверного потока — тут он
-> не используется, поэтому хранить в коде Client ID безопасно.
+> Важно: приложение использует **GIS Token Client** (браузерный token flow).
+> Поэтому в коде нужен только **Client ID**, а **client_secret не используется** —
+> он не вшит и не передаётся в браузере. Для токен-потока настраиваются только
+> **Authorized JavaScript origins** (Authorized redirect URIs не нужны).
+> Хранить Client ID в коде безопасно.
 
 ## 2. Как проверить, что работает
 
@@ -118,8 +128,9 @@ Google-аккаунта).
    (вызов `syncCharacterToCloudIfEnabled` на автосейве).
 
 Если появится `Google Drive API error (403)` — не включён Drive API или не
-добавлен scope/`Test user`. Если `401`/`origin mismatch` — неверный origin в
-Authorized JavaScript origins.
+добавлен scope/`Test user`. Если `401` — токен протух или отозван (приложение
+переавторизуется при следующей синхронизации). Если `origin mismatch` — неверный
+origin в **Authorized JavaScript origins**.
 
 ## 3. Про имя папки
 
