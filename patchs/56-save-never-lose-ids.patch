diff --git a/__tests__/securitron/character-reset-rules.test.js b/__tests__/securitron/character-reset-rules.test.js
index 330dc0f..0b4353b 100644
--- a/__tests__/securitron/character-reset-rules.test.js
+++ b/__tests__/securitron/character-reset-rules.test.js
@@ -11,7 +11,7 @@
 import { describe, it, expect } from 'vitest';
 import { isCharacterLocked } from '../../domain/characterCreation';
 import useCharacterStore from '../../src/store/characterStore';
-import { mergeEquipmentWithStore } from '../../src/store/migrations';
+import { mergeEquipmentWithStore, mergeEquippedWeapons } from '../../src/store/migrations';
 
 describe('isCharacterLocked (точка невозврата)', () => {
   it('не зафиксирован, пока ничего не распределено', () => {
@@ -91,3 +91,19 @@ describe('mergeEquipmentWithStore (сохранение комплекта)', ()
     expect(merged).toEqual({ items: [{ id: 'chem_stimpak' }] });
   });
 });
+
+describe('mergeEquippedWeapons (не теряем встроенное оружие)', () => {
+  it('объединяет снапшот и стор по id, без дублей', () => {
+    const snapshot = [{ id: 'unarmed_human', isBuiltin: true }, { id: 'weapon_laser_gun' }];
+    const store = [{ id: 'weapon_laser_gun' }, { id: 'weapon_missile_launcher' }];
+    const merged = mergeEquippedWeapons(snapshot, store);
+    expect(merged.map((w) => w.id).sort()).toEqual([
+      'unarmed_human', 'weapon_laser_gun', 'weapon_missile_launcher',
+    ]);
+  });
+
+  it('снапшот-метаданные (кулаки) не затираются', () => {
+    const merged = mergeEquippedWeapons([{ id: 'unarmed_human', isBuiltin: true }], []);
+    expect(merged).toEqual([{ id: 'unarmed_human', isBuiltin: true }]);
+  });
+});
diff --git a/components/CharacterContext.js b/components/CharacterContext.js
index 74d916e..b78b72a 100644
--- a/components/CharacterContext.js
+++ b/components/CharacterContext.js
@@ -72,7 +72,7 @@ import { Alert, Platform } from 'react-native';
 
 // Zustand Store integration (Task 4.1)
 import useCharacterStore from '../src/store/characterStore';
-import { denormalizeCharacterState, migrateCharacterState, mergeEquipmentWithStore } from '../src/store/migrations.js';
+import { denormalizeCharacterState, migrateCharacterState, mergeEquipmentWithStore, mergeEquippedWeapons } from '../src/store/migrations.js';
 import { CURRENT_SCHEMA_VERSION, LEGACY_SCHEMA_VERSION } from '../src/store/saveSchema.js';
 import { effectsDictToLegacyArray, syncTimedEffectsToStore } from '../src/store/effectsSync.js';
 
@@ -174,6 +174,11 @@ const preferFilled = (storeVal, snapshotVal) => {
   return storeVal;
 };
 
+/**
+ * Программа заточена на id: origin/trait/equipment — объекты с id, и их НИКОГДА
+ * нельзя затирать «голым» объектом без id из стора. Если стор-значение — объект
+ * без id, а снапшот имеет id — берём снапшот (метаданные), иначе preferFilled.
+ */
 const mergeSnapshotWithStoreData = (snapshot) => {
   const legacyData = denormalizeCharacterState(useCharacterStore.getState());
   return {
@@ -181,7 +186,7 @@ const mergeSnapshotWithStoreData = (snapshot) => {
     attributes: preferFilled(legacyData.attributes, snapshot.attributes),
     skills: preferFilled(legacyData.skills, snapshot.skills),
     equipment: mergeEquipmentWithStore(snapshot.equipment, legacyData.equipment),
-    equippedWeapons: preferFilled(legacyData.equippedWeapons, snapshot.equippedWeapons),
+    equippedWeapons: mergeEquippedWeapons(snapshot.equippedWeapons, legacyData.equippedWeapons),
     activeTimedEffects: preferFilled(legacyData.activeTimedEffects, snapshot.activeTimedEffects),
     rewardedSkills: legacyData.rewardedSkills,
   };
diff --git a/docs/changelog.en.md b/docs/changelog.en.md
index 723926e..92f49fa 100644
--- a/docs/changelog.en.md
+++ b/docs/changelog.en.md
@@ -21,6 +21,7 @@
 - New rule for changing origin/trait/kit: before attributes/skills are allocated (locked=false) switching is free, no warnings; the kit resets together with the origin. After locked, changing origin/trait does a full reset with confirmation; changing the kit resets inventory, skills and skill rewards (attributes are kept). Browsing lists is always free (the "trait already selected" block was removed).
 - Interface setting "Weapon cards display" (new "Interface Settings" section): Cards (default) / Spoilers / Tabs. Spoilers: each card sits in a collapsible header, 2/3 width, closed by default. Tabs: beveled top-left corner tabs below the limbs, active tab shows the 2/3-width card, << >> arrows switch the active tab, swiping left/right over the whole tabs scene (tab row + card) also switches it, and tapping a tab switches it too.
 - Fixed: the chosen equipment kit was not remembered between sessions (after reload the kit was not shown, clicking it "offered to reset"). Cause: snapshot merge preferred store items and lost the kit metadata (id/name). Now metadata comes from the snapshot, items from the store (bought/loot items are kept); the reset confirmation only appears when a kit is actually selected (has id).
+- Systemic save fix (the "lost ids" bug class): the snapshot no longer overwrites the chosen kit metadata or built-in weapons. Kit metadata (id/name) comes from the snapshot, items from the store (bought/loot are kept); built-in weapons (fists for humans) are merged by id with the store without duplicates. origin/trait are already stored by id (serializeState) — this bug class is closed at the root.
 - The "Mk II OS installed" flag is now stored in the character snapshot and restored on load (previously Mk II weapons became inoperable again after reload).
 
 ---
diff --git a/docs/changelog.ru.md b/docs/changelog.ru.md
index 3d3fd4d..3036072 100644
--- a/docs/changelog.ru.md
+++ b/docs/changelog.ru.md
@@ -23,6 +23,7 @@
 - Правило смены ориджина/трейта/комплекта: до распределения атрибутов/навыков (locked=false) смена свободна, без предупреждений; комплект сбрасывается вместе с ориджином. После locked смена ориджина/трейта — полный сброс с подтверждением; смена комплекта — сброс инвентаря, навыков и наград (атрибуты сохраняются). Просмотр списков всегда свободен (блок «Черта уже выбрана» убран).
 - Настройка интерфейса «Отображение карточек оружия» (раздел «Настройки интерфейса» в настройках): Карточки (по умолчанию) / Спойлеры / Табы. Спойлеры: каждая карточка в сворачиваемом заголовке, ширина 2/3, по умолчанию закрыты. Табы: табы со скошенным левым верхним углом под конечностями, активный таб показывает карточку 2/3 ширины, стрелки << >> меняют активный таб, свайп влево/вправо по всей сцене табов (строка + карточка) тоже, тап по табу тоже.
 - Исправлен баг: выбранный комплект снаряжения не сохранялся между сессиями (после перезагрузки комплект не отображался, клик «предлагал сбросить»). Причина: слияние снапшота отдавало предпочтение предметам стора и теряло метаданные комплекта (id/name). Теперь метаданные берутся из снапшота, предметы — из стора (купленные/лут не теряются); confirm сброса — только когда комплект реально выбран (есть id).
+- Системный фикс сохранения (класс багов «потерянные id»): снапшот больше не затирает метаданные выбранного комплекта и встроенное оружие. Метаданные комплекта (id/name) берутся из снапшота, предметы — из стора (купленные/лут не теряются); встроенное оружие (кулаки у людей) объединяется по id со стором без дублей. origin/trait уже хранятся по id (serializeState) — этот класс багов закрыт на корню.
 - Флаг «ОС Mk II установлена» теперь сохраняется в снапшот персонажа и восстанавливается при загрузке (раньше после перезагрузки Mk II оружие снова становилось нерабочим).
 
 ## 47 — Поддержка множественных типов урона
diff --git a/src/store/migrations.js b/src/store/migrations.js
index a185d47..3e3ae1e 100644
--- a/src/store/migrations.js
+++ b/src/store/migrations.js
@@ -612,3 +612,23 @@ export const mergeEquipmentWithStore = (snapshotEquipment, storeEquipment) => {
   }
   return { ...snapshotEquipment, items: storeItems };
 };
+
+/**
+ * Мерж списков оружия при сохранении снапшота: снапшот — источник метаданных
+ * (встроенные кулаки у людей и т.п.), стор — источник «живых» предметов.
+ * Объединяем по id без дублей.
+ */
+export const mergeEquippedWeapons = (snapshotWeapons, storeWeapons) => {
+  const snapshotList = Array.isArray(snapshotWeapons) ? snapshotWeapons : [];
+  const storeList = Array.isArray(storeWeapons) ? storeWeapons : [];
+  const seen = new Set();
+  const merged = [];
+  [...snapshotList, ...storeList].forEach((w) => {
+    if (!w) return;
+    const id = w.id || w.weaponId || w.uniqueId;
+    if (!id || seen.has(id)) return;
+    seen.add(id);
+    merged.push(w);
+  });
+  return merged;
+};
