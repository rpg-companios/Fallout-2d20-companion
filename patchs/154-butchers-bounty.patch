diff --git a/__tests__/perks/found-item-bonus.test.js b/__tests__/perks/found-item-bonus.test.js
index 81baa72..b43567a 100644
--- a/__tests__/perks/found-item-bonus.test.js
+++ b/__tests__/perks/found-item-bonus.test.js
@@ -10,6 +10,7 @@ afterEach(() => {
 const scrounger = perksData.find((perk) => perk.id === 'scrounger');
 const fortuneFinder = perksData.find((perk) => perk.id === 'fortuneFinder');
 const canOpener = perksData.find((perk) => perk.id === 'canOpener');
+const butchersBounty = perksData.find((perk) => perk.id === 'butchersBounty');
 
 describe('found item perk bonuses', () => {
   it('gives Scrounger extra ammo combat dice by rank', () => {
@@ -48,6 +49,13 @@ describe('found item perk bonuses', () => {
       { perkId: 'canOpener', itemType: 'food', extra: 1, match: { state: 'cooked' } },
     ]);
   });
+
+  it('gives Butcher\'s Bounty +1 raw meat', () => {
+    const result = calculatePerkEffects([butchersBounty], [{ id: 'butchersBounty', rank: 1 }]);
+    expect(result.bonuses.foundItemBonuses).toEqual([
+      { perkId: 'butchersBounty', itemType: 'food', extra: 1, match: { rawMeat: true } },
+    ]);
+  });
 });
 
 describe('rollFoundItemBonuses', () => {
diff --git a/domain/perks/butchersBounty.js b/domain/perks/butchersBounty.js
new file mode 100644
index 0000000..d61564b
--- /dev/null
+++ b/domain/perks/butchersBounty.js
@@ -0,0 +1,16 @@
+// domain/perks/butchersBounty.js
+// Добыча еды с rawMeat: true — к введённому количеству +1.
+
+export const butchersBountyPerk = {
+  id: 'butchersBounty',
+  apply() {
+    return {
+      foundItemBonuses: [{
+        perkId: 'butchersBounty',
+        itemType: 'food',
+        extra: 1,
+        match: { rawMeat: true },
+      }],
+    };
+  },
+};
diff --git a/domain/perks/index.js b/domain/perks/index.js
index 5391a3e..807d5ae 100644
--- a/domain/perks/index.js
+++ b/domain/perks/index.js
@@ -15,6 +15,7 @@ import { leadBellyPerk } from './leadBelly';
 import { scroungerPerk } from './scrounger';
 import { fortuneFinderPerk } from './fortuneFinder';
 import { canOpenerPerk } from './canOpener';
+import { butchersBountyPerk } from './butchersBounty';
 
 export const perkEffects = {
   [intenseTrainingPerk.id]: intenseTrainingPerk,
@@ -34,6 +35,7 @@ export const perkEffects = {
   [scroungerPerk.id]: scroungerPerk,
   [fortuneFinderPerk.id]: fortuneFinderPerk,
   [canOpenerPerk.id]: canOpenerPerk,
+  [butchersBountyPerk.id]: butchersBountyPerk,
 };
 
 export function getPerkEffect(effectId) {
