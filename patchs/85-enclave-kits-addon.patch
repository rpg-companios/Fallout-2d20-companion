diff -ruN '--exclude=.git' '--exclude=node_modules' baseA/__tests__/securitron/enclave-kits.test.js targetB/__tests__/securitron/enclave-kits.test.js
--- a/__tests__/securitron/enclave-kits.test.js	1970-01-01 00:00:00.000000000 +0000
+++ b/__tests__/securitron/enclave-kits.test.js	2026-08-13 05:49:13.787334682 +0000
@@ -0,0 +1,79 @@
+/**
+ * Комплекты «Осколка Анклава»: Бывший учёный / Бывший солдат.
+ */
+import { describe, it, expect, vi, beforeAll } from 'vitest';
+
+import { setCurrentLocale } from '../../i18n/locale';
+
+beforeAll(() => setCurrentLocale('ru-RU'));
+
+vi.mock('../../db/Database', async () => {
+  const catalog = await import('../../db/catalogSource');
+  return {
+    getWeaponById: async (id) => catalog.catalogGetWeaponById(id),
+    getWeaponModById: async (id) => catalog.catalogGetWeaponModById(id),
+    getAmmoById: async (id) => catalog.catalogGetAmmoById(id),
+    getItemByName: async (name) => catalog.catalogGetItemByName(name),
+  };
+});
+
+import { getEquipmentCatalogForLocale } from '../../domain/registry';
+import { resolveKitItems } from '../../domain/kitResolver';
+import moduleOrigins from '../../modules/fallout/data/origins.json';
+
+const KIT_IDS = ['enclave_scientist', 'enclave_soldier'];
+const getOrigin = () => moduleOrigins.find((o) => o.id === 'enclaveRemnant');
+
+describe('Комплекты Анклава: данные', () => {
+  it('ориджин ссылается на оба комплекта', () => {
+    expect(getOrigin().equipmentKitIds).toEqual(KIT_IDS);
+  });
+
+  it('имена комплектов локализованы', () => {
+    const catalog = getEquipmentCatalogForLocale('ru-RU');
+    expect(catalog.equipmentKits.enclave_scientist.name).toBe('Бывший учёный');
+    expect(catalog.equipmentKits.enclave_soldier.name).toBe('Бывший солдат');
+  });
+
+  it('учёный: халат, противогаз, лазерный пистолет и 6+3 энергоячейки', async () => {
+    const catalog = getEquipmentCatalogForLocale('ru-RU');
+    const resolved = await resolveKitItems({
+      id: 'enclave_scientist',
+      items: catalog.equipmentKits.enclave_scientist.items,
+    });
+    const ids = resolved.items.map((i) => i.clothingId || i.armorId || i.weaponId || i.itemId || i.id);
+    expect(ids).toContain('clothing_lab_coat');
+    expect(ids).toContain('headwear_gas_mask');
+    expect(ids).toContain('weapon_laser_gun');
+
+    const pistol = resolved.items.find((i) => i.weaponId === 'weapon_laser_gun');
+    expect(pistol).toBeDefined();
+    expect(pistol.resolvedAmmunition?.id).toBe('ammo_energy_cell');
+    expect(pistol.resolvedAmmunition?.quantity).toBeGreaterThanOrEqual(6);
+    expect(pistol.resolvedAmmunition?.quantity).toBeLessThanOrEqual(9);
+  });
+
+  it('солдат: военная униформа, нагрудник ББ, выбор лазерной винтовки/штурмовой винтовки с 8+4 боезапаса', async () => {
+    const catalog = getEquipmentCatalogForLocale('ru-RU');
+    const kit = catalog.equipmentKits.enclave_soldier;
+
+    // choice существует и предлагает ровно два варианта оружия
+    const choice = kit.items.find((e) => e.type === 'choice');
+    expect(choice).toBeDefined();
+    expect(choice.items).toHaveLength(2);
+    const optionIds = choice.items.map((o) => o.weaponId);
+    expect(optionIds).toContain('weapon_laser_gun');
+    expect(optionIds).toContain('weapon_assault_rifle');
+
+    // Резолв лазерной винтовки (пистолет + ствольная коробка/ложа mod_058, ствол mod_054)
+    const laserOption = choice.items.find((o) => o.weaponId === 'weapon_laser_gun');
+    expect(laserOption.modIds).toEqual(['mod_058', 'mod_054']);
+
+    const resolved = await resolveKitItems({ id: 'enclave_soldier', items: kit.items });
+    const ids = resolved.items.map((i) => i.clothingId || i.armorId || i.weaponId || i.itemId || i.id);
+    expect(ids).toContain('clothing_military_fatigues');
+    expect(ids).toContain('armor_combat_chest_001');
+    // один из вариантов оружия попал в комплект (по умолчанию choice — первый)
+    expect(ids.some((id) => id === 'weapon_laser_gun' || id === 'weapon_assault_rifle')).toBe(true);
+  });
+});
diff -ruN '--exclude=.git' '--exclude=node_modules' baseA/__tests__/securitron/enclave-remnant-origin.test.js targetB/__tests__/securitron/enclave-remnant-origin.test.js
--- a/__tests__/securitron/enclave-remnant-origin.test.js	1970-01-01 00:00:00.000000000 +0000
+++ b/__tests__/securitron/enclave-remnant-origin.test.js	2026-08-13 05:49:13.783334682 +0000
@@ -0,0 +1,84 @@
+/**
+ * Ориджин «Осколок Анклава» (enclaveRemnant).
+ *
+ * Содержимое (трейт «Скрытный и преследуемый», доп. навык Скрытность/Выживание,
+ * 100 стартовых крышек) живёт в модуле modules/fallout. Движковые механики
+ * (репутация, враждебность, тайники за очки удачи, проверка тайника) будут
+ * добавлены отдельно — тут проверяем только то, что уже реализовано данными.
+ */
+import { describe, it, expect, vi, beforeAll } from 'vitest';
+
+import { setCurrentLocale } from '../../i18n/locale';
+
+beforeAll(() => {
+  setCurrentLocale('ru-RU');
+});
+
+vi.mock('../../db/Database', async () => {
+  const catalog = await import('../../db/catalogSource');
+  return {
+    getWeaponById: async (id) => catalog.catalogGetWeaponById(id),
+    getWeaponModById: async (id) => catalog.catalogGetWeaponModById(id),
+    getAmmoById: async (id) => catalog.catalogGetAmmoById(id),
+    getItemByName: async (name) => catalog.catalogGetItemByName(name),
+  };
+});
+
+import { getOrigins, getTraits, getOriginI18n, getTraitI18n, getEquipmentCatalogForLocale } from '../../domain/registry';
+import moduleOrigins from '../../modules/fallout/data/origins.json';
+import moduleTraits from '../../modules/fallout/data/traits.json';
+
+const ORIGIN_ID = 'enclaveRemnant';
+const TRAIT_ID = 'enclaveRemnant-hidden';
+
+const getOrigin = () => getOrigins().find((o) => o.id === ORIGIN_ID);
+const getTrait = (id = TRAIT_ID) => getTraits().find((t) => t.id === id);
+
+describe('Ориджин «Осколок Анклава»: данные (в модуле)', () => {
+  it('ориджин лежит в модуле и читается реестром', () => {
+    expect(moduleOrigins.some((o) => o.id === ORIGIN_ID)).toBe(true);
+    const origin = getOrigin();
+    expect(origin).toBeDefined();
+    expect(origin.characterType).toBe('human');
+    expect(origin.bodyPlan).toBe('humanoid');
+    expect(origin.traitIds).toEqual([TRAIT_ID]);
+    // Снаряжение сейчас — стартовый капс (100 крышек); новый комплект будет позже.
+    expect(origin.equipmentKitIds).toEqual(['default_caps_only']);
+  });
+
+  it('трейт «Скрытный и преследуемый» даёт выбор 1 доп. навыка из Скрытность/Выживание', () => {
+    expect(moduleTraits.some((t) => t.id === TRAIT_ID)).toBe(true);
+    const trait = getTrait();
+    expect(trait.originId).toBe(ORIGIN_ID);
+    expect(trait.modifiers.skillPickChoice).toEqual({
+      count: 1,
+      from: ['SNEAK', 'SURVIVAL'],
+    });
+  });
+
+  it('имена/описания локализованы (ru/en), без сырых ключей', () => {
+    const ruName = getOriginI18n('ru-RU')[ORIGIN_ID];
+    const enName = getOriginI18n('en-EN')[ORIGIN_ID];
+    expect(ruName).toBe('Осколок Анклава');
+    expect(enName).toBe('Enclave Remnant');
+
+    const ruTrait = getTraitI18n('ru-RU').traits?.enclaveRemnant?.hidden;
+    const enTrait = getTraitI18n('en-EN').traits?.enclaveRemnant?.hidden;
+    expect(ruTrait?.name).toBe('Скрытный и преследуемый');
+    expect(typeof ruTrait?.description).toBe('string');
+    expect(ruTrait.description.length).toBeGreaterThan(20);
+    expect(enTrait?.name).toBe('Hidden and Hunted');
+    expect(typeof enTrait?.description).toBe('string');
+  });
+
+  it('стартовый комплект существует и выдаёт 100 крышек (currency)', async () => {
+    const { resolveKitItems } = await import('../../domain/kitResolver');
+    const catalog = getEquipmentCatalogForLocale('ru-RU');
+    const kit = catalog.equipmentKits.default_caps_only;
+    expect(kit).toBeDefined();
+    const resolved = await resolveKitItems({ id: 'default_caps_only', items: kit.items });
+    const caps = resolved.items.find((i) => i.itemType === 'currency');
+    expect(caps).toBeDefined();
+    expect(caps.quantity).toBe(100);
+  });
+});
diff -ruN '--exclude=.git' '--exclude=node_modules' baseA/modules/fallout/data/equipmentKits/enclave.json targetB/modules/fallout/data/equipmentKits/enclave.json
--- a/modules/fallout/data/equipmentKits/enclave.json	1970-01-01 00:00:00.000000000 +0000
+++ b/modules/fallout/data/equipmentKits/enclave.json	2026-08-13 05:49:13.743334682 +0000
@@ -0,0 +1,76 @@
+{
+  "enclave_scientist": {
+    "items": [
+      {
+        "type": "fixed",
+        "clothingId": "clothing_lab_coat",
+        "itemType": "clothing"
+      },
+      {
+        "type": "fixed",
+        "clothingId": "headwear_gas_mask",
+        "itemType": "clothing"
+      },
+      {
+        "type": "fixed",
+        "weaponId": "weapon_laser_gun",
+        "itemType": "weapon",
+        "ammo": {
+          "ammoId": "ammo_energy_cell",
+          "quantity": {
+            "base": 6,
+            "rollType": "rollCD",
+            "rollValue": 3,
+            "op": "+"
+          }
+        }
+      }
+    ]
+  },
+  "enclave_soldier": {
+    "items": [
+      {
+        "type": "fixed",
+        "clothingId": "clothing_military_fatigues",
+        "itemType": "clothing"
+      },
+      {
+        "type": "fixed",
+        "armorId": "armor_combat_chest_001",
+        "itemType": "armor"
+      },
+      {
+        "type": "choice",
+        "items": [
+          {
+            "weaponId": "weapon_laser_gun",
+            "itemType": "weapon",
+            "modIds": ["mod_058", "mod_054"],
+            "ammo": {
+              "ammoId": "ammo_energy_cell",
+              "quantity": {
+                "base": 8,
+                "rollType": "rollCD",
+                "rollValue": 4,
+                "op": "+"
+              }
+            }
+          },
+          {
+            "weaponId": "weapon_assault_rifle",
+            "itemType": "weapon",
+            "ammo": {
+              "ammoId": "ammo_5_56mm",
+              "quantity": {
+                "base": 8,
+                "rollType": "rollCD",
+                "rollValue": 4,
+                "op": "+"
+              }
+            }
+          }
+        ]
+      }
+    ]
+  }
+}
diff -ruN '--exclude=.git' '--exclude=node_modules' baseA/modules/fallout/data/equipmentKits/index.js targetB/modules/fallout/data/equipmentKits/index.js
--- a/modules/fallout/data/equipmentKits/index.js	2026-08-13 05:48:50.371334682 +0000
+++ b/modules/fallout/data/equipmentKits/index.js	2026-08-13 05:49:13.751334682 +0000
@@ -13,6 +13,7 @@
 import brotherhoodOutcast from './brotherhoodOutcast.json';
 import childOfAtom from './childOfAtom.json';
 import defaultKit from './default.json';
+import enclave from './enclave.json';
 import minuteman from './minuteman.json';
 import misterHandy from './misterHandy.json';
 import ncr from './ncr.json';
@@ -29,6 +30,7 @@
   ...brotherhoodOutcast,
   ...childOfAtom,
   ...defaultKit,
+  ...enclave,
   ...minuteman,
   ...misterHandy,
   ...ncr,
diff -ruN '--exclude=.git' '--exclude=node_modules' baseA/modules/fallout/data/origins.json targetB/modules/fallout/data/origins.json
--- a/modules/fallout/data/origins.json	2026-08-13 05:48:50.371334682 +0000
+++ b/modules/fallout/data/origins.json	2026-08-13 05:49:13.739334682 +0000
@@ -13,5 +13,18 @@
       "treefamilies_omerta",
       "treefamilies_white_glove"
     ]
+  },
+  {
+    "id": "enclaveRemnant",
+    "displayNameKey": "origins.enclaveRemnant.name",
+    "characterType": "human",
+    "bodyPlan": "humanoid",
+    "traitIds": [
+      "enclaveRemnant-hidden"
+    ],
+    "equipmentKitIds": [
+      "enclave_scientist",
+      "enclave_soldier"
+    ]
   }
-]
\ No newline at end of file
+]
diff -ruN '--exclude=.git' '--exclude=node_modules' baseA/modules/fallout/i18n/en-EN.json targetB/modules/fallout/i18n/en-EN.json
--- a/modules/fallout/i18n/en-EN.json	2026-08-13 05:48:50.371334682 +0000
+++ b/modules/fallout/i18n/en-EN.json	2026-08-13 05:49:13.779334682 +0000
@@ -1,6 +1,7 @@
 {
   "origins": {
-    "TreeFamilies": "Strip Families"
+    "TreeFamilies": "Strip Families",
+    "enclaveRemnant": "Enclave Remnant"
   },
   "traits": {
     "treeFamilies": {
@@ -20,6 +21,12 @@
         "name": "White Glove Society",
         "description": "Food you cook at a Cooking Station restores 1 more HP. When you butcher a corpse for meat, you gain one additional piece. Charisma (CHA) skill tests involving those outside the White Glove Society increase their Complication range by 1."
       }
+    },
+    "enclaveRemnant": {
+      "hidden": {
+        "name": "Hidden and Hunted",
+        "description": "Tag one additional skill of your choice: **Sneak** or **Survival**.\n\nThe NCR and the Brotherhood of Steel hunt war criminals like you:\n- the difficulty of reputation tests with the NCR and Brotherhood of Steel is **increased by 2**;\n- if you are exposed, both factions become **hostile**.\n\nOnce per quest (or game session), you may spend **1 Luck point** to locate one of your caches and claim its contents.\n\nIf you have been exposed, upon reaching a cache make a **LCK + Sneak** test with difficulty 1. On a failure, your cache is compromised."
+      }
     }
   },
   "weapons": [
@@ -163,6 +170,12 @@
     },
     "treefamilies_white_glove": {
       "name": "White Glove Society"
+    },
+    "enclave_scientist": {
+      "name": "Former Scientist"
+    },
+    "enclave_soldier": {
+      "name": "Former Soldier"
     }
   },
   "uniqQualities": [
@@ -1909,4 +1922,4 @@
       "effectDescription": "plus 1 Range"
     }
   ]
-}
\ No newline at end of file
+}
diff -ruN '--exclude=.git' '--exclude=node_modules' baseA/modules/fallout/i18n/ru-RU.json targetB/modules/fallout/i18n/ru-RU.json
--- a/modules/fallout/i18n/ru-RU.json	2026-08-13 05:48:50.371334682 +0000
+++ b/modules/fallout/i18n/ru-RU.json	2026-08-13 05:49:13.775334682 +0000
@@ -1,6 +1,7 @@
 {
   "origins": {
-    "TreeFamilies": "Три семьи"
+    "TreeFamilies": "Три семьи",
+    "enclaveRemnant": "Осколок Анклава"
   },
   "traits": {
     "treeFamilies": {
@@ -20,6 +21,12 @@
         "name": "Общество «Белая перчатка»",
         "description": "Еда, которую вы готовите на Пункте приготовления пищи, восстанавливает на 1 ОЗ больше. Когда вы разделываете труп для получения мяса, вы получаете один дополнительный кусок. Проверки навыков на основе Харизмы (ХАР), связанные с теми, кто не входит в Общество «Белая перчатка», увеличивают свой Диапазон осложнений на 1."
       }
+    },
+    "enclaveRemnant": {
+      "hidden": {
+        "name": "Скрытный и преследуемый",
+        "description": "Отметьте один дополнительный навык на выбор: **Скрытность** или **Выживание**.\n\nНКР и Братство Стали разыскивают военных преступников вроде вас:\n- сложность проверок на репутацию с НКР и Братством Стали **повышена на 2**;\n- если вас раскроют, обе фракции становятся **враждебны**.\n\nОдин раз за квест (или игровой сеанс) вы можете потратить **1 очко удачи**, чтобы найти один из своих тайников и забрать его содержимое.\n\nЕсли вас раскрыли, по прибытии к тайнику пройдите проверку **УДА + Скрытность** со сложностью 1. В случае провала ваш тайник раскрыт."
+      }
     }
   },
   "weapons": [
@@ -163,6 +170,12 @@
     },
     "treefamilies_white_glove": {
       "name": "Общество «Белая перчатка»"
+    },
+    "enclave_scientist": {
+      "name": "Бывший учёный"
+    },
+    "enclave_soldier": {
+      "name": "Бывший солдат"
     }
   },
   "uniqQualities": [
@@ -1908,4 +1921,4 @@
       "effectDescription": "+1 дистанция"
     }
   ]
-}
\ No newline at end of file
+}
