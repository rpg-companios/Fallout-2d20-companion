# RPG Companion

## Equipment catalog source of truth

The canonical equipment catalog is stored in `i18n/<locale>/*.json` and is loaded through `i18n/equipmentCatalog.js`.

`assets/Equipment/*` JSON duplicates were removed to avoid divergent data and to keep a single source of truth.

