// Числа правил крафта — настройки пакета Fallout (патч 251).
//
// По образцу SURVIVAL_RULES: это НЕ поля рецептов и не сейв — модульные числа,
// одинаковые для всех рецептов и правящиеся без перегенерации данных. Универсальный
// движок (domain/craftingEngine.js) их не знает: он получает их значения через
// адаптер операций, когда сеттинг решает, как применять правило.

export const CRAFT_RULES = {
  // Книга: проверка крафта — тест атрибута + навыка, конкретный атрибут
  // печатная таблица не закрепляет (воля ГМ). По умолчанию берём INT —
  // правится здесь, данные рецептов не трогаются.
  testAttribute: 'INT',
  // «При провале на кухне и в химии материалы сгорают» (с. 210–211). На других
  // верстаках провал — только сорванная работа (время — за патчем часов).
  failBurnsMaterialsByBench: { cooking: true, chemistry: true, default: false },
};
