// Числа и правила разбора — настройки пакета Fallout (патч 260).
//
// По образцу CRAFT_RULES/SURVIVAL_RULES: это не поля предметов и не сейв —
// модульные числа, правятся здесь, данные каталога не трогаются. Универсальный
// движок (domain/salvageEngine.js) их не знает: получает через адаптер.
//
// Источник правил — таблицы разбора владельца 2026-09-16 (книга, с. 212–214).

// Определения разборного мира (канон владельца, 2026-09-17):
//  1. Хлам — всё, что разбирается на конечные материалы.
//  2. Материалы — ингредиенты крафта.
//  3. Материалы обладают редкостью (common/unusual/rare — потолок «Мусорщика»).
//  4. Материал может жить отдельно от хлама или быть его содержимым: кристалл —
//     и материал, и строка состава графина/руды.
//  5. Материалы НЕ разбираются. Пункт закреплён гардом в operations
//     (isScrapMaterial) — раньше проверки типа и состава.
export const SALVAGE_RULES = {
  // Проверка разбора: тест ИНТ + Ремонт, сложность 0. Провал возможен только
  // через осложнения (правила d20-проверки); при провале предмет НЕ расходуется.
  checkAttribute: 'INT',
  checkSkill: 'REPAIR',
  difficulty: 0,
  // Хлам разборчивается за 10 минут на предмет. С патча 262 минуты идут через
  // часы выживания (applyActivityMinutes): дробь копится в timeCarried, шесть
  // разборов складываются в игровой час.
  minutesPerItem: 10,
  // То же правило 259 для крафта: осложнение успех не отменяет — помножает время.
  complicationDurationMultiplier: 2,
  // Разбирается только хлам, если не указано иного (решение владельца
  // 2026-09-17). «Иное» = у предмета есть печатный состав разбора (строка
  // таблицы, как у радио) — он разбирается, даже лежа в чужом типе.
  // Всё остальное (оружие, броня, расходники, боеприпасы, предметы общего
  // каталога) — на отказе. Расходники и боеприпасы состава не имеют, поэтому
  // отдельный чёрный список не нужен: их отсекает это же правило.
  salvageableItemType: 'junk',
  // Хлам БЕЗ печатного состава (наследованные добавления владельца — желёзы,
  // жало) идёт по общему правилу выхода: один материал Common; «Мусорщик»
  // поднимает потолок редкости (1 ранг — необычные тоже, 2 ранг — и редкие).
  // Клей и Масло не выпадают никогда.
  // С 263 тот же потолок режет и печатный состав (решение владельца): редкий
  // материал без «Мусорщика» из хлама не вынуть — строка не бросается; сверх
  // состава ничего не надбавляется (нет unusual в хламе — и при ранге 2 пусто).
  scrapperPerkId: 'scrapper',
  scrapperUncommonRank: 1,
  scrapperRareRank: 2,
  nonJunkExcludedMaterials: ['adhesive', 'oil'],
  // Надбавка «+1 единица за ОД» держится в контракте движка, но не считается:
  // счётчика ОД нет и после подключения времени (262) — тратятся только часы.
  apBonusPerItem: 0,
};
