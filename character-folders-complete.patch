diff --git a/__tests__/character-folders/database-contract.test.js b/__tests__/character-folders/database-contract.test.js
new file mode 100644
index 0000000..234939f
--- /dev/null
+++ b/__tests__/character-folders/database-contract.test.js
@@ -0,0 +1,26 @@
+import { describe, expect, it } from 'vitest';
+import fs from 'node:fs';
+
+const database = fs.readFileSync(new URL('../../db/Database.js', import.meta.url), 'utf8');
+const webDatabase = fs.readFileSync(new URL('../../db/Database.web.js', import.meta.url), 'utf8');
+const schema = fs.readFileSync(new URL('../../db/schema.js', import.meta.url), 'utf8');
+
+describe('character folder persistence contract', () => {
+  it('defines the folder entities in the shared schema', () => {
+    expect(schema).toContain('CREATE TABLE IF NOT EXISTS character_folders');
+    expect(schema).toContain('CREATE TABLE IF NOT EXISTS character_folder_memberships');
+  });
+
+  it.each([database, webDatabase])('exposes the documented operations in both adapters', (source) => {
+    [
+      'getCharacterFolders', 'createCharacterFolder', 'renameCharacterFolder',
+      'deleteCharacterFolderAndCharacters', 'getCharacterFolderId', 'moveCharacterToFolder',
+      'getRootCharactersList', 'getCharactersInFolder',
+    ].forEach((operation) => expect(source).toContain(`function ${operation}`));
+  });
+
+  it('removes a character membership when the character is deleted', () => {
+    expect(database).toContain('DELETE FROM character_folder_memberships WHERE character_id = ?');
+    expect(webDatabase).toContain('DELETE FROM character_folder_memberships WHERE character_id = ?');
+  });
+});
diff --git a/components/screens/HomeScreen/HomeScreen.js b/components/screens/HomeScreen/HomeScreen.js
index d0be9aa..4f05b9c 100644
--- a/components/screens/HomeScreen/HomeScreen.js
+++ b/components/screens/HomeScreen/HomeScreen.js
@@ -11,6 +11,7 @@ import {
   Platform,
   Modal,
   Linking,
+  TextInput,
 } from 'react-native';
 import { MaterialCommunityIcons, FontAwesome5 } from '@expo/vector-icons';
 import { useFocusEffect } from '@react-navigation/native';
@@ -30,6 +31,7 @@ import { openCloudFolderInDrive, syncAllCharactersWithCloud } from '../../cloudS
 import { forcePwaUpdate } from '../../../src/utils/forcePwaUpdate';
 import styles from '../../../styles/HomeScreen.styles';
 import SettingsModal from '../../settings/SettingsModal';
+import useAppSettingsStore from '../../../src/store/appSettingsStore';
 
 const getOriginImage = (originName) => {
   if (!originName) return null;
@@ -39,14 +41,14 @@ const getOriginImage = (originName) => {
 
 const NUM_COLS = 3;
 
-const ActionCell = ({ icon, label, onPress }) => (
-  <TouchableOpacity style={styles.createCell} onPress={onPress} activeOpacity={0.7}>
-    <Text style={styles.createPlus}>{icon}</Text>
+const ActionCell = ({ icon, label, onPress, disabled = false }) => (
+  <TouchableOpacity style={[styles.createCell, disabled && styles.disabledCell]} onPress={onPress} disabled={disabled} activeOpacity={0.7}>
+    {typeof icon === 'string' ? <Text style={styles.createPlus}>{icon}</Text> : icon}
     <Text style={styles.createLabel}>{label}</Text>
   </TouchableOpacity>
 );
 
-const CharacterCell = ({ character, onPress, onDelete, onDownload }) => {
+const CharacterCell = ({ character, onPress, onDelete, onDownload, onMoveStart, moving }) => {
   const originImage = getOriginImage(character.originName);
   return (
     <TouchableOpacity
@@ -54,6 +56,8 @@ const CharacterCell = ({ character, onPress, onDelete, onDownload }) => {
       onPress={onPress}
       activeOpacity={0.8}
     >
+      <TouchableOpacity style={styles.dragHandle} onLongPress={onMoveStart} delayLongPress={250} onPress={(event) => event?.stopPropagation?.()}><MaterialCommunityIcons name="arrow-all" size={18} color="#111827" /></TouchableOpacity>
+      {moving && <View style={styles.draggingOverlay}><Text style={styles.draggingText}>↕</Text></View>}
       <View style={styles.characterImageContainer}>
         {originImage ? (
           <Image source={originImage} style={styles.characterImage} resizeMode="cover" />
@@ -98,6 +102,13 @@ export default function HomeScreen({ navigation }) {
   const locale = useLocale();
   const { getCharactersList, loadCharacter, resetCharacter, deleteCharacter } = useCharacter();
   const [characters, setCharacters] = useState([]);
+  const [folders, setFolders] = useState([]);
+  const [folderCounts, setFolderCounts] = useState({});
+  const [folderDraftVisible, setFolderDraftVisible] = useState(false);
+  const [folderName, setFolderName] = useState('');
+  const [activeFolder, setActiveFolder] = useState(null);
+  const [movingCharacterId, setMovingCharacterId] = useState(null);
+  const characterFoldersEnabled = useAppSettingsStore((state) => state.characterFoldersEnabled);
   const [loading, setLoading] = useState(true);
   const [menuVisible, setMenuVisible] = useState(false);
   const [aboutVisible, setAboutVisible] = useState(false);
@@ -176,15 +187,20 @@ export default function HomeScreen({ navigation }) {
   const loadList = useCallback(async () => {
     setLoading(true);
     try {
-      const list = await getCharactersList();
-      list.sort((a, b) => (b.createdAt || 0) - (a.createdAt || 0));
-      setCharacters(list);
+      const savedFolders = await db.getCharacterFolders();
+      const folderLists = await Promise.all(savedFolders.map((folder) => db.getCharactersInFolder(folder.id)));
+      const counts = Object.fromEntries(savedFolders.map((folder, index) => [folder.id, folderLists[index].length]));
+      const visible = activeFolder ? folderLists[savedFolders.findIndex((folder) => folder.id === activeFolder.id)] || [] : await db.getRootCharactersList();
+      visible.sort((a, b) => (b.createdAt || 0) - (a.createdAt || 0));
+      setCharacters(visible);
+      setFolders(savedFolders);
+      setFolderCounts(counts);
     } catch (e) {
       setCharacters([]);
     } finally {
       setLoading(false);
     }
-  }, [getCharactersList]);
+  }, [getCharactersList, activeFolder]);
 
   useFocusEffect(
     useCallback(() => {
@@ -192,6 +208,35 @@ export default function HomeScreen({ navigation }) {
     }, [loadList])
   );
 
+  const handleCreateFolder = async () => {
+    if (!folderName.trim()) return;
+    await db.createCharacterFolder(folderName);
+    setFolderName('');
+    setFolderDraftVisible(false);
+    loadList();
+  };
+
+  const handleMoveToFolder = async (folderId) => {
+    if (!movingCharacterId) return;
+    await db.moveCharacterToFolder(movingCharacterId, folderId);
+    setMovingCharacterId(null);
+    loadList();
+  };
+
+  const handleDeleteFolder = (folder) => {
+    const remove = async () => {
+      await db.deleteCharacterFolderAndCharacters(folder.id);
+      if (activeFolder?.id === folder.id) setActiveFolder(null);
+      loadList();
+    };
+    const message = `${tHomeScreen('folders.deleteMessage')} ${tHomeScreen('folders.characters')}: ${folderCounts[folder.id] || 0}.`;
+    if (Platform.OS === 'web' && typeof window !== 'undefined') {
+      if (window.confirm(message)) remove();
+      return;
+    }
+    Alert.alert(tHomeScreen('folders.deleteTitle'), message, [{ text: tHomeScreen('folders.cancel'), style: 'cancel' }, { text: tHomeScreen('folders.deleteConfirm'), style: 'destructive', onPress: remove }]);
+  };
+
   const handleCreate = () => {
     resetCharacter();
     navigation.navigate('CharacterTab');
@@ -369,8 +414,9 @@ export default function HomeScreen({ navigation }) {
   };
 
   const allItems = [
-    { type: 'create' },
-    { type: 'upload' },
+    ...(activeFolder ? [] : [{ type: 'create' }, { type: 'upload' }, { type: 'createFolder' }]),
+    ...(activeFolder ? [] : (folderDraftVisible ? [{ type: 'folderDraft' }] : [])),
+    ...(activeFolder ? [] : folders.map((folder) => ({ type: 'folder', ...folder }))),
     ...characters.map(c => ({ type: 'character', ...c })),
   ];
 
@@ -421,6 +467,8 @@ export default function HomeScreen({ navigation }) {
         <Text style={styles.title}>{tHomeScreen("title")}</Text>
         <Text style={styles.subtitle}>{tHomeScreen("subtitle")}</Text>
       </View>
+      {activeFolder && <View style={styles.folderHeader}><TouchableOpacity onPress={() => { setActiveFolder(null); setMovingCharacterId(null); }}><Text style={styles.folderBack}>← {tHomeScreen('folders.back')}</Text></TouchableOpacity><Text style={styles.folderHeaderTitle}>{activeFolder.name}</Text><Text style={styles.folderHeaderCount}>{tHomeScreen('folders.characters')}: {folderCounts[activeFolder.id] || 0}</Text>{movingCharacterId && <TouchableOpacity style={styles.rootDropZone} onPress={() => handleMoveToFolder(null)}><Text style={styles.rootDropText}>{tHomeScreen('folders.back')}</Text></TouchableOpacity>}</View>}
+      {movingCharacterId && !activeFolder && <Text style={styles.moveHint}>{tHomeScreen('folders.moving')}: {tHomeScreen('folders.moveHint')}</Text>}
       <ScrollView
         style={styles.scrollView}
         contentContainerStyle={styles.scrollContent}
@@ -451,6 +499,15 @@ export default function HomeScreen({ navigation }) {
                     />
                   );
                 }
+                if (item.type === 'createFolder') {
+                  return <ActionCell key="create-folder" icon={<MaterialCommunityIcons name="folder-plus-outline" size={38} color={characterFoldersEnabled ? '#5a5a5a' : '#aaa'} />} label={tHomeScreen('folders.create')} disabled={!characterFoldersEnabled} onPress={() => setFolderDraftVisible(true)} />;
+                }
+                if (item.type === 'folderDraft') {
+                  return <View key="folder-draft" style={styles.folderDraftCell}><MaterialCommunityIcons name="folder-outline" size={38} color="#5a5a5a" /><TextInput autoFocus value={folderName} onChangeText={setFolderName} placeholder={tHomeScreen('folders.namePlaceholder')} style={styles.folderNameInput} /><View style={styles.folderDraftActions}><TouchableOpacity onPress={handleCreateFolder}><Text>{tHomeScreen('folders.confirm')}</Text></TouchableOpacity><TouchableOpacity onPress={() => { setFolderDraftVisible(false); setFolderName(''); }}><Text>{tHomeScreen('folders.cancel')}</Text></TouchableOpacity></View></View>;
+                }
+                if (item.type === 'folder') {
+                  return <TouchableOpacity key={item.id} style={[styles.folderCell, movingCharacterId && styles.folderDropTarget]} onPress={() => movingCharacterId ? handleMoveToFolder(item.id) : setActiveFolder(item)}><TouchableOpacity style={styles.folderDeleteButton} onPress={(event) => { event?.stopPropagation?.(); handleDeleteFolder(item); }}><Text>×</Text></TouchableOpacity><MaterialCommunityIcons name="folder-outline" size={52} color="#d4af37" /><Text style={styles.folderName}>{item.name}</Text><Text style={styles.folderCount}>{tHomeScreen('folders.characters')}: {folderCounts[item.id] || 0}</Text></TouchableOpacity>;
+                }
                 if (item.type === 'empty') {
                   return <EmptyCell key={item.id} id={item.id} />;
                 }
@@ -461,6 +518,8 @@ export default function HomeScreen({ navigation }) {
                     onPress={() => handleOpen(item.id)}
                     onDelete={() => handleDelete(item)}
                     onDownload={() => handleDownload(item)}
+                    onMoveStart={() => setMovingCharacterId(item.id)}
+                    moving={movingCharacterId === item.id}
                   />
                 );
               })}
diff --git a/components/settings/SettingsModal.js b/components/settings/SettingsModal.js
index c1c53cf..4ea4366 100644
--- a/components/settings/SettingsModal.js
+++ b/components/settings/SettingsModal.js
@@ -8,10 +8,13 @@ export default function SettingsModal({ visible, onClose }) {
   const loss = useAppSettingsStore((state) => state.weaponDurabilityLossPer10Shots);
   const setEnabled = useAppSettingsStore((state) => state.setRandomWeaponDurabilityEnabled);
   const setLoss = useAppSettingsStore((state) => state.setWeaponDurabilityLossPer10Shots);
+  const foldersEnabled = useAppSettingsStore((state) => state.characterFoldersEnabled);
+  const setFoldersEnabled = useAppSettingsStore((state) => state.setCharacterFoldersEnabled);
   return <Modal visible={visible} transparent animationType="slide" onRequestClose={onClose}>
     <View style={styles.backdrop}><View style={styles.modal}>
       <Text style={styles.title}>{tHomeScreen('settings.title')}</Text>
       <View style={styles.row}><View style={styles.text}><Text style={styles.label}>{tHomeScreen('settings.durabilityTitle')}</Text><Text style={styles.description}>{tHomeScreen('settings.durabilityDescription')}</Text></View><Switch value={enabled} onValueChange={setEnabled} /></View>
+      <View style={[styles.row, styles.loss]}><View style={styles.text}><Text style={styles.label}>{tHomeScreen('settings.foldersTitle')}</Text><Text style={styles.description}>{tHomeScreen('settings.foldersDescription')}</Text></View><Switch value={foldersEnabled} onValueChange={setFoldersEnabled} /></View>
       {enabled && <View style={styles.loss}><Text style={styles.label}>{tHomeScreen('settings.lossTitle')}</Text><View style={styles.counter}><TouchableOpacity disabled={loss <= 1} onPress={() => setLoss(loss - 1)}><Text style={styles.button}>−</Text></TouchableOpacity><Text style={styles.value}>{loss}</Text><TouchableOpacity disabled={loss >= 100} onPress={() => setLoss(loss + 1)}><Text style={styles.button}>+</Text></TouchableOpacity></View><Text style={styles.description}>{tHomeScreen('settings.lossDescription')}</Text></View>}
       <TouchableOpacity onPress={onClose} style={styles.close}><Text>OK</Text></TouchableOpacity>
     </View></View>
diff --git a/db/Database.js b/db/Database.js
index 74e3f3a..da061e9 100644
--- a/db/Database.js
+++ b/db/Database.js
@@ -138,7 +138,10 @@ export async function getCharactersList() {
 }
 
 export async function deleteCharacter(id) {
-  await runQuery('DELETE FROM characters WHERE id = ?', [id]);
+  await runBatch([
+    { sql: 'DELETE FROM character_folder_memberships WHERE character_id = ?', params: [id] },
+    { sql: 'DELETE FROM characters WHERE id = ?', params: [id] },
+  ]);
 }
 
 // ─── Правила эффектов перков (если используются — остаются в БД) ─────────────────
@@ -147,3 +150,77 @@ export async function getPerkEffects(_perkName, _rank = 1) {
   // perk_effects больше не хранится в БД (каталог в JSON). Не используется в коде.
   return [];
 }
+
+// ─── Каталоги персонажей ────────────────────────────────────────────────────
+
+const cleanFolderName = (name) => String(name ?? '').trim();
+const makeFolderId = () => `folder_${Date.now()}_${Math.random().toString(36).slice(2, 10)}`;
+
+export async function getCharacterFolders() {
+  const rows = await getAll('SELECT id, name, created_at, updated_at, sort_order FROM character_folders ORDER BY sort_order ASC, created_at ASC');
+  return rows.map((row) => ({
+    id: row.id, name: row.name, createdAt: row.created_at, updatedAt: row.updated_at, sortOrder: row.sort_order,
+  }));
+}
+
+export async function createCharacterFolder(name) {
+  const cleanName = cleanFolderName(name);
+  if (!cleanName) throw new Error('Folder name is required');
+  const now = Date.now();
+  const last = await getFirst('SELECT sort_order FROM character_folders ORDER BY sort_order DESC LIMIT 1');
+  const folder = { id: makeFolderId(), name: cleanName, createdAt: now, updatedAt: now, sortOrder: Number(last?.sort_order ?? -1) + 1 };
+  await runQuery('INSERT INTO character_folders (id, name, created_at, updated_at, sort_order) VALUES (?, ?, ?, ?, ?)',
+    [folder.id, folder.name, folder.createdAt, folder.updatedAt, folder.sortOrder]);
+  return folder;
+}
+
+export async function renameCharacterFolder(folderId, name) {
+  const cleanName = cleanFolderName(name);
+  if (!cleanName) throw new Error('Folder name is required');
+  await runQuery('UPDATE character_folders SET name = ?, updated_at = ? WHERE id = ?', [cleanName, Date.now(), folderId]);
+}
+
+export async function getCharacterFolderId(characterId) {
+  const row = await getFirst('SELECT folder_id FROM character_folder_memberships WHERE character_id = ?', [characterId]);
+  return row?.folder_id ?? null;
+}
+
+export async function moveCharacterToFolder(characterId, folderId) {
+  if (folderId == null) {
+    await runQuery('DELETE FROM character_folder_memberships WHERE character_id = ?', [characterId]);
+    return;
+  }
+  const folder = await getFirst('SELECT id FROM character_folders WHERE id = ?', [folderId]);
+  if (!folder) throw new Error('Folder not found');
+  const existing = await getCharacterFolderId(characterId);
+  if (existing) await runQuery('UPDATE character_folder_memberships SET folder_id = ? WHERE character_id = ?', [folderId, characterId]);
+  else await runQuery('INSERT INTO character_folder_memberships (character_id, folder_id) VALUES (?, ?)', [characterId, folderId]);
+}
+
+export async function getRootCharactersList() {
+  const rows = await getAll('SELECT id, name, level, origin_name, created_at, updated_at FROM characters ORDER BY created_at DESC');
+  const memberships = await getAll('SELECT character_id FROM character_folder_memberships');
+  const assigned = new Set(memberships.map((row) => row.character_id));
+  return rows.filter((row) => !assigned.has(row.id)).map((row) => ({ id: row.id, name: row.name, level: row.level, originName: row.origin_name, createdAt: row.created_at, updatedAt: row.updated_at }));
+}
+
+export async function getCharactersInFolder(folderId) {
+  const rows = await getAll('SELECT id, name, level, origin_name, created_at, updated_at FROM characters ORDER BY created_at DESC');
+  const memberships = await getAll('SELECT character_id, folder_id FROM character_folder_memberships WHERE folder_id = ?', [folderId]);
+  const ids = new Set(memberships.map((row) => row.character_id));
+  return rows.filter((row) => ids.has(row.id)).map((row) => ({ id: row.id, name: row.name, level: row.level, originName: row.origin_name, createdAt: row.created_at, updatedAt: row.updated_at }));
+}
+
+export async function deleteCharacterFolderAndCharacters(folderId) {
+  const members = await getAll('SELECT character_id FROM character_folder_memberships WHERE folder_id = ?', [folderId]);
+  const statements = members.flatMap((member) => [
+    { sql: 'DELETE FROM character_folder_memberships WHERE character_id = ?', params: [member.character_id] },
+    { sql: 'DELETE FROM characters WHERE id = ?', params: [member.character_id] },
+  ]);
+  statements.push(
+    { sql: 'DELETE FROM character_folder_memberships WHERE folder_id = ?', params: [folderId] },
+    { sql: 'DELETE FROM character_folders WHERE id = ?', params: [folderId] },
+  );
+  await runBatch(statements);
+  return members.map((member) => member.character_id);
+}
diff --git a/db/Database.web.js b/db/Database.web.js
index 5594060..f6068b7 100644
--- a/db/Database.web.js
+++ b/db/Database.web.js
@@ -119,10 +119,59 @@ export async function getCharactersList() {
 }
 
 export async function deleteCharacter(id) {
-  await runQuery('DELETE FROM characters WHERE id = ?', [id]);
+  await runBatch([{ sql: 'DELETE FROM character_folder_memberships WHERE character_id = ?', params: [id] }, { sql: 'DELETE FROM characters WHERE id = ?', params: [id] }]);
 }
 
 export async function getPerkEffects(_perkName, _rank = 1) {
   // perk_effects больше не хранится в БД (каталог в JSON). Не используется в коде.
   return [];
 }
+
+
+// ─── Каталоги персонажей ────────────────────────────────────────────────────
+const cleanFolderName = (name) => String(name ?? '').trim();
+const makeFolderId = () => `folder_${Date.now()}_${Math.random().toString(36).slice(2, 10)}`;
+const characterSummary = (row) => ({ id: row.id, name: row.name, level: row.level, originName: row.origin_name, createdAt: row.created_at, updatedAt: row.updated_at });
+
+export async function getCharacterFolders() {
+  const rows = await getAll('SELECT id, name, created_at, updated_at, sort_order FROM character_folders ORDER BY sort_order ASC, created_at ASC');
+  return rows.map((row) => ({ id: row.id, name: row.name, createdAt: row.created_at, updatedAt: row.updated_at, sortOrder: row.sort_order }));
+}
+export async function createCharacterFolder(name) {
+  const cleanName = cleanFolderName(name);
+  if (!cleanName) throw new Error('Folder name is required');
+  const now = Date.now();
+  const last = await getFirst('SELECT sort_order FROM character_folders ORDER BY sort_order DESC LIMIT 1');
+  const folder = { id: makeFolderId(), name: cleanName, createdAt: now, updatedAt: now, sortOrder: Number(last?.sort_order ?? -1) + 1 };
+  await runQuery('INSERT INTO character_folders (id, name, created_at, updated_at, sort_order) VALUES (?, ?, ?, ?, ?)', [folder.id, folder.name, now, now, folder.sortOrder]);
+  return folder;
+}
+export async function renameCharacterFolder(folderId, name) {
+  const cleanName = cleanFolderName(name);
+  if (!cleanName) throw new Error('Folder name is required');
+  await runQuery('UPDATE character_folders SET name = ?, updated_at = ? WHERE id = ?', [cleanName, Date.now(), folderId]);
+}
+export async function getCharacterFolderId(characterId) {
+  return (await getFirst('SELECT folder_id FROM character_folder_memberships WHERE character_id = ?', [characterId]))?.folder_id ?? null;
+}
+export async function moveCharacterToFolder(characterId, folderId) {
+  if (folderId == null) return runQuery('DELETE FROM character_folder_memberships WHERE character_id = ?', [characterId]);
+  if (!(await getFirst('SELECT id FROM character_folders WHERE id = ?', [folderId]))) throw new Error('Folder not found');
+  if (await getCharacterFolderId(characterId)) await runQuery('UPDATE character_folder_memberships SET folder_id = ? WHERE character_id = ?', [folderId, characterId]);
+  else await runQuery('INSERT INTO character_folder_memberships (character_id, folder_id) VALUES (?, ?)', [characterId, folderId]);
+}
+export async function getRootCharactersList() {
+  const [characters, memberships] = await Promise.all([getAll('SELECT id, name, level, origin_name, created_at, updated_at FROM characters ORDER BY created_at DESC'), getAll('SELECT character_id FROM character_folder_memberships')]);
+  const assigned = new Set(memberships.map((row) => row.character_id));
+  return characters.filter((row) => !assigned.has(row.id)).map(characterSummary);
+}
+export async function getCharactersInFolder(folderId) {
+  const [characters, memberships] = await Promise.all([getAll('SELECT id, name, level, origin_name, created_at, updated_at FROM characters ORDER BY created_at DESC'), getAll('SELECT character_id FROM character_folder_memberships WHERE folder_id = ?', [folderId])]);
+  const ids = new Set(memberships.map((row) => row.character_id));
+  return characters.filter((row) => ids.has(row.id)).map(characterSummary);
+}
+export async function deleteCharacterFolderAndCharacters(folderId) {
+  const members = await getAll('SELECT character_id FROM character_folder_memberships WHERE folder_id = ?', [folderId]);
+  await runBatch([...members.flatMap(({ character_id }) => [{ sql: 'DELETE FROM character_folder_memberships WHERE character_id = ?', params: [character_id] }, { sql: 'DELETE FROM characters WHERE id = ?', params: [character_id] }]), { sql: 'DELETE FROM character_folder_memberships WHERE folder_id = ?', params: [folderId] }, { sql: 'DELETE FROM character_folders WHERE id = ?', params: [folderId] }]);
+  return members.map((row) => row.character_id);
+}
diff --git a/db/schema.js b/db/schema.js
index 7788506..a2a41e2 100644
--- a/db/schema.js
+++ b/db/schema.js
@@ -5,7 +5,7 @@
 //
 // schema_meta оставлена для версионирования/миграций на будущее.
 
-export const SCHEMA_VERSION = 9;
+export const SCHEMA_VERSION = 10;
 
 export const CREATE_TABLES = [
   `CREATE TABLE IF NOT EXISTS schema_meta (
@@ -22,4 +22,17 @@ export const CREATE_TABLES = [
     created_at INTEGER NOT NULL,
     updated_at INTEGER NOT NULL
   )`,
+
+  `CREATE TABLE IF NOT EXISTS character_folders (
+    id TEXT PRIMARY KEY,
+    name TEXT NOT NULL,
+    created_at INTEGER NOT NULL,
+    updated_at INTEGER NOT NULL,
+    sort_order INTEGER NOT NULL DEFAULT 0
+  )`,
+
+  `CREATE TABLE IF NOT EXISTS character_folder_memberships (
+    character_id TEXT PRIMARY KEY,
+    folder_id TEXT NOT NULL
+  )`,
 ];
diff --git a/docs/character-folders-design.md b/docs/character-folders-design.md
new file mode 100644
index 0000000..3bfdca3
--- /dev/null
+++ b/docs/character-folders-design.md
@@ -0,0 +1,311 @@
+# Дизайн: каталоги персонажей / Character folders
+
+**Статус:** утверждённый дизайн, реализация в работе  
+**Назначение:** организация сохранённых персонажей по пользовательским каталогам.
+
+---
+
+## 1. Цели
+
+Каталоги позволяют пользователю группировать созданных персонажей, например по кампании, фракции, игроку или сценарию.
+
+Функция должна:
+
+- не менять текущий сценарий использования для тех, кто не создаёт каталоги;
+- сохранять структуру между запусками приложения;
+- позволять перемещать персонажа между каталогами жестом drag-and-drop;
+- позволять вернуть персонажа в корневой список;
+- поддерживать русский и английский интерфейс.
+
+---
+
+## 2. Настройка
+
+В глобальном окне **«Настройки»** добавляется переключатель:
+
+```text
+Каталоги персонажей
+Организуйте созданных персонажей по каталогам.
+
+[ переключатель ]
+```
+
+English:
+
+```text
+Character folders
+Organize created characters into folders.
+
+[ switch ]
+```
+
+### Правила
+
+- Настройка глобальная, не привязана к отдельному персонажу.
+- По умолчанию выключена.
+- При включённой настройке пользователь может создавать новые каталоги.
+- При выключенной настройке уже созданные каталоги остаются видимыми и открываемыми.
+- При выключенной настройке создание новых каталогов недоступно.
+- Кнопка «Создать каталог» остаётся на месте, но отображается неактивной.
+- Выключение настройки не удаляет каталоги, персонажей или их распределение.
+
+---
+
+## 3. Корневой экран персонажей
+
+На экране списка персонажей используется сетка из трёх колонок.
+
+Первая строка содержит три action-ячейки:
+
+```text
+[ + Создать ] [ ↑ Загрузить ] [ folder-plus-outline Создать каталог ]
+```
+
+Ниже располагаются:
+
+1. карточки каталогов;
+2. карточки персонажей, не принадлежащих ни одному каталогу.
+
+Персонаж без каталога считается находящимся в корневом списке.
+
+---
+
+## 4. Создание каталога
+
+При нажатии **«Создать каталог»**, если настройка включена, в следующей свободной ячейке появляется временная карточка:
+
+```text
+┌──────────────────────┐
+│         📁           │
+│ [Название каталога]  │
+│                      │
+│ [Создать] [Отмена]   │
+└──────────────────────┘
+```
+
+### Правила ввода
+
+- Поле получает фокус автоматически.
+- Пустое название создать нельзя.
+- Пробелы в начале и конце удаляются.
+- Названия могут повторяться: уникальность обеспечивается внутренним `id` каталога.
+- «Создать» сохраняет каталог.
+- «Отмена» удаляет временную ячейку без изменений.
+
+---
+
+## 5. Карточка каталога
+
+Пример карточки:
+
+```text
+┌──────────────────────┐
+│         📁           │
+│  Выжившие Мохаве     │
+│  Персонажей: 4       │
+└──────────────────────┘
+```
+
+Нажатие на карточку открывает отдельный экран каталога.
+
+---
+
+## 6. Экран каталога
+
+Экран каталога содержит:
+
+```text
+← Название каталога
+  Персонажей: N
+
+[ карточка ] [ карточка ] [ карточка ]
+```
+
+На нём показываются только персонажи выбранного каталога.
+
+Доступны обычные операции с персонажем:
+
+- открыть;
+- экспортировать;
+- удалить;
+- перенести в другой каталог;
+- вернуть в корневой список.
+
+---
+
+## 7. Перемещение персонажей
+
+### 7.1. Маркер захвата
+
+В верхнем левом углу карточки персонажа отображается векторная иконка:
+
+```text
+arrow-all
+```
+
+Именно этот элемент является drag-handle.
+
+- Нажатие на основную область карточки открывает персонажа как раньше.
+- Нажатие на удаление и экспорт продолжает выполнять соответствующие операции.
+- Drag-and-drop начинается только с `arrow-all`.
+
+### 7.2. Перенос в каталог
+
+1. Пользователь удерживает drag-handle.
+2. Карточка поднимается над сеткой.
+3. Карточки каталогов подсвечиваются как допустимые drop-зоны.
+4. Пользователь отпускает карточку над каталогом.
+5. Персонажу назначается `folderId` выбранного каталога.
+6. Счётчик персонажей обновляется.
+
+Если карточка отпущена вне допустимой зоны, перенос отменяется.
+
+### 7.3. Возврат в корневой список
+
+На экране открытого каталога доступна отдельная drop-зона:
+
+```text
+Вернуть в список персонажей
+```
+
+Drop на эту зону снимает `folderId` у персонажа. Он снова отображается в корневом списке.
+
+---
+
+## 8. Удаление каталога
+
+Карточка каталога имеет кнопку удаления.
+
+При удалении отображается подтверждение:
+
+```text
+Удалить каталог?
+
+Будут безвозвратно удалены каталог «Выжившие Мохаве»
+и все персонажи внутри него: 4.
+
+[Удалить каталог и персонажей] [Отмена]
+```
+
+### Правило удаления
+
+Подтверждённое удаление удаляет:
+
+- каталог;
+- всех персонажей, находящихся в нём;
+- связи персонажей с каталогом;
+- связанные локальные данные сохранений;
+- связанные записи облачной синхронизации, если она включена.
+
+---
+
+## 9. Модель данных
+
+Каталог является отдельной сущностью. Он не должен храниться внутри JSON конкретного персонажа, потому что каталог может быть пустым.
+
+```js
+{
+  id: 'folder_...',
+  name: 'Выжившие Мохаве',
+  createdAt: 1760000000000,
+  updatedAt: 1760000000000,
+  sortOrder: 0
+}
+```
+
+Принадлежность персонажа к каталогу:
+
+```js
+{
+  characterId: 'char_...',
+  folderId: 'folder_...'
+}
+```
+
+Персонаж без записи принадлежности находится в корневом списке.
+
+---
+
+## 10. Хранение и синхронизация
+
+Нужно поддержать данные каталогов в обоих окружениях:
+
+- native / SQLite;
+- web / AsyncStorage adapter.
+
+Необходимые операции:
+
+```js
+getCharacterFolders()
+createCharacterFolder(name)
+renameCharacterFolder(folderId, name)
+deleteCharacterFolderAndCharacters(folderId)
+getCharacterFolderId(characterId)
+moveCharacterToFolder(characterId, folderId)
+getRootCharactersList()
+getCharactersInFolder(folderId)
+```
+
+### Облачная синхронизация
+
+Облачная синхронизация должна включать:
+
+- список каталогов;
+- принадлежность персонажа к каталогу;
+- удаление каталогов;
+- удаление персонажей из удалённого каталога.
+
+### Импорт и экспорт
+
+- Экспорт отдельного персонажа не зависит от каталога.
+- Импортированный персонаж по умолчанию попадает в корневой список.
+
+---
+
+## 11. Локализация
+
+Необходимые пользовательские строки:
+
+| RU | EN |
+|---|---|
+| Каталоги персонажей | Character folders |
+| Создать каталог | Create folder |
+| Название каталога | Folder name |
+| Персонажей: {{count}} | Characters: {{count}} |
+| Вернуть в список персонажей | Move to character list |
+| Удалить каталог? | Delete folder? |
+| Удалить каталог и персонажей | Delete folder and characters |
+| Отмена | Cancel |
+| Создать | Create |
+
+---
+
+## 12. Совместимость
+
+Для существующих пользователей:
+
+- автоматически не создаются каталоги;
+- все существующие персонажи считаются находящимися в корневом списке;
+- при выключенной настройке старый сценарий работы с персонажами не меняется;
+- существующие каталоги не скрываются при выключении настройки.
+
+---
+
+## 13. Критерии готовности
+
+1. Настройка сохраняется между запусками.
+2. При выключенной настройке существующие каталоги видны, но создать новый нельзя.
+3. При включённой настройке доступна третья action-ячейка «Создать каталог».
+4. Каталог нельзя создать без названия.
+5. Новый каталог появляется в следующей ячейке сетки.
+6. Каталог показывает актуальное количество персонажей.
+7. Нажатие на каталог открывает его отдельный экран.
+8. Drag начинается только с `arrow-all`.
+9. Drop на каталог переносит персонажа.
+10. Drop мимо допустимой зоны отменяет перенос.
+11. Персонажа можно вернуть в корневой список.
+12. Удаление каталога требует подтверждения.
+13. Подтверждённое удаление удаляет каталог и персонажей внутри.
+14. Каталоги и принадлежность персонажей сохраняются после перезапуска.
+15. Каталоги и принадлежность синхронизируются через облако.
+16. Все новые строки доступны в RU и EN.
diff --git a/i18n/en-EN/screens/home/screen.json b/i18n/en-EN/screens/home/screen.json
index f39c075..91e9213 100644
--- a/i18n/en-EN/screens/home/screen.json
+++ b/i18n/en-EN/screens/home/screen.json
@@ -69,6 +69,21 @@
     "durabilityTitle": "Random weapon durability",
     "durabilityDescription": "Weapons obtained through “Add Loot” have random durability from 1 to 100. Firing weapons consumes durability.",
     "lossTitle": "Durability loss per 10 shots",
-    "lossDescription": "The default is 1 durability per 10 shots. Unreliable weapons use twice as much, while Reliable weapons use half as much."
+    "lossDescription": "The default is 1 durability per 10 shots. Unreliable weapons use twice as much, while Reliable weapons use half as much.",
+    "foldersTitle": "Character folders",
+    "foldersDescription": "Organize created characters into folders."
+  },
+  "folders": {
+    "create": "Create folder",
+    "namePlaceholder": "Folder name",
+    "confirm": "Create",
+    "cancel": "Cancel",
+    "characters": "Characters",
+    "back": "Move to character list",
+    "deleteTitle": "Delete folder?",
+    "deleteMessage": "The folder and all characters inside it will be permanently deleted.",
+    "deleteConfirm": "Delete folder and characters",
+    "moveHint": "Hold the arrow icon, then tap a folder to move the character.",
+    "moving": "Select a destination folder"
   }
 }
diff --git a/i18n/ru-RU/screens/home/screen.json b/i18n/ru-RU/screens/home/screen.json
index b42ba9a..2e8d232 100644
--- a/i18n/ru-RU/screens/home/screen.json
+++ b/i18n/ru-RU/screens/home/screen.json
@@ -69,6 +69,21 @@
     "durabilityTitle": "Случайная прочность оружия",
     "durabilityDescription": "Оружие, получаемое через «Добавить добычу», обладает случайной прочностью от 1 до 100. Выстрелы из оружия тратят прочность.",
     "lossTitle": "Расход прочности за 10 выстрелов",
-    "lossDescription": "По умолчанию расходуется 1 прочность за 10 выстрелов. «Ненадёжное» оружие тратит в 2 раза больше, а «Надёжное» — в 2 раза меньше."
+    "lossDescription": "По умолчанию расходуется 1 прочность за 10 выстрелов. «Ненадёжное» оружие тратит в 2 раза больше, а «Надёжное» — в 2 раза меньше.",
+    "foldersTitle": "Каталоги персонажей",
+    "foldersDescription": "Организуйте созданных персонажей по каталогам."
+  },
+  "folders": {
+    "create": "Создать каталог",
+    "namePlaceholder": "Название каталога",
+    "confirm": "Создать",
+    "cancel": "Отмена",
+    "characters": "Персонажей",
+    "back": "Вернуть в список персонажей",
+    "deleteTitle": "Удалить каталог?",
+    "deleteMessage": "Каталог и все персонажи внутри него будут безвозвратно удалены.",
+    "deleteConfirm": "Удалить каталог и персонажей",
+    "moveHint": "Удерживайте значок стрелок, затем нажмите каталог для переноса.",
+    "moving": "Выберите каталог назначения"
   }
 }
diff --git a/patchs/45-character-folder-creation.patch b/patchs/45-character-folder-creation.patch
new file mode 100644
index 0000000..007321a
--- /dev/null
+++ b/patchs/45-character-folder-creation.patch
@@ -0,0 +1,312 @@
+diff --git a/components/characterFolders.js b/components/characterFolders.js
+new file mode 100644
+index 0000000..53ef672
+--- /dev/null
++++ b/components/characterFolders.js
+@@ -0,0 +1,26 @@
++import AsyncStorage from '@react-native-async-storage/async-storage';
++
++const STORAGE_KEY = 'fallout2d20:character-folders';
++
++const readFolders = async () => {
++  const raw = await AsyncStorage.getItem(STORAGE_KEY);
++  if (!raw) return [];
++  try {
++    const folders = JSON.parse(raw);
++    return Array.isArray(folders) ? folders : [];
++  } catch (_) {
++    return [];
++  }
++};
++
++export const getCharacterFolders = readFolders;
++
++export const createCharacterFolder = async (name) => {
++  const cleanName = String(name || '').trim();
++  if (!cleanName) throw new Error('Folder name is required');
++  const folders = await readFolders();
++  const now = Date.now();
++  const folder = { id: `folder_${now}_${Math.random().toString(36).slice(2, 8)}`, name: cleanName, createdAt: now };
++  await AsyncStorage.setItem(STORAGE_KEY, JSON.stringify([...folders, folder]));
++  return folder;
++};
+diff --git a/components/screens/HomeScreen/HomeScreen.js b/components/screens/HomeScreen/HomeScreen.js
+index 7566e13..43c0e4e 100644
+--- a/components/screens/HomeScreen/HomeScreen.js
++++ b/components/screens/HomeScreen/HomeScreen.js
+@@ -11,6 +11,7 @@ import {
+   Platform,
+   Modal,
+   Linking,
++  TextInput,
+ } from 'react-native';
+ import { MaterialCommunityIcons, FontAwesome5 } from '@expo/vector-icons';
+ import { useFocusEffect } from '@react-navigation/native';
+@@ -29,6 +30,9 @@ import {
+ import { openCloudFolderInDrive, syncAllCharactersWithCloud } from '../../cloudSync/googleDriveSync';
+ import { forcePwaUpdate } from '../../../src/utils/forcePwaUpdate';
+ import styles from '../../../styles/HomeScreen.styles';
++import SettingsModal from '../../settings/SettingsModal';
++import useAppSettingsStore from '../../../src/store/appSettingsStore';
++import { getCharacterFolders, createCharacterFolder } from '../../characterFolders';
+ 
+ const getOriginImage = (originName) => {
+   if (!originName) return null;
+@@ -40,7 +44,7 @@ const NUM_COLS = 3;
+ 
+ const ActionCell = ({ icon, label, onPress }) => (
+   <TouchableOpacity style={styles.createCell} onPress={onPress} activeOpacity={0.7}>
+-    <Text style={styles.createPlus}>{icon}</Text>
++    {typeof icon === 'string' ? <Text style={styles.createPlus}>{icon}</Text> : icon}
+     <Text style={styles.createLabel}>{label}</Text>
+   </TouchableOpacity>
+ );
+@@ -97,10 +101,15 @@ export default function HomeScreen({ navigation }) {
+   const locale = useLocale();
+   const { getCharactersList, loadCharacter, resetCharacter, deleteCharacter } = useCharacter();
+   const [characters, setCharacters] = useState([]);
++  const [folders, setFolders] = useState([]);
++  const [folderDraftVisible, setFolderDraftVisible] = useState(false);
++  const [folderName, setFolderName] = useState('');
+   const [loading, setLoading] = useState(true);
++  const characterFoldersEnabled = useAppSettingsStore((state) => state.characterFoldersEnabled);
+   const [menuVisible, setMenuVisible] = useState(false);
+   const [aboutVisible, setAboutVisible] = useState(false);
+   const [communityVisible, setCommunityVisible] = useState(false);
++  const [settingsVisible, setSettingsVisible] = useState(false);
+ 
+   const [installPrompt, setInstallPrompt] = useState(null);
+   const [isStandalone, setIsStandalone] = useState(false);
+@@ -174,9 +183,10 @@ export default function HomeScreen({ navigation }) {
+   const loadList = useCallback(async () => {
+     setLoading(true);
+     try {
+-      const list = await getCharactersList();
++      const [list, savedFolders] = await Promise.all([getCharactersList(), getCharacterFolders()]);
+       list.sort((a, b) => (b.createdAt || 0) - (a.createdAt || 0));
+       setCharacters(list);
++      setFolders(savedFolders);
+     } catch (e) {
+       setCharacters([]);
+     } finally {
+@@ -190,6 +200,15 @@ export default function HomeScreen({ navigation }) {
+     }, [loadList])
+   );
+ 
++  const handleCreateFolder = async () => {
++    const name = folderName.trim();
++    if (!name) return;
++    await createCharacterFolder(name);
++    setFolderName('');
++    setFolderDraftVisible(false);
++    loadList();
++  };
++
+   const handleCreate = () => {
+     resetCharacter();
+     navigation.navigate('CharacterTab');
+@@ -369,6 +388,9 @@ export default function HomeScreen({ navigation }) {
+   const allItems = [
+     { type: 'create' },
+     { type: 'upload' },
++    { type: 'createFolder' },
++    ...(folderDraftVisible ? [{ type: 'folderDraft' }] : []),
++    ...folders.map(folder => ({ type: 'folder', ...folder })),
+     ...characters.map(c => ({ type: 'character', ...c })),
+   ];
+ 
+@@ -449,6 +471,24 @@ export default function HomeScreen({ navigation }) {
+                     />
+                   );
+                 }
++                if (item.type === 'createFolder') {
++                  return <ActionCell
++                    key="create-folder"
++                    icon={<MaterialCommunityIcons name="folder-plus-outline" size={38} color={characterFoldersEnabled ? '#5a5a5a' : '#aaa'} />}
++                    label={tHomeScreen('folders.create')}
++                    onPress={() => characterFoldersEnabled && setFolderDraftVisible(true)}
++                  />;
++                }
++                if (item.type === 'folderDraft') {
++                  return <View key="folder-draft" style={styles.folderDraftCell}>
++                    <MaterialCommunityIcons name="folder-outline" size={38} color="#5a5a5a" />
++                    <TextInput autoFocus value={folderName} onChangeText={setFolderName} placeholder={tHomeScreen('folders.namePlaceholder')} style={styles.folderNameInput} />
++                    <View style={styles.folderDraftActions}><TouchableOpacity onPress={handleCreateFolder}><Text>{tHomeScreen('folders.confirm')}</Text></TouchableOpacity><TouchableOpacity onPress={() => { setFolderDraftVisible(false); setFolderName(''); }}><Text>{tHomeScreen('folders.cancel')}</Text></TouchableOpacity></View>
++                  </View>;
++                }
++                if (item.type === 'folder') {
++                  return <View key={item.id} style={styles.folderCell}><MaterialCommunityIcons name="folder-outline" size={52} color="#d4af37" /><Text style={styles.folderName}>{item.name}</Text></View>;
++                }
+                 if (item.type === 'empty') {
+                   return <EmptyCell key={item.id} id={item.id} />;
+                 }
+@@ -477,6 +517,10 @@ export default function HomeScreen({ navigation }) {
+       <Modal visible={menuVisible} transparent animationType="fade" onRequestClose={() => setMenuVisible(false)}>
+         <Pressable style={styles.modalBackdrop} onPress={() => setMenuVisible(false)}>
+           <Pressable style={styles.menuPanel} onPress={() => {}}>
++            <TouchableOpacity style={styles.menuItem} onPress={() => { setMenuVisible(false); setSettingsVisible(true); }}>
++              <MaterialCommunityIcons name="cog-outline" size={20} color="#d4af37" />
++              <Text style={styles.menuText}>{tHomeScreen('menu.settings')}</Text>
++            </TouchableOpacity>
+             <TouchableOpacity style={styles.menuItem} onPress={handleCloudSync}>
+               <FontAwesome5 name="google-drive" size={18} color="#d4af37" />
+               <Text style={styles.menuText}>{tHomeScreen('menu.sync')}</Text>
+@@ -515,6 +559,8 @@ export default function HomeScreen({ navigation }) {
+         </Pressable>
+       </Modal>
+ 
++      <SettingsModal visible={settingsVisible} onClose={() => setSettingsVisible(false)} />
++
+       <Modal visible={aboutVisible} transparent animationType="slide" onRequestClose={() => setAboutVisible(false)}>
+         <View style={styles.modalBackdropCenter}>
+           <View style={styles.infoModal}>
+diff --git a/components/settings/SettingsModal.js b/components/settings/SettingsModal.js
+new file mode 100644
+index 0000000..4ea4366
+--- /dev/null
++++ b/components/settings/SettingsModal.js
+@@ -0,0 +1,23 @@
++import React from 'react';
++import { Modal, View, Text, Switch, TouchableOpacity, StyleSheet } from 'react-native';
++import useAppSettingsStore from '../../src/store/appSettingsStore';
++import { tHomeScreen } from '../screens/HomeScreen/logic/homeScreenI18n';
++
++export default function SettingsModal({ visible, onClose }) {
++  const enabled = useAppSettingsStore((state) => state.randomWeaponDurabilityEnabled);
++  const loss = useAppSettingsStore((state) => state.weaponDurabilityLossPer10Shots);
++  const setEnabled = useAppSettingsStore((state) => state.setRandomWeaponDurabilityEnabled);
++  const setLoss = useAppSettingsStore((state) => state.setWeaponDurabilityLossPer10Shots);
++  const foldersEnabled = useAppSettingsStore((state) => state.characterFoldersEnabled);
++  const setFoldersEnabled = useAppSettingsStore((state) => state.setCharacterFoldersEnabled);
++  return <Modal visible={visible} transparent animationType="slide" onRequestClose={onClose}>
++    <View style={styles.backdrop}><View style={styles.modal}>
++      <Text style={styles.title}>{tHomeScreen('settings.title')}</Text>
++      <View style={styles.row}><View style={styles.text}><Text style={styles.label}>{tHomeScreen('settings.durabilityTitle')}</Text><Text style={styles.description}>{tHomeScreen('settings.durabilityDescription')}</Text></View><Switch value={enabled} onValueChange={setEnabled} /></View>
++      <View style={[styles.row, styles.loss]}><View style={styles.text}><Text style={styles.label}>{tHomeScreen('settings.foldersTitle')}</Text><Text style={styles.description}>{tHomeScreen('settings.foldersDescription')}</Text></View><Switch value={foldersEnabled} onValueChange={setFoldersEnabled} /></View>
++      {enabled && <View style={styles.loss}><Text style={styles.label}>{tHomeScreen('settings.lossTitle')}</Text><View style={styles.counter}><TouchableOpacity disabled={loss <= 1} onPress={() => setLoss(loss - 1)}><Text style={styles.button}>−</Text></TouchableOpacity><Text style={styles.value}>{loss}</Text><TouchableOpacity disabled={loss >= 100} onPress={() => setLoss(loss + 1)}><Text style={styles.button}>+</Text></TouchableOpacity></View><Text style={styles.description}>{tHomeScreen('settings.lossDescription')}</Text></View>}
++      <TouchableOpacity onPress={onClose} style={styles.close}><Text>OK</Text></TouchableOpacity>
++    </View></View>
++  </Modal>;
++}
++const styles=StyleSheet.create({backdrop:{flex:1,backgroundColor:'rgba(0,0,0,.6)',justifyContent:'center',padding:20},modal:{backgroundColor:'#fff',borderRadius:12,padding:20},title:{fontSize:22,fontWeight:'700',marginBottom:18},row:{flexDirection:'row',gap:12},text:{flex:1},label:{fontSize:16,fontWeight:'700'},description:{fontSize:13,color:'#555',lineHeight:18,marginTop:6},loss:{borderTopWidth:1,borderColor:'#ddd',marginTop:18,paddingTop:16},counter:{flexDirection:'row',alignItems:'center',marginTop:12,gap:20},button:{fontSize:28,fontWeight:'700',paddingHorizontal:12},value:{fontSize:22,minWidth:45,textAlign:'center'},close:{alignSelf:'flex-end',marginTop:18,padding:10}});
+diff --git a/i18n/en-EN/screens/home/screen.json b/i18n/en-EN/screens/home/screen.json
+index 858d593..048519d 100644
+--- a/i18n/en-EN/screens/home/screen.json
++++ b/i18n/en-EN/screens/home/screen.json
+@@ -46,7 +46,8 @@
+     "community": "Community",
+     "buyCoffee": "Buy author a coffee",
+     "buyCoffeeDescription": "Author support links will be added in future updates.",
+-    "checkUpdates": "Check for updates"
++    "checkUpdates": "Check for updates",
++    "settings": "Settings"
+   },
+   "about": {
+     "description": "Fallout 2d20 Companion is a character manager for tabletop RPG: create, store, edit, import/export, and sync through cloud."
+@@ -62,5 +63,20 @@
+     "iosInstructions": "To install the app on iPhone/iPad:\n\n1. Tap the Share button at the bottom of Safari.\n2. Select «Add to Home Screen».\n3. Tap «Add».",
+     "androidInstructions": "To install the app on Android:\n\nIn Chrome — tap the menu (⋮) in the top right corner and select «Install app» or «Add to Home screen».\n\nIn Samsung Internet — tap the menu (☰) → «Add page» → «On Home screen».",
+     "desktopInstructions": "To install the app:\n\nIn Edge / Chrome — look for the install icon (⊕ or a computer icon) at the right side of the address bar and click it.\n\nIn Firefox — open the menu (···) → «Install site as app»."
++  },
++  "settings": {
++    "title": "Settings",
++    "durabilityTitle": "Random weapon durability",
++    "durabilityDescription": "Weapons obtained through “Add Loot” have random durability from 1 to 100. Firing weapons consumes durability.",
++    "lossTitle": "Durability loss per 10 shots",
++    "lossDescription": "The default is 1 durability per 10 shots. Unreliable weapons use twice as much, while Reliable weapons use half as much.",
++    "foldersTitle": "Character folders",
++    "foldersDescription": "Organize created characters into folders."
++  },
++  "folders": {
++    "create": "Create folder",
++    "namePlaceholder": "Folder name",
++    "confirm": "Create",
++    "cancel": "Cancel"
+   }
+ }
+diff --git a/i18n/ru-RU/screens/home/screen.json b/i18n/ru-RU/screens/home/screen.json
+index e92117b..256ee93 100644
+--- a/i18n/ru-RU/screens/home/screen.json
++++ b/i18n/ru-RU/screens/home/screen.json
+@@ -46,7 +46,8 @@
+     "community": "Сообщество",
+     "buyCoffee": "Купить автору кофе",
+     "buyCoffeeDescription": "Поддержка автора появится в следующих обновлениях.",
+-    "checkUpdates": "Проверить обновления"
++    "checkUpdates": "Проверить обновления",
++    "settings": "Настройки"
+   },
+   "about": {
+     "description": "Fallout 2d20 Companion — менеджер персонажей для настольной ролевой игры: создание, хранение, редактирование, импорт/экспорт и облачная синхронизация."
+@@ -62,5 +63,20 @@
+     "iosInstructions": "Чтобы установить приложение на iPhone/iPad:\n\n1. Нажмите кнопку «Поделиться» (Share) в нижней панели Safari.\n2. Выберите «На экран «Домой»» (Add to Home Screen).\n3. Нажмите «Добавить».",
+     "androidInstructions": "Чтобы установить приложение на Android:\n\nВ Chrome — нажмите меню (⋮) в правом верхнем углу и выберите «Установить приложение» или «Добавить на главный экран».\n\nВ Samsung Internet — нажмите меню (☰) → «Добавить страницу» → «На главный экран».",
+     "desktopInstructions": "Чтобы установить приложение:\n\nВ Edge / Chrome — найдите значок установки (⊕ или значок компьютера) в правой части адресной строки и нажмите его.\n\nВ Firefox — откройте меню (···) → «Установить сайт как приложение»."
++  },
++  "settings": {
++    "title": "Настройки",
++    "durabilityTitle": "Случайная прочность оружия",
++    "durabilityDescription": "Оружие, получаемое через «Добавить добычу», обладает случайной прочностью от 1 до 100. Выстрелы из оружия тратят прочность.",
++    "lossTitle": "Расход прочности за 10 выстрелов",
++    "lossDescription": "По умолчанию расходуется 1 прочность за 10 выстрелов. «Ненадёжное» оружие тратит в 2 раза больше, а «Надёжное» — в 2 раза меньше.",
++    "foldersTitle": "Каталоги персонажей",
++    "foldersDescription": "Организуйте созданных персонажей по каталогам."
++  },
++  "folders": {
++    "create": "Создать каталог",
++    "namePlaceholder": "Название каталога",
++    "confirm": "Создать",
++    "cancel": "Отмена"
+   }
+ }
+diff --git a/src/store/appSettingsStore.js b/src/store/appSettingsStore.js
+new file mode 100644
+index 0000000..304048d
+--- /dev/null
++++ b/src/store/appSettingsStore.js
+@@ -0,0 +1,29 @@
++import { create } from 'zustand';
++import { persist, createJSONStorage } from 'zustand/middleware';
++import AsyncStorage from '@react-native-async-storage/async-storage';
++
++const clampLoss = (value) => Math.min(100, Math.max(1, Math.floor(Number(value) || 1)));
++
++const useAppSettingsStore = create(
++  persist(
++    (set) => ({
++      randomWeaponDurabilityEnabled: false,
++      weaponDurabilityLossPer10Shots: 1,
++      characterFoldersEnabled: false,
++      setCharacterFoldersEnabled: (enabled) => set({ characterFoldersEnabled: Boolean(enabled) }),
++      setRandomWeaponDurabilityEnabled: (enabled) => set({ randomWeaponDurabilityEnabled: Boolean(enabled) }),
++      setWeaponDurabilityLossPer10Shots: (value) => set({ weaponDurabilityLossPer10Shots: clampLoss(value) }),
++    }),
++    {
++      name: 'fallout2d20:settings',
++      storage: createJSONStorage(() => AsyncStorage),
++      partialize: (state) => ({
++        randomWeaponDurabilityEnabled: state.randomWeaponDurabilityEnabled,
++        weaponDurabilityLossPer10Shots: state.weaponDurabilityLossPer10Shots,
++        characterFoldersEnabled: state.characterFoldersEnabled,
++      }),
++    },
++  ),
++);
++
++export default useAppSettingsStore;
+diff --git a/styles/HomeScreen.styles.js b/styles/HomeScreen.styles.js
+index 0128b4b..11c94e1 100644
+--- a/styles/HomeScreen.styles.js
++++ b/styles/HomeScreen.styles.js
+@@ -155,6 +155,11 @@ const styles = StyleSheet.create({
+   },
+   characterLevel: { fontSize: 10, color: '#555', textAlign: 'center', paddingBottom: 4 },
+   emptyCell: { flex: 1, margin: 5, aspectRatio: 0.75 },
++  folderCell: { flex: 1, margin: 5, aspectRatio: 0.75, borderWidth: 1, borderColor: '#d4af37', borderRadius: 8, backgroundColor: 'rgba(17,24,39,0.9)', justifyContent: 'center', alignItems: 'center', padding: 8 },
++  folderName: { color: '#f0e68c', textAlign: 'center', fontWeight: '700', marginTop: 8 },
++  folderDraftCell: { flex: 1, margin: 5, aspectRatio: 0.75, borderWidth: 1, borderStyle: 'dashed', borderColor: '#d4af37', borderRadius: 8, backgroundColor: 'rgba(255,255,255,0.92)', justifyContent: 'center', alignItems: 'center', padding: 8 },
++  folderNameInput: { width: '100%', borderBottomWidth: 1, borderColor: '#555', textAlign: 'center', marginTop: 8, padding: 4 },
++  folderDraftActions: { flexDirection: 'row', gap: 12, marginTop: 12 },
+   modalBackdrop: {
+     flex: 1,
+     backgroundColor: 'rgba(0,0,0,0.45)',
diff --git a/src/store/appSettingsStore.js b/src/store/appSettingsStore.js
index ed7be6a..94127b7 100644
--- a/src/store/appSettingsStore.js
+++ b/src/store/appSettingsStore.js
@@ -9,6 +9,8 @@ const useAppSettingsStore = create(
     (set) => ({
       randomWeaponDurabilityEnabled: false,
       weaponDurabilityLossPer10Shots: 1,
+      characterFoldersEnabled: false,
+      setCharacterFoldersEnabled: (enabled) => set({ characterFoldersEnabled: Boolean(enabled) }),
       setRandomWeaponDurabilityEnabled: (enabled) => set({ randomWeaponDurabilityEnabled: Boolean(enabled) }),
       setWeaponDurabilityLossPer10Shots: (value) => set({ weaponDurabilityLossPer10Shots: clampLoss(value) }),
     }),
@@ -17,6 +19,7 @@ const useAppSettingsStore = create(
       storage: createJSONStorage(() => AsyncStorage),
       partialize: (state) => ({
         randomWeaponDurabilityEnabled: state.randomWeaponDurabilityEnabled,
+        characterFoldersEnabled: state.characterFoldersEnabled,
         weaponDurabilityLossPer10Shots: state.weaponDurabilityLossPer10Shots,
       }),
     },
diff --git a/styles/HomeScreen.styles.js b/styles/HomeScreen.styles.js
index 0128b4b..53962d6 100644
--- a/styles/HomeScreen.styles.js
+++ b/styles/HomeScreen.styles.js
@@ -155,6 +155,25 @@ const styles = StyleSheet.create({
   },
   characterLevel: { fontSize: 10, color: '#555', textAlign: 'center', paddingBottom: 4 },
   emptyCell: { flex: 1, margin: 5, aspectRatio: 0.75 },
+  disabledCell: { opacity: 0.45 },
+  dragHandle: { position: 'absolute', top: 5, left: 5, zIndex: 20, width: 28, height: 28, borderRadius: 14, backgroundColor: '#f0e68c', alignItems: 'center', justifyContent: 'center' },
+  draggingOverlay: { ...StyleSheet.absoluteFillObject, backgroundColor: 'rgba(212,175,55,0.35)', zIndex: 15, alignItems: 'center', justifyContent: 'center' },
+  draggingText: { fontSize: 36, color: '#111827' },
+  folderCell: { flex: 1, margin: 5, aspectRatio: 0.75, borderWidth: 1, borderColor: '#d4af37', borderRadius: 8, backgroundColor: '#111827', justifyContent: 'center', alignItems: 'center', padding: 8 },
+  folderDropTarget: { borderWidth: 3, backgroundColor: '#374151' },
+  folderName: { color: '#f0e68c', textAlign: 'center', fontWeight: '700', marginTop: 8 },
+  folderCount: { color: '#d1d5db', fontSize: 11, marginTop: 4 },
+  folderDeleteButton: { position: 'absolute', top: 5, right: 5, width: 24, height: 24, borderRadius: 12, backgroundColor: '#fff', alignItems: 'center', justifyContent: 'center', zIndex: 2 },
+  folderDraftCell: { flex: 1, margin: 5, aspectRatio: 0.75, borderWidth: 1, borderStyle: 'dashed', borderColor: '#d4af37', borderRadius: 8, backgroundColor: 'rgba(255,255,255,0.92)', justifyContent: 'center', alignItems: 'center', padding: 8 },
+  folderNameInput: { width: '100%', borderBottomWidth: 1, borderColor: '#555', textAlign: 'center', marginTop: 8, padding: 4 },
+  folderDraftActions: { flexDirection: 'row', gap: 12, marginTop: 12 },
+  folderHeader: { marginHorizontal: 16, marginBottom: 8, padding: 10, borderWidth: 1, borderColor: '#d4af37', borderRadius: 8, backgroundColor: '#111827' },
+  folderBack: { color: '#93c5fd', fontWeight: '700' },
+  folderHeaderTitle: { color: '#f0e68c', fontSize: 18, fontWeight: '700', marginTop: 6 },
+  folderHeaderCount: { color: '#d1d5db', marginTop: 2 },
+  rootDropZone: { marginTop: 10, padding: 10, borderWidth: 1, borderStyle: 'dashed', borderColor: '#d4af37', borderRadius: 6 },
+  rootDropText: { color: '#f0e68c', textAlign: 'center' },
+  moveHint: { color: '#f0e68c', textAlign: 'center', marginHorizontal: 16, marginBottom: 8 },
   modalBackdrop: {
     flex: 1,
     backgroundColor: 'rgba(0,0,0,0.45)',
